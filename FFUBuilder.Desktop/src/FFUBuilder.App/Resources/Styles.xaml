<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Tab item style for left-side navigation -->
    <Style x:Key="NavTabItem" TargetType="TabItem">
        <Setter Property="MinWidth" Value="120" />
        <Setter Property="MinHeight" Value="40" />
        <Setter Property="Padding" Value="16,8" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
    </Style>

    <!-- Status colors -->
    <SolidColorBrush x:Key="StatusPassBrush" Color="#2E7D32" />
    <SolidColorBrush x:Key="StatusFailBrush" Color="#C62828" />
    <SolidColorBrush x:Key="StatusWarningBrush" Color="#F57F17" />
    <SolidColorBrush x:Key="StatusPendingBrush" Color="#757575" />

    <!-- Validation error template for TextBox -->
    <Style x:Key="ValidatedTextBox" TargetType="TextBox">
        <Setter Property="Margin" Value="0,2,0,2" />
        <Style.Triggers>
            <Trigger Property="Validation.HasError" Value="True">
                <Setter Property="BorderBrush" Value="#C62828" />
                <Setter Property="BorderThickness" Value="2" />
                <Setter Property="ToolTip" Value="{Binding RelativeSource={RelativeSource Self}, Path=(Validation.Errors)[0].ErrorContent}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- GroupBox header style for settings sections -->
    <Style x:Key="SettingsGroupBox" TargetType="GroupBox">
        <Setter Property="Margin" Value="0,0,0,12" />
        <Setter Property="Padding" Value="8" />
    </Style>

    <!-- Settings label style -->
    <Style x:Key="SettingsLabel" TargetType="TextBlock">
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Margin" Value="0,4,8,4" />
    </Style>

</ResourceDictionary>
