---
phase: 46-dashboard-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - FFUDevelopment/BuildFFUVM_UI.xaml
  - FFUDevelopment/FFUUI.Core/FFUUI.Core.Initialize.psm1
autonomous: true

must_haves:
  truths:
    - "Home tab contains category Expander controls for System, Hypervisor, Build Tools, Network, Optimization"
    - "Summary status banner with colored background exists at top of Home tab"
    - "Refresh button exists on Home tab"
    - "Progress indicator panel (indeterminate bar + status text) exists on Home tab"
    - "All new Home tab controls are registered in Initialize-UIControls"
  artifacts:
    - path: "FFUDevelopment/BuildFFUVM_UI.xaml"
      provides: "Home tab XAML layout with dashboard controls"
      contains: "stackDashboardContainer"
    - path: "FFUDevelopment/FFUUI.Core/FFUUI.Core.Initialize.psm1"
      provides: "Control registration for all dashboard UI elements"
      contains: "borderSummaryStatus"
  key_links:
    - from: "BuildFFUVM_UI.xaml"
      to: "FFUUI.Core.Initialize.psm1"
      via: "x:Name references matched by FindName calls"
      pattern: "FindName.*borderSummaryStatus"
---

<objective>
Create the Home tab XAML layout for the pre-flight readiness dashboard and register all new controls in Initialize-UIControls.

Purpose: Establish the visual structure that all subsequent plans wire behavior into. Without the XAML controls and their registration, no dashboard functionality can be connected.

Output: BuildFFUVM_UI.xaml with complete Home tab layout; FFUUI.Core.Initialize.psm1 with all dashboard control registrations.
</objective>

<execution_context>
@C:\Users\joanderson\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\joanderson\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/46-dashboard-foundation/46-CONTEXT.md
@.planning/phases/46-dashboard-foundation/46-RESEARCH.md
@FFUDevelopment/BuildFFUVM_UI.xaml
@FFUDevelopment/FFUUI.Core/FFUUI.Core.Initialize.psm1
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Home tab placeholder with dashboard XAML layout</name>
  <files>FFUDevelopment/BuildFFUVM_UI.xaml</files>
  <action>
Replace the existing Home tab content (the placeholder `TextBlock Text="Welcome to FFU Builder"` inside the Home TabItem) with the full dashboard layout. The Home tab currently looks like:

```xml
<TabItem Header="Home" Padding="20">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="10">
            <TextBlock Text="Welcome to FFU Builder" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Grid>
    </ScrollViewer>
</TabItem>
```

Replace the inner Grid with a StackPanel named `stackDashboardContainer` containing:

1. **Summary Status Banner** (`borderSummaryStatus`): Full-width Border with TextBlock (`txtSummaryStatus`). Default text "Checking system readiness..." with FontSize 16, FontWeight Bold. Background starts as LightGray.

2. **Progress Panel** (`pnlDashboardProgress`): StackPanel with Visibility="Collapsed" containing:
   - Indeterminate ProgressBar (`progressDashboard`) Height 4
   - TextBlock (`txtDashboardProgressStatus`) for "Running check 5 of 12: Windows ADK Installation..."

3. **Refresh Button** (`btnRefreshChecks`): Button with Content="Refresh Checks", Width 140, HorizontalAlignment Left.

4. **Five category Expander controls**, each using `Style="{StaticResource MinimalExpanderNoHighlightStyle}"`:
   - `expSystem` (System) - IsExpanded True
   - `expHypervisor` (Hypervisor)
   - `expBuildTools` (Build Tools)
   - `expNetwork` (Network)
   - `expOptimization` (Optimization)

   Each Expander Header is a horizontal StackPanel with:
   - Status icon TextBlock (`txt{Category}Icon`) - default text "○", FontSize 14, Foreground Gray
   - Category name TextBlock - FontWeight SemiBold
   - Summary text TextBlock (`txt{Category}Summary`) - default "(checking...)", Foreground Gray, FontStyle Italic

   Each Expander Content is a StackPanel (`pnl{Category}Checks`) with Margin "20,5,0,5" that will be populated dynamically.

Use consistent margins: summary banner Margin "0,0,0,8", progress panel Margin "0,0,0,8", refresh button Margin "0,0,0,10", each Expander Margin "0,2".

Keep the ScrollViewer wrapper. Do NOT modify any other tabs or the Window.Resources section.
  </action>
  <verify>
Open BuildFFUVM_UI.xaml and confirm:
- Home TabItem contains stackDashboardContainer
- 5 Expander controls exist (expSystem, expHypervisor, expBuildTools, expNetwork, expOptimization)
- borderSummaryStatus, txtSummaryStatus, progressDashboard, btnRefreshChecks all present
- Each category has icon TextBlock, name TextBlock, and summary TextBlock
- XAML is well-formed (no unclosed tags)
  </verify>
  <done>Home tab XAML contains complete dashboard layout with summary banner, progress indicator, refresh button, and 5 category Expander controls with proper naming convention.</done>
</task>

<task type="auto">
  <name>Task 2: Register all dashboard controls in Initialize-UIControls</name>
  <files>FFUDevelopment/FFUUI.Core/FFUUI.Core.Initialize.psm1</files>
  <action>
In the `Initialize-UIControls` function in FFUUI.Core.Initialize.psm1, add FindName registrations for all new dashboard controls. Add them in a clearly labeled section after the existing control registrations.

Add a comment block `# ---------- Home Tab: Dashboard Controls ----------` and register:

**Summary and Progress:**
- `$State.Controls.borderSummaryStatus = $window.FindName('borderSummaryStatus')`
- `$State.Controls.txtSummaryStatus = $window.FindName('txtSummaryStatus')`
- `$State.Controls.pnlDashboardProgress = $window.FindName('pnlDashboardProgress')`
- `$State.Controls.progressDashboard = $window.FindName('progressDashboard')`
- `$State.Controls.txtDashboardProgressStatus = $window.FindName('txtDashboardProgressStatus')`
- `$State.Controls.btnRefreshChecks = $window.FindName('btnRefreshChecks')`

**Category Expanders (5 categories):**
For each category (System, Hypervisor, BuildTools, Network, Optimization):
- `$State.Controls.exp{Category} = $window.FindName('exp{Category}')`
- `$State.Controls.txt{Category}Icon = $window.FindName('txt{Category}Icon')`
- `$State.Controls.txt{Category}Summary = $window.FindName('txt{Category}Summary')`
- `$State.Controls.pnl{Category}Checks = $window.FindName('pnl{Category}Checks')`

That's 6 + (4 * 5) = 26 new control registrations total.

Do NOT modify any existing control registrations. Just add the new section.
  </action>
  <verify>
Grep for `borderSummaryStatus` and `expSystem` and `pnlOptimizationChecks` in FFUUI.Core.Initialize.psm1 to confirm all controls registered. Count that 26 new FindName lines exist in the dashboard section.
  </verify>
  <done>All 26 dashboard controls are registered in Initialize-UIControls with clear section labeling. FindName keys match XAML x:Name attributes exactly.</done>
</task>

</tasks>

<verification>
1. XAML is well-formed: Open in any XML validator or load the UI to confirm no parse errors
2. All x:Name values in XAML have matching FindName calls in Initialize-UIControls
3. No existing controls or tabs were modified
4. MinimalExpanderNoHighlightStyle is referenced correctly (already defined in Window.Resources)
</verification>

<success_criteria>
- Home tab displays structured layout with summary banner, progress area, refresh button, and 5 category sections
- All 26 new controls are accessible via $State.Controls in PowerShell code
- Existing UI functionality is unchanged (other tabs, buttons, etc.)
</success_criteria>

<output>
After completion, create `.planning/phases/46-dashboard-foundation/46-01-SUMMARY.md`
</output>
