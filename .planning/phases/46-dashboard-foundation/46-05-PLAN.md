---
phase: 46-dashboard-foundation
plan: 05
type: execute
wave: 3
depends_on: ["46-03", "46-04"]
files_modified:
  - FFUDevelopment/FFUUI.Core/FFUUI.Core.psd1
  - FFUDevelopment/version.json
  - CHANGELOG_FORK.md
autonomous: true

must_haves:
  truths:
    - "FFUUI.Core module version has been bumped"
    - "Main FFU Builder version in version.json has been bumped"
    - "CHANGELOG_FORK.md contains Phase 46 entry with all 8 DASH requirements"
    - "All Pester tests pass (existing + new dashboard tests)"
    - "verify-app agent confirms no regressions"
  artifacts:
    - path: "FFUDevelopment/FFUUI.Core/FFUUI.Core.psd1"
      provides: "Updated module version"
    - path: "FFUDevelopment/version.json"
      provides: "Updated main version and FFUUI.Core module version"
    - path: "CHANGELOG_FORK.md"
      provides: "Phase 46 changelog entry"
      contains: "Dashboard Foundation"
  key_links:
    - from: "FFUDevelopment/version.json"
      to: "FFUDevelopment/FFUUI.Core/FFUUI.Core.psd1"
      via: "version synchronization"
      pattern: "FFUUI.Core"
---

<objective>
Bump versions, update changelog, and run full verification to confirm Phase 46 is complete and regression-free.

Purpose: Ensure proper version tracking and verify all 8 DASH requirements are met before marking the phase complete.

Output: Updated version numbers, changelog entry, verification confirmation.
</objective>

<execution_context>
@C:\Users\joanderson\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\joanderson\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/46-dashboard-foundation/46-01-SUMMARY.md
@.planning/phases/46-dashboard-foundation/46-02-SUMMARY.md
@.planning/phases/46-dashboard-foundation/46-03-SUMMARY.md
@.planning/phases/46-dashboard-foundation/46-04-SUMMARY.md
@FFUDevelopment/version.json
@FFUDevelopment/FFUUI.Core/FFUUI.Core.psd1
@CHANGELOG_FORK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump versions and update changelog</name>
  <files>
    FFUDevelopment/FFUUI.Core/FFUUI.Core.psd1
    FFUDevelopment/version.json
    CHANGELOG_FORK.md
  </files>
  <action>
**FFUUI.Core.psd1 version bump:**
Read the current ModuleVersion in FFUUI.Core.psd1. Bump it by MINOR (e.g., if 1.0.0, bump to 1.1.0). This is a MINOR bump because the Dashboard submodule adds significant new user-facing functionality (6 new exported functions, entirely new UI capability).

Add release notes to the .psd1 PrivateData.PSData.ReleaseNotes field:
```
'v{NEW_VERSION}: Pre-flight readiness dashboard - 6 new functions for Home tab dashboard UI (Get-CheckCategory, Update-DashboardCheckUI, Update-CategorySummary, Update-SummaryStatus, Update-BuildButtonState, Clear-DashboardResults). Dashboard submodule FFUUI.Core.Dashboard.psm1.'
```

**version.json update:**
1. Read FFUDevelopment/version.json
2. Bump the main version PATCH (e.g., 1.11.0 -> 1.11.1) -- subcomponent change requires minimum PATCH bump
3. Update buildDate to today's date (2026-02-06)
4. Update the FFUUI.Core module version in the modules section to match the .psd1 version
5. Add a description update for FFUUI.Core mentioning "Pre-flight readiness dashboard"

**CHANGELOG_FORK.md update:**
Add a new entry at the top of the changelog under the current milestone section:

```markdown
### Phase 46: Dashboard Foundation (v1.11.1)
**Date:** 2026-02-06

**Features:**
- **DASH-01:** Home tab displays grouped pre-flight check results organized by category (System, Hypervisor, Build Tools, Network, Optimization)
- **DASH-02:** Each check displays visual status indicator (pass/fail/warning) with color coding (green/yellow/red)
- **DASH-03:** Dashboard runs all checks automatically on application launch without blocking the UI
- **DASH-04:** Refresh button re-runs all checks (disabled during active builds)
- **DASH-05:** Summary status banner shows "Ready to Build" or "N Critical Issues, M Warnings"
- **DASH-06:** Failed checks display actionable error messages with remediation guidance from FFU.Preflight
- **DASH-07:** Progress indicator shows "Running check N of M: CheckName..." while checks execute
- **DASH-08:** Build button disabled on critical failures; warnings show confirmation dialog

**Files Modified:**
- `BuildFFUVM_UI.xaml` — Home tab XAML layout with dashboard controls
- `BuildFFUVM_UI.ps1` — Background execution, polling, event handlers, build gating
- `FFUUI.Core/FFUUI.Core.Dashboard.psm1` — NEW: 6 dashboard helper functions
- `FFUUI.Core/FFUUI.Core.psd1` — Dashboard submodule registration
- `Tests/Unit/FFUUI.Core.Dashboard.Tests.ps1` — NEW: 30+ Pester tests
```
  </action>
  <verify>
1. Read FFUUI.Core.psd1 and confirm ModuleVersion was bumped
2. Read version.json and confirm main version was bumped and FFUUI.Core version matches psd1
3. Read CHANGELOG_FORK.md and confirm Phase 46 entry exists with all 8 DASH items
  </verify>
  <done>All versions bumped. Changelog contains complete Phase 46 entry with all 8 DASH requirements documented.</done>
</task>

<task type="auto">
  <name>Task 2: Run full verification (Pester tests + verify-app)</name>
  <files></files>
  <action>
Run the full test suite to confirm no regressions:

```powershell
# Run all dashboard tests
Invoke-Pester -Path 'Tests/Unit/FFUUI.Core.Dashboard.Tests.ps1' -Output Detailed

# Run any existing FFUUI.Core tests
Invoke-Pester -Path 'Tests/Unit/' -Output Detailed 2>&1 | Select-Object -Last 20
```

Then invoke the verify-app agent:
```
Act as verify-app agent (read .claude/agents/verify-app.md). Run full regression verification.
```

This should confirm:
1. All Pester tests pass (existing + new)
2. No new PSScriptAnalyzer errors
3. All modules import successfully (including FFUUI.Core with new Dashboard submodule)
4. Code coverage maintained or improved

If any failures, fix them before marking this task complete.

**Critical checks:**
- FFUUI.Core imports cleanly with Dashboard submodule
- FFUUI.Core.Dashboard.psm1 exports all 6 functions
- Get-CheckCategory maps all known checks correctly
- No PSScriptAnalyzer errors on new files
  </action>
  <verify>
```powershell
# Final verification
$result = Invoke-Pester -Path 'Tests/Unit/' -Output Detailed -PassThru
Write-Host "Tests: $($result.PassedCount) passed, $($result.FailedCount) failed"
$result.FailedCount -eq 0
```
  </verify>
  <done>All Pester tests pass. verify-app confirms no regressions. Phase 46 is complete and verified.</done>
</task>

</tasks>

<verification>
1. FFUUI.Core.psd1 version bumped (MINOR)
2. version.json main version bumped (PATCH minimum)
3. CHANGELOG_FORK.md has Phase 46 entry with all 8 DASH requirements
4. All Pester tests pass (0 failures)
5. PSScriptAnalyzer clean on new/modified files
6. All modules import successfully
</verification>

<success_criteria>
- Version numbers correctly bumped and synchronized
- Changelog documents all 8 DASH requirements
- Full test suite passes with zero failures
- verify-app confirms no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/46-dashboard-foundation/46-05-SUMMARY.md`
</output>
