---
phase: 46-ffu-artifactscanner-module
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - FFUDevelopment/Modules/FFU.ArtifactScanner/Classes/ArtifactScanner.Classes.ps1
  - FFUDevelopment/Modules/FFU.ArtifactScanner/FFU.ArtifactScanner.psm1
  - FFUDevelopment/Modules/FFU.ArtifactScanner/FFU.ArtifactScanner.psd1
  - Tests/Unit/FFU.ArtifactScanner.Tests.ps1
autonomous: true
must_haves:
  truths:
    - "ArtifactScanner module imports without error"
    - "Get-ArtifactMetadata extracts WindowsVersion, SKU, Architecture from FFU via DISM mock"
    - "Get-ArtifactMetadata falls back to filename parsing when DISM/WIMMount unavailable"
    - "Architecture integer (0/9/12) is mapped to string (x86/x64/arm64)"
    - "All class types (ArtifactManifest, ArtifactResult, FFUMetadata, etc.) are instantiable"
  artifacts:
    - path: "FFUDevelopment/Modules/FFU.ArtifactScanner/Classes/ArtifactScanner.Classes.ps1"
      provides: "Enums and data classes for artifact scanning"
      contains: "enum ArtifactStatus"
    - path: "FFUDevelopment/Modules/FFU.ArtifactScanner/FFU.ArtifactScanner.psm1"
      provides: "Module root with Get-ArtifactMetadata public function"
      exports: ["Get-ArtifactMetadata"]
    - path: "FFUDevelopment/Modules/FFU.ArtifactScanner/FFU.ArtifactScanner.psd1"
      provides: "Module manifest with RequiredModules and FunctionsToExport"
      contains: "FFU.ArtifactScanner"
    - path: "Tests/Unit/FFU.ArtifactScanner.Tests.ps1"
      provides: "Pester tests for module import and Get-ArtifactMetadata"
  key_links:
    - from: "FFU.ArtifactScanner.psm1"
      to: "Classes/ArtifactScanner.Classes.ps1"
      via: "dot-sourcing"
      pattern: "Join-Path.*ArtifactScanner.Classes.ps1"
    - from: "FFU.ArtifactScanner.psm1"
      to: "FFU.Core"
      via: "RequiredModules dependency"
      pattern: "WriteLog"
---

<!-- Requirements: VALID-02 -->

<objective>
Create the FFU.ArtifactScanner module scaffold with data contract classes (enums, result types, manifest) and implement Get-ArtifactMetadata as the first public function.

Purpose: Establishes the typed data contract that Phase 47 (pipeline), Phase 48 (UI columns), and Phase 49 (controls) all depend on. Get-ArtifactMetadata is independently testable and addresses VALID-02 (FFU metadata extraction via DISM).

Output: Working module at Modules/FFU.ArtifactScanner/ with classes, manifest, and Get-ArtifactMetadata function passing Pester tests.
</objective>

<execution_context>
@C:/Users/joanderson/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/joanderson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-ffu-artifactscanner-module/46-CONTEXT.md
@.planning/phases/46-ffu-artifactscanner-module/46-RESEARCH.md

@FFUDevelopment/Modules/FFU.Hypervisor/FFU.Hypervisor.psm1
@FFUDevelopment/Modules/FFU.Hypervisor/FFU.Hypervisor.psd1
@FFUDevelopment/Modules/FFU.Hypervisor/Classes/VMConfiguration.ps1

<interfaces>
<!-- Existing module patterns to follow -->

From FFU.Hypervisor/FFU.Hypervisor.psm1 (lines 27-43):
```powershell
$script:ModuleRoot = $PSScriptRoot
$script:ClassesPath = Join-Path $ModuleRoot 'Classes'
. (Join-Path $ClassesPath 'VMConfiguration.ps1')
```

From FFU.Core (WriteLog pattern):
```powershell
# ThreadJob-safe logging
if ($function:WriteLog) {
    WriteLog $msg
} else {
    Write-Verbose $msg
}
```

From FFU.Preflight (WIMMount gate):
```powershell
$wimMountResult = Test-FFUWimMount -AttemptRemediation:$true
# Returns object with .Status property ('Passed' or 'Failed')
```

DISM Get-WindowsImage returns:
```powershell
# .Architecture => integer: 0=x86, 9=x64, 12=arm64
# .Version      => '10.0.22621.xxx'
# .EditionId    => 'Professional'
# .ImageName    => 'Windows 11 Pro'
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create module scaffold with data contract classes and manifest</name>
  <files>
    FFUDevelopment/Modules/FFU.ArtifactScanner/Classes/ArtifactScanner.Classes.ps1
    FFUDevelopment/Modules/FFU.ArtifactScanner/FFU.ArtifactScanner.psd1
    FFUDevelopment/Modules/FFU.ArtifactScanner/FFU.ArtifactScanner.psm1
    Tests/Unit/FFU.ArtifactScanner.Tests.ps1
  </files>
  <behavior>
    - Test: Module imports without error (`Import-Module FFU.ArtifactScanner -Force` succeeds)
    - Test: [ArtifactStatus] enum has values: Found, Missing, Error, Degraded
    - Test: [ArtifactType] enum has values: FFU, DeployISO, Drivers, PPKG, Unattend, Autopilot, AppsISO
    - Test: [ArtifactResult]::new() creates object with Files list initialized
    - Test: [ArtifactManifest] has Warnings, FoundCount, MissingCount, ErrorCount, IsReady properties
    - Test: [FFUMetadata] has WindowsVersion, WindowsSKU, Architecture, ImageName, BuildDate, MetadataSource, ErrorMessage
    - Test: [CompatibilityWarning] has Severity, Message, AffectedArtifacts properties
    - Test: [ArtifactFileEntry] has FilePath, FileSizeBytes, LastWriteTime properties
  </behavior>
  <action>
    1. Create directory `FFUDevelopment/Modules/FFU.ArtifactScanner/Classes/`

    2. Create `Classes/ArtifactScanner.Classes.ps1` with ALL enums and classes per CONTEXT.md locked decisions:
       - `enum ArtifactStatus { Found; Missing; Error; Degraded }`
       - `enum ArtifactType { FFU; DeployISO; Drivers; PPKG; Unattend; Autopilot; AppsISO }`
       - `class ArtifactFileEntry` with [string]FilePath, [long]FileSizeBytes, [DateTime]LastWriteTime
       - `class FFUMetadata` with [string]WindowsVersion, [string]WindowsSKU, [string]Architecture, [string]ImageName, [DateTime]BuildDate, [string]MetadataSource (DISM or Filename), [string]ErrorMessage
       - `class ArtifactResult` with [ArtifactType]ArtifactType, [ArtifactStatus]Status, [string]ErrorMessage, [int]AgeDays, [string]FilePath, [long]FileSizeBytes, [DateTime]LastWriteTime, [System.Collections.Generic.List[ArtifactFileEntry]]Files, [int]FileCount, [long]TotalSizeBytes, [bool]IsPrimary, [FFUMetadata]Metadata. Constructor initializes Files list.
       - `class CompatibilityWarning` with [string]Severity, [string]Message, [string[]]AffectedArtifacts
       - `class ArtifactManifest` with [string]BasePath, [DateTime]ScanTimestamp, [ArtifactResult[]]FFUFiles, [ArtifactResult]DeployISO, [ArtifactResult]Drivers, [ArtifactResult[]]PPKGFiles, [ArtifactResult[]]UnattendFiles, [ArtifactResult[]]AutopilotFiles, [ArtifactResult]AppsISO, [CompatibilityWarning[]]Warnings, [int]FoundCount, [int]MissingCount, [int]ErrorCount, [bool]IsReady

    3. Create `FFU.ArtifactScanner.psm1` following FFU.Hypervisor pattern:
       - `#Requires -Version 7.0`
       - Module root variables: `$script:ModuleRoot`, `$script:ClassesPath`
       - Dot-source `Classes/ArtifactScanner.Classes.ps1`
       - Stub public functions: `Get-ArtifactMetadata`, `Find-FFUArtifacts`, `Test-ArtifactCompatibility` (stubs return $null for now, implemented in this and next plan)
       - Include `ConvertTo-ArchitectureString` private helper (switch: 0->x86, 9->x64, 12->arm64, default->Unknown)
       - Use `[DateTime]::Now` not `Get-Date` (ThreadJob compatibility)
       - Use safe WriteLog pattern (`if ($function:WriteLog) { WriteLog $msg } else { Write-Verbose $msg }`)

    4. Create `FFU.ArtifactScanner.psd1` manifest:
       - Generate new GUID
       - RootModule = 'FFU.ArtifactScanner.psm1'
       - ModuleVersion = '1.0.0'
       - PowerShellVersion = '7.0'
       - RequiredModules = @(@{ModuleName='FFU.Core'; ModuleVersion='1.0.0'}, @{ModuleName='FFU.Preflight'; ModuleVersion='1.0.0'})
       - FunctionsToExport = @('Find-FFUArtifacts', 'Get-ArtifactMetadata', 'Test-ArtifactCompatibility', 'New-ArtifactManifest', 'New-ArtifactResult')
       - Include factory functions in exports for cross-scope class instantiation (per Pitfall 4 from RESEARCH.md)

    5. Create initial Pester test file `Tests/Unit/FFU.ArtifactScanner.Tests.ps1`:
       - Module import test block
       - Enum value tests for ArtifactStatus and ArtifactType
       - Class instantiation tests for all 6 classes
       - Property existence tests for key types
       - Tests for Get-ArtifactMetadata (added in Task 2)
  </action>
  <verify>
    <automated>pwsh -Command "Import-Module 'D:\claude\FFUBuilder\FFUDevelopment\Modules\FFU.ArtifactScanner\FFU.ArtifactScanner.psd1' -Force -ErrorAction Stop; Write-Host 'Module imported successfully'"</automated>
  </verify>
  <done>Module scaffold exists with all classes/enums defined. Module imports without error. All class types are instantiable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Get-ArtifactMetadata with DISM and filename fallback</name>
  <files>
    FFUDevelopment/Modules/FFU.ArtifactScanner/FFU.ArtifactScanner.psm1
    Tests/Unit/FFU.ArtifactScanner.Tests.ps1
  </files>
  <behavior>
    - Test: Get-ArtifactMetadata with mocked Get-WindowsImage returns correct WindowsVersion, SKU, Architecture
    - Test: Architecture integer 9 maps to 'x64', 12 maps to 'arm64', 0 maps to 'x86'
    - Test: When DISM fails, returns partial metadata with MetadataSource='Filename' and ErrorMessage populated
    - Test: MetadataSource is 'DISM' on successful extraction
    - Test: Filename like 'Windows11_23H2_x64_Pro.ffu' yields parsed architecture and version info
    - Test: Factory function New-ArtifactManifest returns [ArtifactManifest] instance
    - Test: Factory function New-ArtifactResult returns [ArtifactResult] instance with initialized Files list
  </behavior>
  <action>
    Implement `Get-ArtifactMetadata` in FFU.ArtifactScanner.psm1:

    1. **Function signature:**
       ```
       function Get-ArtifactMetadata {
           [CmdletBinding()]
           [OutputType([FFUMetadata])]
           param(
               [Parameter(Mandatory)]
               [string]$FFUPath,
               [Parameter()]
               [bool]$WimMountAvailable = $true
           )
       }
       ```

    2. **DISM extraction path** (when WimMountAvailable is true):
       - `Import-Module DISM -ErrorAction Stop`
       - `$imageInfo = Get-WindowsImage -ImagePath $FFUPath -Index 1 -ErrorAction Stop`
       - Map `$imageInfo.Architecture` integer to string via `ConvertTo-ArchitectureString`
       - Populate FFUMetadata: WindowsVersion from .Version, WindowsSKU from .EditionId, Architecture from mapped string, ImageName from .ImageName, BuildDate from file LastWriteTime, MetadataSource='DISM'
       - Wrap in try/catch; on failure fall through to filename parsing

    3. **Filename parsing fallback:**
       - Parse FFU filename with regex patterns to extract architecture (x64, arm64, x86) and version hints (23H2, 24H2, etc.)
       - Set MetadataSource='Filename', ErrorMessage with DISM failure reason
       - Return partial FFUMetadata with whatever could be parsed

    4. **Factory functions** (for cross-scope class instantiation per Pitfall 4):
       - `New-ArtifactManifest`: Returns `[ArtifactManifest]::new()` with ScanTimestamp set to `[DateTime]::Now`
       - `New-ArtifactResult`: Returns `[ArtifactResult]::new()` with specified ArtifactType parameter

    5. **Add tests** to FFU.ArtifactScanner.Tests.ps1:
       - Mock `Get-WindowsImage` to return test data with Architecture=9, Version='10.0.22631.1', EditionId='Professional', ImageName='Windows 11 Pro'
       - Mock `Import-Module` for DISM
       - Test DISM success path
       - Test DISM failure path (mock throws, verify filename fallback)
       - Test architecture mapping for all three values (0, 9, 12)
       - Test factory functions return correct types
  </action>
  <verify>
    <automated>pwsh -Command "& 'D:\claude\FFUBuilder\Tests\Unit\Invoke-PesterTests.ps1' -Module 'FFU.ArtifactScanner'"</automated>
  </verify>
  <done>Get-ArtifactMetadata extracts FFU metadata via DISM with filename fallback. All Pester tests pass. Factory functions work for cross-scope instantiation.</done>
</task>

</tasks>

<verification>
1. `Import-Module FFU.ArtifactScanner -Force` succeeds without error
2. All enum values accessible: `[ArtifactStatus]::Found`, `[ArtifactType]::FFU`, etc.
3. All classes instantiable: `[ArtifactManifest]::new()`, `[ArtifactResult]::new()`, etc.
4. `Get-ArtifactMetadata` returns [FFUMetadata] with correct properties
5. Pester tests pass: `.\Tests\Unit\Invoke-PesterTests.ps1 -Module 'FFU.ArtifactScanner'`
</verification>

<success_criteria>
- Module scaffold at FFUDevelopment/Modules/FFU.ArtifactScanner/ with Classes/, .psm1, .psd1
- All 6 data contract classes and 2 enums defined and instantiable
- Get-ArtifactMetadata works with DISM primary and filename fallback
- Architecture integer-to-string mapping correct for x86/x64/arm64
- Factory functions (New-ArtifactManifest, New-ArtifactResult) exported
- All Pester tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/46-ffu-artifactscanner-module/46-01-SUMMARY.md`
</output>
