---
phase: 51-settings-configuration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - FFUBuilder.Desktop/src/FFUBuilder.App/Models/BuildConfiguration.cs
  - FFUBuilder.Desktop/src/FFUBuilder.App/ViewModels/SettingsViewModel.cs
  - FFUBuilder.Desktop/src/FFUBuilder.App/Services/Interfaces/IConfigurationService.cs
  - FFUBuilder.Desktop/src/FFUBuilder.App/Services/ConfigurationService.cs
autonomous: true

must_haves:
  truths:
    - "BuildConfiguration model has JsonProperty attributes matching every non-deprecated property in the config schema"
    - "SettingsViewModel exposes all config properties with two-way binding support"
    - "SettingsViewModel validates key fields (paths, numeric ranges, required fields) via INotifyDataErrorInfo"
    - "ConfigurationService produces JSON output compatible with PowerShell UI (matching key names, types, byte values for Memory/Disksize)"
    - "Reset to defaults populates all properties with schema-defined default values"
  artifacts:
    - path: "FFUBuilder.Desktop/src/FFUBuilder.App/Models/BuildConfiguration.cs"
      provides: "Complete config model with ~70 non-deprecated properties"
      contains: "JsonProperty"
    - path: "FFUBuilder.Desktop/src/FFUBuilder.App/ViewModels/SettingsViewModel.cs"
      provides: "SettingsViewModel with load, save, reset commands and validation"
      contains: "INotifyDataErrorInfo"
    - path: "FFUBuilder.Desktop/src/FFUBuilder.App/Services/ConfigurationService.cs"
      provides: "Load/Save with defaults factory method"
      contains: "CreateDefaults"
  key_links:
    - from: "SettingsViewModel"
      to: "IConfigurationService"
      via: "constructor injection"
      pattern: "IConfigurationService"
    - from: "SettingsViewModel"
      to: "BuildConfiguration"
      via: "Configuration property with PropertyChanged propagation"
      pattern: "BuildConfiguration"
---

<objective>
Expand the BuildConfiguration model to cover all ~70 non-deprecated config properties from the JSON schema, create a fully functional SettingsViewModel with load/save/reset commands and inline validation, and enhance ConfigurationService to produce PowerShell-compatible JSON.

Purpose: The model and ViewModel are the foundation for the Settings UI. Without a complete model matching the schema, the UI cannot bind to or persist all config properties. Without validation, users cannot get feedback on invalid inputs.

Output: Complete BuildConfiguration.cs with all properties, SettingsViewModel.cs with INotifyDataErrorInfo, enhanced ConfigurationService.cs with defaults factory.
</objective>

<execution_context>
@C:\Users\joanderson\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\joanderson\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Key reference files:
@FFUDevelopment/config/ffubuilder-config.schema.json — full JSON schema (source of truth for property names, types, defaults, enums, ranges)
@FFUDevelopment/config/Sample_default.json — default values
@FFUDevelopment/FFUUI.Core/FFUUI.Core.Config.psm1 — how PowerShell UI reads/writes config (Get-UIConfig shows exact JSON key names and value transformations)
@FFUBuilder.Desktop/src/FFUBuilder.App/Models/BuildConfiguration.cs — current minimal model (15 properties)
@FFUBuilder.Desktop/src/FFUBuilder.App/ViewModels/SettingsViewModel.cs — current placeholder
@FFUBuilder.Desktop/src/FFUBuilder.App/Services/ConfigurationService.cs — current load/save
@FFUBuilder.Desktop/src/FFUBuilder.App/Services/Interfaces/IConfigurationService.cs — current interface
@FFUBuilder.Desktop/src/FFUBuilder.App/ViewModels/DashboardViewModel.cs — reference for CommunityToolkit.Mvvm patterns
@FFUBuilder.Desktop/src/FFUBuilder.App/App.xaml.cs — DI registration (SettingsViewModel is Transient)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand BuildConfiguration model with all config properties</name>
  <files>FFUBuilder.Desktop/src/FFUBuilder.App/Models/BuildConfiguration.cs</files>
  <action>
Rewrite BuildConfiguration.cs to include ALL non-deprecated properties from ffubuilder-config.schema.json. Skip deprecated properties: AppsPath, DownloadDrivers, InstallWingetApps, OfficePath, Threads, Verbose.

For each property:
- Add `[JsonProperty("ExactJsonKeyName")]` attribute matching the schema key name exactly
- Use correct C# type: `bool` for booleans, `long` for Memory/Disksize (bytes), `int` for Processors/MaxUSBDrives/etc., `string` for strings, `string?` for nullable strings, `Dictionary<string, string>?` for Headers/AppsScriptVariables, `List<string>?` for AdditionalFFUFiles, `Dictionary<string, string>?` for USBDriveList
- Set default values matching the schema defaults

Property grouping (organize in this order with region comments):
1. **Schema/Meta**: configSchemaVersion
2. **Windows Settings**: WindowsRelease (int, default 11), WindowsSKU, WindowsVersion, WindowsArch, WindowsLang, MediaType, ProductKey, ISOPath, OptionalFeatures
3. **VM Settings**: VMName (default "YOURPC"), Memory (long, default 4294967296), Disksize (long, default 32212254720), Processors (int, default 4), VMLocation, VMSwitchName, VMHostIPAddress, LogicalSectorSizeBytes (int, default 512), VMShutdownTimeoutMinutes (int, default 60), FFUFileLockWaitSeconds (int, default 120), FFUFileLockRetryCount (int, default 3), FFUFileLockRetryDelaySeconds (int, default 10)
4. **Hypervisor Settings**: HypervisorType (default "HyperV"), ShowVMConsole (bool, default true), ForceVMwareDriverDownload (bool, default false), VMwareNetworkType (default "nat"), VMwareNicType (default "e1000e"), VirtualDiskFormat (default "VHDX"), VMwareSettings (nested object — use a VMwareSettingsModel class with NetworkType and NicType)
5. **Paths**: FFUDevelopmentPath, FFUCaptureLocation, FFUPrefix (default "_FFU"), CustomFFUNameTemplate, ShareName (default "FFUCaptureShare"), SharePath, Username (default "ffu_user"), ConfigFile, ExportConfigFile
6. **Apps/Office**: InstallApps (default true), InstallOffice (default true), AppListPath, UserAppListPath, OrchestrationPath, AppsScriptVariables, OfficeConfigXMLFile, CopyOfficeConfigXML, InjectUnattend
7. **Drivers**: InstallDrivers (default true), Make, Model, DriversFolder, DriversJsonPath, PEDriversFolder, CopyDrivers, CopyPEDrivers, UseDriversAsPEDrivers, CompressDownloadedDriversToWim
8. **Updates**: UpdateLatestCU (default true), UpdateLatestNet (default true), UpdateLatestNet48 (default true), UpdateLatestDefender (default true), UpdateEdge (default true), UpdateOneDrive (default true), UpdateLatestMSRT (default true), UpdateLatestMicrocode, UpdatePreviewCU, IncludePreviewUpdates, BitsPriority (default "Normal")
9. **Build Options**: CompactOS (default true), Optimize (default true), CreateCaptureMedia (default true), CreateDeploymentMedia (default true), UpdateADK (default true)
10. **USB/Deployment**: BuildUSBDrive, MaxUSBDrives (int, default 5), AllowExternalHardDiskMedia, PromptExternalHardDiskMedia (default true), CopyAutopilot, CopyUnattend, CopyPPKG, CopyAdditionalFFUFiles, AdditionalFFUFiles, USBDriveList, RemoveFFU
11. **Cleanup**: CleanupAppsISO (default true), CleanupCaptureISO (default true), CleanupCaptureVM (default true), CleanupDeployISO (default true), CleanupDrivers, CleanupCurrentRunDownloads, RemoveApps, RemoveUpdates
12. **Network**: Headers, UserAgent, AllowVHDXCaching

Also create a nested `VMwareSettingsModel` class (can be in the same file):
```csharp
public class VMwareSettingsModel
{
    [JsonProperty("NetworkType")]
    public string NetworkType { get; set; } = "nat";

    [JsonProperty("NicType")]
    public string NicType { get; set; } = "e1000e";
}
```

Also add a static factory method:
```csharp
public static BuildConfiguration CreateDefaults() => new();
```

IMPORTANT compatibility notes:
- `WindowsRelease` must be `int` (not string) — the schema has it as integer with values like 10, 11, 2016, 2019 etc. The current model has it as string "24H2" which is WRONG for compatibility. Change to `int` with default `11`.
- `Memory` and `Disksize` are in BYTES (long) — PowerShell UI stores them as bytes in JSON (e.g., 4294967296 for 4GB). The ViewModel will handle GB display conversion.
- Remove `ApplyDrivers` and `OEM` from the current model — they are not in the schema. The schema uses `Make` instead of `OEM`.
- Keep `[JsonExtensionData]` attribute on a `Dictionary<string, object?>? AdditionalData` property to preserve unknown/future properties during round-trip.

Add `using Newtonsoft.Json.Linq;` if needed for JToken handling of the Headers dictionary.
  </action>
  <verify>
Build the project: `dotnet build FFUBuilder.Desktop/src/FFUBuilder.App/FFUBuilder.App.csproj`
Verify no compilation errors.
  </verify>
  <done>BuildConfiguration.cs contains ~70 properties matching the schema, with correct JsonProperty names, C# types, and default values. Has CreateDefaults() factory method. No compilation errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create SettingsViewModel with load/save/reset and validation</name>
  <files>
    FFUBuilder.Desktop/src/FFUBuilder.App/ViewModels/SettingsViewModel.cs
    FFUBuilder.Desktop/src/FFUBuilder.App/Services/Interfaces/IConfigurationService.cs
    FFUBuilder.Desktop/src/FFUBuilder.App/Services/ConfigurationService.cs
    FFUBuilder.Desktop/src/FFUBuilder.App/App.xaml.cs
  </files>
  <action>
**IConfigurationService.cs** — Add two new methods to the interface:
```csharp
BuildConfiguration CreateDefaults();
string GetDefaultConfigPath(string ffuDevelopmentPath);
```

**ConfigurationService.cs** — Implement the new methods:
- `CreateDefaults()` returns `BuildConfiguration.CreateDefaults()`
- `GetDefaultConfigPath(string ffuDevPath)` returns `Path.Combine(ffuDevPath, "config", "FFUConfig.json")`
- In `SaveAsync`, add `NullValueHandling = NullValueHandling.Ignore` to JsonSerializerSettings so null properties don't clutter the JSON
- In `SaveAsync`, sort properties alphabetically for consistency with PowerShell UI output (use a custom JsonConverter or sort the serialized dictionary)

**SettingsViewModel.cs** — Complete rewrite with:

1. **Constructor injection**: `IConfigurationService configService` (store as private readonly field)

2. **Observable properties**:
   - `BuildConfiguration Configuration` — the main config object. Use `[ObservableProperty]` with `partial void OnConfigurationChanged()` to propagate changes. When Configuration changes, call `OnPropertyChanged("")` to refresh all bindings.
   - `bool IsDirty` — tracks unsaved changes
   - `bool IsLoading` — prevents dirty tracking during load
   - `string StatusMessage` — status bar text
   - `string? ConfigFilePath` — current loaded config file path
   - `string? ValidationSummary` — validation error summary

3. **Helper properties for display conversion** (NOT on the model, on the ViewModel):
   - `double MemoryGB { get => Configuration.Memory / (1024.0 * 1024 * 1024); set => Configuration.Memory = (long)(value * 1024 * 1024 * 1024); }` — two-way GB<->bytes for UI display
   - `double DisksizeGB { get => Configuration.Disksize / (1024.0 * 1024 * 1024); set => Configuration.Disksize = (long)(value * 1024 * 1024 * 1024); }` — two-way GB<->bytes
   - `string OptionalFeaturesDisplay` — semicolon-separated features for display

4. **Enum option lists** (for ComboBox ItemsSource binding):
   - `string[] WindowsReleaseOptions => new[] { "10", "11", "2016", "2019", "2022", "2025" };` (display values)
   - `string[] WindowsSKUOptions` — from schema enum
   - `string[] WindowsArchOptions => new[] { "x86", "x64", "arm64" };`
   - `string[] WindowsLangOptions` — from schema enum (38 languages)
   - `string[] MediaTypeOptions => new[] { "consumer", "business" };`
   - `string[] HypervisorTypeOptions => new[] { "HyperV", "VMware", "Auto" };`
   - `string[] MakeOptions => new[] { "", "Microsoft", "Dell", "HP", "Lenovo", "Acer", "Dynabook", "Panasonic", "Samsung", "Fujitsu", "ASUS", "MSI", "Getac" };`
   - `string[] BitsPriorityOptions => new[] { "Foreground", "High", "Normal", "Low" };`
   - `int[] LogicalSectorSizeOptions => new[] { 512, 4096 };`

5. **Commands** (use `[RelayCommand]`):
   - `LoadConfigAsync()` — calls `configService.LoadAsync()`, sets Configuration, clears IsDirty
   - `SaveConfigAsync()` — validates, calls `configService.SaveAsync()`, clears IsDirty
   - `ResetToDefaults()` — sets `Configuration = BuildConfiguration.CreateDefaults()`, sets IsDirty = true
   - `BrowseFFUDevelopmentPath()` — (placeholder, will be wired in Plan 02 XAML)
   - `BrowseFFUCaptureLocation()` — (placeholder)
   - `BrowseISOPath()` — (placeholder)

6. **INotifyDataErrorInfo implementation** — implement the interface directly (CommunityToolkit.Mvvm does NOT source-generate this):
   - Private `Dictionary<string, List<string>> _errors`
   - `bool HasErrors => _errors.Any(kv => kv.Value.Count > 0);`
   - `event EventHandler<DataErrorsChangedEventArgs>? ErrorsChanged;`
   - `IEnumerable GetErrors(string? propertyName)` — returns errors for the property
   - `void ValidateProperty(string propertyName)` — validate a single property
   - `void ValidateAll()` — validate all properties

   Validation rules (from schema min/max constraints):
   - Memory: 2GB (2147483648) to 128GB (137438953472)
   - Disksize: 25GB (26843545600) to 2TB (2199023255552)
   - Processors: 1 to 64
   - MaxUSBDrives: 0 to 100
   - VMShutdownTimeoutMinutes: 5 to 120
   - FFUFileLockWaitSeconds: 30 to 600
   - FFUDevelopmentPath: warn if empty
   - VMName: must match pattern `^[a-zA-Z0-9_\-]+$` if not empty

7. **Property change tracking**: Subscribe to Configuration's PropertyChanged (if it raises it) or use a manual approach — when any bound property setter fires, set IsDirty = true (unless IsLoading).

**App.xaml.cs** — No changes needed since SettingsViewModel is already registered as Transient and gets IConfigurationService via DI.

**IMPORTANT**: The SettingsViewModel constructor should call `Configuration = BuildConfiguration.CreateDefaults()` to ensure a non-null initial state. Set `IsLoading = true` during initialization, then `IsLoading = false` after, to prevent marking dirty.
  </action>
  <verify>
Build the project: `dotnet build FFUBuilder.Desktop/src/FFUBuilder.App/FFUBuilder.App.csproj`
Verify no compilation errors.
  </verify>
  <done>SettingsViewModel has load/save/reset commands, INotifyDataErrorInfo validation with range checks, GB<->bytes conversion properties, enum option lists for ComboBox binding, and dirty tracking. ConfigurationService has CreateDefaults and GetDefaultConfigPath methods. Project compiles cleanly.</done>
</task>

</tasks>

<verification>
1. `dotnet build FFUBuilder.Desktop/src/FFUBuilder.App/FFUBuilder.App.csproj` succeeds with zero errors
2. BuildConfiguration has ~70 properties with [JsonProperty] attributes
3. SettingsViewModel implements INotifyDataErrorInfo
4. SettingsViewModel has LoadConfigCommand, SaveConfigCommand, ResetToDefaultsCommand
5. All JsonProperty names match the keys in ffubuilder-config.schema.json
6. Memory/Disksize stored as bytes (long), with MemoryGB/DisksizeGB conversion properties on ViewModel
</verification>

<success_criteria>
- BuildConfiguration model covers all non-deprecated config schema properties
- SettingsViewModel provides two-way binding support with validation
- ConfigurationService can produce JSON that matches PowerShell UI format
- Project builds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/51-settings-configuration/51-01-SUMMARY.md`
</output>
