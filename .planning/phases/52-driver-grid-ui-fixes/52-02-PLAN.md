---
phase: 52-driver-grid-ui-fixes
plan: 02
type: execute
wave: 2
depends_on: ["52-01"]
files_modified:
  - FFUDevelopment/FFUUI.Core/FFUUI.Core.Shared.psm1
  - FFUDevelopment/FFUUI.Core/FFUUI.Core.Drivers.psm1
  - FFUDevelopment/FFUUI.Core/FFUUI.Core.Initialize.psm1
autonomous: true
requirements: [DGRID-02]

must_haves:
  truths:
    - "Selecting/deselecting drivers while filtered then saving preserves selections for hidden (filtered-out) rows"
    - "The select-all header checkbox affects only visible/filtered rows for the driver grid"
    - "Only the driver grid opts into the visible-only header scope; the other five grids keep full-list behavior"
    - "The select-all header checkbox is centered/aligned consistently with row checkboxes"
  artifacts:
    - path: "FFUDevelopment/FFUUI.Core/FFUUI.Core.Shared.psm1"
      provides: "Add-SelectableGridViewColumn -HeaderSelectionAffectsVisibleItemsOnly switch + scope-aware handlers + Update-SelectAllHeaderCheckBoxState scope + header alignment wrapper"
      contains: "HeaderSelectionAffectsVisibleItemsOnly"
    - path: "FFUDevelopment/FFUUI.Core/FFUUI.Core.Drivers.psm1"
      provides: "Save-DriversJson reads $State.Data.allDriverModels master list with null-guard"
      contains: "allDriverModels"
    - path: "FFUDevelopment/FFUUI.Core/FFUUI.Core.Initialize.psm1"
      provides: "Driver grid (line 457) opts into -HeaderSelectionAffectsVisibleItemsOnly; other 5 call sites unchanged"
      contains: "HeaderSelectionAffectsVisibleItemsOnly"
  key_links:
    - from: "Save-DriversJson"
      to: "$State.Data.allDriverModels"
      via: "null-guarded master-list read instead of lstDriverModels.Items"
      pattern: "allDriverModels"
    - from: "FFUUI.Core.Initialize.psm1:457 (lstDriverModels)"
      to: "Add-SelectableGridViewColumn -HeaderSelectionAffectsVisibleItemsOnly"
      via: "single opt-in call site"
      pattern: "HeaderSelectionAffectsVisibleItemsOnly"
---

<objective>
Port f09c989 then 42ed281 (DGRID-02) so driver selections survive filtering+saving and
the select-all header is correctly scoped and aligned.

Three layered edits, in strict order (f09c989 before 42ed281 per CONTEXT.md D-08):
1. f09c989 select-all scope in `FFUUI.Core.Shared.psm1` — add the
   `-HeaderSelectionAffectsVisibleItemsOnly` switch, Tag field, scope-aware Checked/
   Unchecked handlers, and the matching `Update-SelectAllHeaderCheckBoxState` scope read.
2. f09c989 save + opt-in — fix `Save-DriversJson` (Drivers.psm1) to read the master list,
   and opt the driver grid (Initialize.psm1 line 457 ONLY) into the new switch.
3. 42ed281 header alignment — wrap the header checkbox in Border/Grid/GridViewColumnHeader
   in `Add-SelectableGridViewColumn` (builds on the f09c989 header region).

Purpose: Per CONTEXT.md D-05/D-06/D-07/D-08, the header select-all currently iterates the
full master list (the bug), and `Save-DriversJson` reads `lstDriverModels.Items` which,
under a CollectionView filter, returns only visible rows — a save-path data leak. These
are the load-bearing correctness fixes.
Output: Edited Shared.psm1, Drivers.psm1, Initialize.psm1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-driver-grid-ui-fixes/52-CONTEXT.md
@.planning/phases/52-driver-grid-ui-fixes/52-RESEARCH.md
@.planning/phases/52-driver-grid-ui-fixes/52-PATTERNS.md
@./CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port f09c989 select-all visible scope (Shared.psm1)</name>
  <files>FFUDevelopment/FFUUI.Core/FFUUI.Core.Shared.psm1</files>
  <read_first>
    - `FFUDevelopment/FFUUI.Core/FFUUI.Core.Shared.psm1` — `Add-SelectableGridViewColumn`
      at line 295 (param block ends line 306; `$headerTagObject` lines 321-324; Add_Checked
      body lines 333-337; Add_Unchecked body lines 347-352); `Update-SelectAllHeaderCheckBoxState`
      at line 415 (`$collectionToInspect` block lines 423-436).
    - Upstream reference: `git show f09c989` — verbatim switch + scope-aware handler source.
    - `52-PATTERNS.md` sections "Add-SelectableGridViewColumn (DGRID-02, f09c989 + 42ed281)"
      Parts (switch, Tag, Checked, Unchecked) and "Update-SelectAllHeaderCheckBoxState
      (DGRID-02, f09c989)" — the exact replacement blocks.
    - `52-RESEARCH.md` Pitfall 4 — visible scope MUST iterate
      `[System.Windows.Data.CollectionViewSource]::GetDefaultView(...)`, not `@(ItemsSource)`.
    - CONTEXT.md D-05/D-06 — scope to VISIBLE/filtered rows via the switch; port
      Update-SelectAllHeaderCheckBoxState together so tri-state does not desync.
  </read_first>
  <action>
    In `Add-SelectableGridViewColumn`: add `[switch]$HeaderSelectionAffectsVisibleItemsOnly`
    to the param block after `[string]$IsSelectedPropertyName = "IsSelected"`; add a
    `HeaderSelectionAffectsVisibleItemsOnly = [bool]$HeaderSelectionAffectsVisibleItemsOnly`
    field to `$headerTagObject`; replace the Add_Checked one-liner with the scope-aware
    block that, when `$tagData.HeaderSelectionAffectsVisibleItemsOnly` is set and
    ItemsSource is non-null, enumerates the default CollectionView to build
    `$collectionToUpdate` (else `@(ItemsSource)`, else `@(Items)`) and sets each item's
    selected property to `$true` then `$actualListView.Items.Refresh()`; replace the
    Add_Unchecked one-liner with the identical scope-aware block setting `$false`.
    In `Update-SelectAllHeaderCheckBoxState`: replace the `$collectionToInspect` block
    (lines 423-436) with the version that reads
    `$HeaderCheckBox.Tag.HeaderSelectionAffectsVisibleItemsOnly` (guarded via
    `PSObject.Properties['HeaderSelectionAffectsVisibleItemsOnly']`) and, when set,
    inspects the filtered CollectionView; the selectedCount/tri-state logic below is
    UNCHANGED. Use the upstream f09c989 source verbatim (D-05/D-06). Do NOT add the
    42ed281 alignment wrapper in this task (that is Task 3).
  </action>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -Command "Import-Module .\FFUDevelopment\FFUUI.Core\FFUUI.Core.psd1 -Force; Get-Command Add-SelectableGridViewColumn | Select-Object -ExpandProperty Parameters | Out-Null; (Get-Command Add-SelectableGridViewColumn).Parameters.ContainsKey('HeaderSelectionAffectsVisibleItemsOnly')"</automated>
  </verify>
  <acceptance_criteria>
    - `Add-SelectableGridViewColumn` declares a `[switch]$HeaderSelectionAffectsVisibleItemsOnly` parameter (the import+`Get-Command` check returns `True`).
    - `$headerTagObject` carries a `HeaderSelectionAffectsVisibleItemsOnly` boolean field.
    - Add_Checked and Add_Unchecked handlers, when the switch is set, enumerate `CollectionViewSource.GetDefaultView($actualListView.ItemsSource)` (visible rows only); when unset they use `@($actualListView.ItemsSource)`.
    - `Update-SelectAllHeaderCheckBoxState` reads `HeaderSelectionAffectsVisibleItemsOnly` off `$HeaderCheckBox.Tag` and inspects the filtered view when set; the existing tri-state comparison logic is unchanged.
    - `Import-Module .\FFUDevelopment\FFUUI.Core\FFUUI.Core.psd1 -Force` succeeds.
  </acceptance_criteria>
  <done>Switch + scope-aware Checked/Unchecked handlers + Update-SelectAllHeaderCheckBoxState scope ported; module imports and exposes the new switch parameter.</done>
</task>

<task type="auto">
  <name>Task 2: Port f09c989 Save-DriversJson source + Initialize.psm1 opt-in</name>
  <files>FFUDevelopment/FFUUI.Core/FFUUI.Core.Drivers.psm1, FFUDevelopment/FFUUI.Core/FFUUI.Core.Initialize.psm1</files>
  <read_first>
    - `FFUDevelopment/FFUUI.Core/FFUUI.Core.Drivers.psm1` — `Save-DriversJson` at line 215;
      bug line 221 (`$selectedDrivers = @($State.Controls.lstDriverModels.Items | Where-Object ...)`);
      context anchor line 220 (`WriteLog "Save-DriversJson function called."`).
    - `FFUDevelopment/FFUUI.Core/FFUUI.Core.Initialize.psm1` — ALL SIX
      `Add-SelectableGridViewColumn` call sites: line 457 (lstDriverModels — the ONLY one
      to change), 489 (lstWingetResults), 645 (lstApplications), 668 (lstAppsScriptVariables),
      730 (lstUSBDrives), 792 (lstAdditionalFFUs). The latter five MUST remain unchanged.
    - Upstream reference: `git show f09c989` — Save-DriversJson null-guard and the
      driver-grid opt-in.
    - `52-PATTERNS.md` sections "Save-DriversJson (DGRID-02, f09c989)" and
      "Initialize.psm1 driver grid call site (DGRID-02, f09c989)" — exact replacements.
    - CONTEXT.md D-07 (master-list null-guarded read) and D-05 (opt in only line 457);
      52-RESEARCH.md Pitfall 1 (do NOT add the switch to the other five call sites).
  </read_first>
  <action>
    In `Save-DriversJson` (Drivers.psm1 line 221): replace the `lstDriverModels.Items`
    read with the null-guarded master-list read —
    `$driverSelectionSource = if ($null -ne $State.Data.allDriverModels) { $State.Data.allDriverModels } else { $State.Controls.lstDriverModels.Items }`
    then `$selectedDrivers = @($driverSelectionSource | Where-Object { $_.IsSelected })`,
    matching f09c989 verbatim (D-07).
    In `Initialize.psm1` line 457 (lstDriverModels ONLY): append
    `-HeaderSelectionAffectsVisibleItemsOnly` to the existing
    `Add-SelectableGridViewColumn` call. Do NOT modify the calls at lines 489, 645, 668,
    730, 792 — they keep full-list select-all behavior (D-05, Pitfall 1).
  </action>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File .\FFUDevelopment\Tests\Test-Phase52DriverGridFixes.ps1</automated>
  </verify>
  <acceptance_criteria>
    - `Save-DriversJson` reads `$State.Data.allDriverModels` when non-null and falls back to `$State.Controls.lstDriverModels.Items` only when null.
    - In `Initialize.psm1`, exactly ONE `Add-SelectableGridViewColumn` call carries `-HeaderSelectionAffectsVisibleItemsOnly`, and it is the lstDriverModels call (line ~457).
    - The lstWingetResults / lstApplications / lstAppsScriptVariables / lstUSBDrives / lstAdditionalFFUs calls do NOT carry the switch.
    - The DGRID-02 save + single-opt-in assertions in `Test-Phase52DriverGridFixes.ps1` PASS.
    - `Import-Module .\FFUDevelopment\FFUUI.Core\FFUUI.Core.psd1 -Force` succeeds.
  </acceptance_criteria>
  <done>Save-DriversJson reads master list with null-guard; only the driver grid opts into the visible-only header scope; DGRID-02 save/opt-in assertions green.</done>
</task>

<task type="auto">
  <name>Task 3: Port 42ed281 header checkbox alignment (Shared.psm1)</name>
  <files>FFUDevelopment/FFUUI.Core/FFUUI.Core.Shared.psm1</files>
  <read_first>
    - `FFUDevelopment/FFUUI.Core/FFUUI.Core.Shared.psm1` — `Add-SelectableGridViewColumn`
      AS MODIFIED by Task 1 of this plan; the existing `$headerCheckBox.HorizontalAlignment`
      line (~318), the `Add_Unchecked` handler closing `})`, the
      `$State.Controls[$HeaderCheckBoxKeyName] = $headerCheckBox` line (~355), and the
      `$selectableColumn.Header = $headerCheckBox` assignment (~359).
    - Upstream reference: `git show 42ed281` — the ~25-line Border/Grid/GridViewColumnHeader
      wrapper. DEPENDS ON f09c989 (Task 1) being applied first (52-RESEARCH.md Pitfall 5).
    - `52-PATTERNS.md` section "42ed281 — Header checkbox alignment (apply AFTER f09c989)"
      — exact insertions and the header assignment replacement.
    - CONTEXT.md D-08 — port 42ed281 after f09c989.
  </read_first>
  <action>
    Apply 42ed281 verbatim into `Add-SelectableGridViewColumn` (after Task 1): add
    `$headerCheckBox.VerticalAlignment = [System.Windows.VerticalAlignment]::Center` after
    the existing HorizontalAlignment line; after the Add_Unchecked handler's closing `})`
    and BEFORE `$State.Controls[$HeaderCheckBoxKeyName] = $headerCheckBox`, construct the
    wrapper: a `System.Windows.Controls.Border` (Padding Thickness(12,0,0,0), Stretch
    both), a `System.Windows.Controls.Grid` (Stretch both) whose Children gets the
    `$headerCheckBox`, set `$headerBorder.Child = $headerGrid`, then a
    `System.Windows.Controls.GridViewColumnHeader` (`$selectableHeader`) with
    Horizontal/VerticalContentAlignment Stretch, zeroed Padding/Margin, and
    `$selectableHeader.Content = $headerBorder`. Finally replace
    `$selectableColumn.Header = $headerCheckBox` with
    `$selectableColumn.Header = $selectableHeader`. Use code-behind WPF object
    construction (no XAML style resources) per RESEARCH "Don't Hand-Roll".
  </action>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File .\FFUDevelopment\Tests\Test-Phase52DriverGridFixes.ps1</automated>
  </verify>
  <acceptance_criteria>
    - `Add-SelectableGridViewColumn` constructs a `System.Windows.Controls.GridViewColumnHeader` containing a Border>Grid>CheckBox graph, and assigns it via `$selectableColumn.Header = $selectableHeader` (no longer `= $headerCheckBox`).
    - `$headerCheckBox.VerticalAlignment` is set to Center; the GridViewColumnHeader has zeroed Padding/Margin.
    - The DGRID-02 header-wrapper assertion (GridViewColumnHeader present) in `Test-Phase52DriverGridFixes.ps1` PASSES; all DGRID-01 and DGRID-02 assertions are now green.
    - `Import-Module .\FFUDevelopment\FFUUI.Core\FFUUI.Core.psd1 -Force` succeeds with no parse errors.
  </acceptance_criteria>
  <done>Header checkbox wrapped for centered alignment via GridViewColumnHeader; full DGRID-02 automated assertion set green; module imports cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Edits are WPF header/selection state in the shared column builder, a JSON-save source change reading an in-memory master list, and a UI init opt-in. No new network/auth/untrusted-input path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-02 | Information Disclosure | Save-DriversJson selection source | mitigate | The change REDUCES a data-correctness defect: it stops the save path from silently dropping selected-but-hidden rows. Output is the same local drivers JSON; no new sink or exposure. |
| T-52-03 | Tampering | Header select-all scope switch | accept | Opt-in switch affects only the local driver grid's in-memory `IsSelected` flags on the UI thread; no boundary crossed. |
| T-52-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase. |

Threat surface is minimal — local WPF UI state and a local JSON write of the user's own selections.
</threat_model>

<verification>
- `Import-Module .\FFUDevelopment\FFUUI.Core\FFUUI.Core.psd1 -Force` succeeds.
- `powershell ... Test-Phase52DriverGridFixes.ps1` — all DGRID-01 and DGRID-02 assertions PASS (DGRID-03 may still be RED until plan 03).
- `Get-Command Add-SelectableGridViewColumn` exposes `HeaderSelectionAffectsVisibleItemsOnly`.
</verification>

<success_criteria>
- Save-DriversJson preserves hidden-row selections (master-list read with null-guard).
- Select-all header scoped to visible rows for the driver grid only; other five grids unchanged.
- Header checkbox aligned via the GridViewColumnHeader wrapper.
- DGRID-02 automated assertions green; FFUUI.Core imports cleanly.
</success_criteria>

<output>
Create `.planning/phases/52-driver-grid-ui-fixes/52-02-SUMMARY.md` when done.
</output>
