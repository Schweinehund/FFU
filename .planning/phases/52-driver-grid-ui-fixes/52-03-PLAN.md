---
phase: 52-driver-grid-ui-fixes
plan: 03
type: execute
wave: 3
depends_on: ["52-02"]
files_modified:
  - FFUDevelopment/BuildFFUVM.ps1
autonomous: true
requirements: [DGRID-03]

must_haves:
  truths:
    - "A build with CopyDrivers=true and BuildUSBDrive=false (and not USBOnlyMode) is blocked with a clear thrown error before it starts"
    - "The validation runs on config-merged effective values (END block, after config load), not raw CLI args"
    - "Valid USB-only builds are not blocked by the new throw"
  artifacts:
    - path: "FFUDevelopment/BuildFFUVM.ps1"
      provides: "CopyDrivers↔BuildUSBDrive dependency throw in the PARAMETER VALIDATION block, USBOnlyMode-guarded"
      contains: "-CopyDrivers is set to"
  key_links:
    - from: "BuildFFUVM.ps1 ###PARAMETER VALIDATION (END block, post config-load)"
      to: "throw on invalid CopyDrivers/BuildUSBDrive combination"
      via: "if ($CopyDrivers -and (-not $BuildUSBDrive) -and (-not $USBOnlyMode))"
      pattern: "CopyDrivers.*BuildUSBDrive.*USBOnlyMode"
---

<objective>
Port dc801e9 (DGRID-03) into the CLI build script so an invalid CopyDrivers configuration
(CopyDrivers=true while BuildUSBDrive=false) is blocked before a build starts — guarding
both CLI and UI callers (the UI launches BuildFFUVM.ps1).

This is a single, fork-adapted insertion: the upstream throw, placed in the fork's END
block AFTER config load (so it sees config-merged values) and guarded with
`-and -not $USBOnlyMode` per CONTEXT.md D-02/D-03.

Purpose: Per CONTEXT.md D-01..D-04, the load-bearing fix is the CLI `throw` (NOT a UI
MessageBox and NOT a checkbox grey-out). The fork's BEGIN/END refactor and USBOnlyMode
auto-sets require the two deliberate divergences from verbatim upstream.
Output: Edited BuildFFUVM.ps1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-driver-grid-ui-fixes/52-CONTEXT.md
@.planning/phases/52-driver-grid-ui-fixes/52-RESEARCH.md
@.planning/phases/52-driver-grid-ui-fixes/52-PATTERNS.md
@./CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert CopyDrivers/BuildUSBDrive dependency throw (dc801e9, fork-adapted)</name>
  <files>FFUDevelopment/BuildFFUVM.ps1</files>
  <read_first>
    - `FFUDevelopment/BuildFFUVM.ps1` — anchor context lines 2605-2609: line 2606 is
      `###PARAMETER VALIDATION`, line 2607 is blank, line 2608 is `#Validate drivers folder`
      (`if ($InstallDrivers -or $CopyDrivers) {`). Insert BETWEEN 2606 and 2608.
      Verified structure: BEGIN block ends ~line 694; config file load is at ~line 775
      (inside END); USBOnlyMode block lines 1869-2366 returns at ~2366 (before 2606).
    - Upstream reference: `git show dc801e9` — the verbatim 6-line WriteLog + throw.
    - `52-PATTERNS.md` section "BuildFFUVM.ps1 — CopyDrivers dependency validation
      (DGRID-03, dc801e9 fork-adapted)" — exact insertion text and anchors.
    - `52-RESEARCH.md` "DGRID-03: dc801e9" porting map; Pitfall 2 (do NOT place in BEGIN
      block); CONTEXT.md D-01 (CLI throw, verbatim wording), D-02 (END block after config
      load), D-03 (`-and -not $USBOnlyMode` guard, mandatory), D-04 (no checkbox grey-out,
      no UI MessageBox).
  </read_first>
  <action>
    Insert the dependency-validation block as the FIRST item after `###PARAMETER VALIDATION`
    (between line 2606 and the `#Validate drivers folder` block), in the END block so it
    validates config-merged effective values. Use upstream dc801e9 wording verbatim for
    the `WriteLog` and `throw` strings (the `-CopyDrivers is set to $true, but
    -BuildUSBDrive is not set to $true...Please set -BuildUSBDrive to $true and try again.`
    message, with `$true` escaped as in PATTERNS.md), and apply the fork adaptation D-03
    by gating the condition as `if ($CopyDrivers -and (-not $BuildUSBDrive) -and (-not $USBOnlyMode))`.
    Do NOT place this in the BEGIN block (Pitfall 2). Do NOT add a UI MessageBox or a
    CopyDrivers checkbox grey-out (D-04). Do NOT modify the existing `#Validate drivers
    folder` block below it.
  </action>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File .\FFUDevelopment\Tests\Test-Phase52DriverGridFixes.ps1</automated>
  </verify>
  <acceptance_criteria>
    - `BuildFFUVM.ps1` contains a throw guarded by `if ($CopyDrivers -and (-not $BuildUSBDrive) -and (-not $USBOnlyMode))` with the verbatim dc801e9 message wording, preceded by the matching `WriteLog` call.
    - The throw appears at a line number GREATER than the config-file load point (>775), i.e. inside the END `###PARAMETER VALIDATION` block — NOT in the BEGIN block (<694).
    - The existing `#Validate drivers folder` (`if ($InstallDrivers -or $CopyDrivers)`) block remains intact directly below the new block.
    - No UI MessageBox and no CopyDrivers checkbox disable/grey-out was added (D-04).
    - All DGRID-03 assertions in `Test-Phase52DriverGridFixes.ps1` PASS (throw wording, USBOnlyMode guard, line > config-load), and the full DGRID-01/02/03 suite is green; the file still parses: `powershell -NoProfile -Command "[void][System.Management.Automation.Language.Parser]::ParseFile((Resolve-Path .\FFUDevelopment\BuildFFUVM.ps1), [ref]$null, [ref]$null)"` reports no errors.
  </acceptance_criteria>
  <done>CopyDrivers/BuildUSBDrive throw inserted in the END PARAMETER VALIDATION block with the USBOnlyMode guard; full phase test suite green; script parses cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI/UI → build orchestrator | Build parameters (CLI args + loaded config JSON) cross into BuildFFUVM.ps1. This plan adds a validation gate on that already-existing boundary; it introduces no new boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-04 | Tampering / misconfiguration | CopyDrivers vs BuildUSBDrive parameter combination | mitigate | The new throw REDUCES a misconfiguration risk by fail-fast rejecting an invalid combination before any build side effects, validating config-merged values in the END block; the USBOnlyMode guard prevents false rejection of valid USB-only builds. |
| T-52-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase. |

Threat surface is minimal — a CLI parameter-validation throw that strengthens existing input validation; no new external-input, network, or auth path.
</threat_model>

<verification>
- `powershell ... Test-Phase52DriverGridFixes.ps1` — DGRID-03 assertions PASS; full DGRID-01/02/03 suite green (exit 0).
- `BuildFFUVM.ps1` parses with no errors via `[System.Management.Automation.Language.Parser]::ParseFile`.
</verification>

<success_criteria>
- Invalid CopyDrivers/BuildUSBDrive combination throws a clear error before build start (DGRID-03).
- Throw is in the END block (post config-load) and guarded for USBOnlyMode.
- No UI MessageBox / checkbox grey-out added; script parses cleanly.
</success_criteria>

<output>
Create `.planning/phases/52-driver-grid-ui-fixes/52-03-SUMMARY.md` when done.
</output>
