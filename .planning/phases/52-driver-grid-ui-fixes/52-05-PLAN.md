---
phase: 52-driver-grid-ui-fixes
plan: 05
type: execute
wave: 5
depends_on: ["52-04"]
files_modified: []
autonomous: false
requirements: [DGRID-01, DGRID-02, DGRID-03]

must_haves:
  truths:
    - "A human confirms the six WPF visual/functional behaviors that cannot be tested headlessly"
    - "Filter persists after sort; select-all is scoped to visible rows; tri-state is correct; save preserves hidden selections; header is aligned; invalid CopyDrivers config is blocked at build start"
  artifacts:
    - path: ".planning/phases/52-driver-grid-ui-fixes/52-HUMAN-UAT.md"
      provides: "Recorded outcomes of the six manual UAT scenarios"
      contains: "DGRID"
  key_links:
    - from: "Live FFU Builder UI (BuildFFUVM_UI.ps1)"
      to: "DGRID-01/02/03 visual behaviors"
      via: "manual UAT scenarios from 52-VALIDATION.md"
      pattern: "manual"
---

<objective>
Human verification of the six WPF behaviors that are not headlessly testable (CONTEXT.md
D-10 routes these to manual UAT). The automated content/logic assertions already cover
source correctness in plans 01-04; this plan confirms the actual runtime UI behavior.

Purpose: Per CONTEXT.md D-10, WPF CollectionView/Dispatcher behavior (filter-persists-after
-sort, select-all-under-filter scope, header tri-state, save-under-filter, header
alignment) and the end-to-end CopyDrivers block cannot be unit-tested — mocking would
test the mock, not the framework. A human must drive the live UI.
Output: 52-HUMAN-UAT.md recording pass/fail for each scenario.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-driver-grid-ui-fixes/52-VALIDATION.md
@.planning/phases/52-driver-grid-ui-fixes/52-CONTEXT.md
@./CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Manual UAT — six driver-grid behaviors</name>
  <read_first>
    - `.planning/phases/52-driver-grid-ui-fixes/52-VALIDATION.md` "Manual-Only
      Verifications" — the six scenarios with exact test instructions.
    - `52-RESEARCH.md` "Manual UAT Scenarios (not automatable)" — the same six steps.
  </read_first>
  <what-built>
    Plans 01-04 ported b4305a1 (DGRID-01 sort keeps filter), f09c989 + 42ed281 (DGRID-02
    save preserves hidden selections, select-all visible scope, tri-state, header
    alignment), and dc801e9 (DGRID-03 CopyDrivers throw), and bumped versions. All
    automated source/logic assertions are green and verify-app passed.
  </what-built>
  <how-to-verify>
    Launch the UI: `cd C:\FFUDevelopment; .\BuildFFUVM_UI.ps1` (or from the repo
    FFUDevelopment path). Perform each scenario and record PASS/FAIL:
    1. DGRID-01 (filter persists after sort): Drivers tab → type a search term to filter →
       click a column header to sort → rows stay filtered (no reset to the full list).
    2. DGRID-02 (select-all visible scope): with the filter active, click the header
       checkbox → only visible rows toggle; hidden rows keep their prior state.
    3. DGRID-02 (tri-state): 3 visible rows (1 selected, 2 unselected) → click header →
       all 3 selected → click again → all cleared → deselect one visible → header
       indeterminate.
    4. DGRID-02 (save under filter): select 2 rows in the full list → apply a filter that
       hides one selected row → Save → the saved drivers JSON includes the hidden selected
       row.
    5. DGRID-02 (alignment): Drivers tab → the header checkbox is horizontally centered and
       vertically aligned with the row checkboxes.
    6. DGRID-03 (CopyDrivers block): set CopyDrivers=true and BuildUSBDrive=false → start a
       build → a clear error (thrown by BuildFFUVM.ps1) is surfaced in the UI log and the
       build does not proceed.
    Record results in `.planning/phases/52-driver-grid-ui-fixes/52-HUMAN-UAT.md`.
  </how-to-verify>
  <acceptance_criteria>
    - All six scenarios are executed against the live UI and each is marked PASS or FAIL with notes in `52-HUMAN-UAT.md`.
    - Any FAIL is captured with the observed vs expected behavior so a gap-closure plan can be derived.
  </acceptance_criteria>
  <resume-signal>Type "approved" if all six pass, or describe which scenarios failed and how.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Manual verification only; no code changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-06 | (verification) | Live UI behaviors | accept | Human UAT confirms runtime behavior the automated gate cannot reach; no new attack surface. |
| T-52-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase. |

Threat surface is none — verification-only plan.
</threat_model>

<verification>
- 52-HUMAN-UAT.md exists with PASS/FAIL recorded for all six scenarios.
</verification>

<success_criteria>
- All six manual UAT scenarios pass against the live UI (or failures are documented for gap closure).
</success_criteria>

<output>
Create `.planning/phases/52-driver-grid-ui-fixes/52-05-SUMMARY.md` (and `52-HUMAN-UAT.md`) when done.
</output>
