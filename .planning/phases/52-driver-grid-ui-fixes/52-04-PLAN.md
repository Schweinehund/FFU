---
phase: 52-driver-grid-ui-fixes
plan: 04
type: execute
wave: 4
depends_on: ["52-01", "52-02", "52-03"]
files_modified:
  - FFUDevelopment/FFUUI.Core/FFUUI.Core.psd1
  - FFUDevelopment/version.json
  - FFUDevelopment/WinPEDeployFFUFiles/ApplyFFU.ps1
  - CHANGELOG_FORK.md
autonomous: true
requirements: [DGRID-01, DGRID-02, DGRID-03]

must_haves:
  truths:
    - "FFUUI.Core module version is bumped to 0.0.22 and reflected in version.json"
    - "Main FFU Builder version is 1.12.1 with an updated buildDate"
    - "ApplyFFU.ps1 hardcoded $version reads 1.12.1"
    - "CHANGELOG_FORK.md has a v1.12.1 entry listing DGRID-01/02/03 fixes"
    - "verify-app regression gate passes (BLOCKING) and the phase test exits 0"
  artifacts:
    - path: "FFUDevelopment/FFUUI.Core/FFUUI.Core.psd1"
      provides: "ModuleVersion 0.0.22 + release notes"
      contains: "0.0.22"
    - path: "FFUDevelopment/version.json"
      provides: "main version 1.12.1, FFUUI.Core 0.0.22, current buildDate"
      contains: "1.12.1"
    - path: "FFUDevelopment/WinPEDeployFFUFiles/ApplyFFU.ps1"
      provides: "hardcoded $version = '1.12.1'"
      contains: "1.12.1"
    - path: "CHANGELOG_FORK.md"
      provides: "v1.12.1 Fixed entries for DGRID-01/02/03"
      contains: "DGRID-01"
  key_links:
    - from: "FFUUI.Core.psd1 ModuleVersion 0.0.22"
      to: "version.json FFUUI.Core.version 0.0.22 + main 1.12.1"
      via: "synchronized version bump per CLAUDE.md versioning policy"
      pattern: "0\\.0\\.22"
---

<objective>
Finalize Phase 52: bump versions, write the changelog entry, and run the BLOCKING
verify-app regression gate after the DGRID-01/02/03 ports.

Purpose: Per CONTEXT.md D-11 and CLAUDE.md versioning policy, any subcomponent version
change requires a synchronized main PATCH bump, a matching version.json update, the
ApplyFFU.ps1 hardcoded-version update, a CHANGELOG_FORK.md entry, and a passing verify-app
gate (BLOCKING) before the phase can complete.
Output: Version bumps across 3 files, changelog entry, green verify-app + phase test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-driver-grid-ui-fixes/52-RESEARCH.md
@./CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Version bumps + CHANGELOG_FORK entry</name>
  <files>FFUDevelopment/FFUUI.Core/FFUUI.Core.psd1, FFUDevelopment/version.json, FFUDevelopment/WinPEDeployFFUFiles/ApplyFFU.ps1, CHANGELOG_FORK.md</files>
  <read_first>
    - `FFUDevelopment/FFUUI.Core/FFUUI.Core.psd1` — `ModuleVersion = '0.0.21'` at line 15
      and the release-notes section.
    - `FFUDevelopment/version.json` — main `"version": "1.12.0"` (line 5),
      `FFUUI.Core` block `"version": "0.0.21"` (line ~27), and `buildDate`.
    - `FFUDevelopment/WinPEDeployFFUFiles/ApplyFFU.ps1` — `$version = '1.12.0'` at line 560
      (hardcoded; runs in WinPE without version.json — confirmed path via grep).
    - `CHANGELOG_FORK.md` — existing entry format (newest-first; `## vX.Y.Z - [date]`
      with `### Fixed` bullets).
    - `52-RESEARCH.md` "Version and Changelog Mechanics" — the exact bump table and the
      ready-to-use CHANGELOG_FORK.md entry text; CLAUDE.md "Version Increment Checklist".
  </read_first>
  <action>
    Bump `FFUUI.Core.psd1` ModuleVersion 0.0.21 → 0.0.22 and add a release note describing
    the DGRID-01/02/03 driver-grid fixes. In `version.json`: set main `version` 1.12.0 →
    1.12.1, set the `FFUUI.Core` module `version` 0.0.21 → 0.0.22, and update `buildDate`
    to the current date (2026-06-28). In `ApplyFFU.ps1`: set the hardcoded
    `$version = '1.12.0'` → `'1.12.1'` (line 560). Add a `## v1.12.1 - 2026-06-28`
    `### Fixed` section to the TOP of `CHANGELOG_FORK.md` listing the five DGRID fix
    bullets (DGRID-01 sort-keeps-filter b4305a1; DGRID-02 save-preserves-hidden f09c989;
    DGRID-02 header scope f09c989; DGRID-02 header alignment 42ed281; DGRID-03 CopyDrivers
    throw dc801e9 fork-adapted) per the RESEARCH.md changelog text.
  </action>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -Command "$j = Get-Content .\FFUDevelopment\version.json -Raw | ConvertFrom-Json; $psd = Import-PowerShellDataFile .\FFUDevelopment\FFUUI.Core\FFUUI.Core.psd1; $applyOk = (Select-String -Path .\FFUDevelopment\WinPEDeployFFUFiles\ApplyFFU.ps1 -Pattern \"version\s*=\s*'1\.12\.1'\" -Quiet); $clogOk = (Select-String -Path .\CHANGELOG_FORK.md -Pattern 'v1\.12\.1' -Quiet); if ($j.version -eq '1.12.1' -and $j.modules.'FFUUI.Core'.version -eq '0.0.22' -and $psd.ModuleVersion -eq '0.0.22' -and $applyOk -and $clogOk) { 'OK' } else { throw 'version bump incomplete' }"</automated>
  </verify>
  <acceptance_criteria>
    - `FFUUI.Core.psd1` `ModuleVersion` equals `0.0.22` (verified via `Import-PowerShellDataFile`).
    - `version.json` main `version` equals `1.12.1`, `modules.FFUUI.Core.version` equals `0.0.22`, and `buildDate` is updated to 2026-06-28.
    - `ApplyFFU.ps1` line 560 reads `$version = '1.12.1'`.
    - `CHANGELOG_FORK.md` has a top `## v1.12.1` `### Fixed` section naming DGRID-01, DGRID-02, and DGRID-03.
    - The combined version-check command prints `OK`.
  </acceptance_criteria>
  <done>All four version/changelog touchpoints updated and consistent (1.12.1 / 0.0.22); version-check command prints OK.</done>
</task>

<task type="auto">
  <name>Task 2: Run phase test + verify-app BLOCKING regression gate</name>
  <files>(no source edits — verification only)</files>
  <read_first>
    - `./CLAUDE.md` — "Mandatory Verification (BLOCKING)" and "After Code Changes
      (BLOCKING)" sections: verify-app must pass (all tests, PSScriptAnalyzer no new
      errors, modules import) before the phase is complete; HALT on failure.
    - `.claude/agents/verify-app.md` — the verify-app agent definition to act as.
    - `FFUDevelopment/Tests/Test-Phase52DriverGridFixes.ps1` — the phase test (must exit 0).
    - `52-RESEARCH.md` "verify-app scope for WPF changes" — verify-app gates syntax/import
      correctness; WPF visual behavior is routed to manual UAT (plan 05), not here.
  </read_first>
  <action>
    Run `powershell -NoProfile -ExecutionPolicy Bypass -File .\FFUDevelopment\Tests\Test-Phase52DriverGridFixes.ps1`
    and confirm exit 0 (all DGRID-01/02/03 assertions green). Then invoke the verify-app
    agent (Task tool, subagent_type="general-purpose", prompt: "Act as verify-app agent
    (read .claude/agents/verify-app.md). Run full regression verification.") as the
    BLOCKING gate per CLAUDE.md. Confirm: targeted Pester/custom tests pass, no new
    PSScriptAnalyzer errors on the four edited source files (Shared.psm1, Drivers.psm1,
    Initialize.psm1, BuildFFUVM.ps1), and FFUUI.Core imports cleanly. If verify-app fails,
    HALT and record the failure in the SUMMARY — do not mark the phase complete. Use
    targeted tests, not the full env-dependent suite (per project memory: full suite has
    pre-existing env-dependent reds).
  </action>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File .\FFUDevelopment\Tests\Test-Phase52DriverGridFixes.ps1; echo "exit=$LASTEXITCODE"</automated>
  </verify>
  <acceptance_criteria>
    - `Test-Phase52DriverGridFixes.ps1` exits 0 (all DGRID-01/02/03 assertions PASS).
    - verify-app agent reports: targeted tests pass, no NEW PSScriptAnalyzer errors on the four edited files, all FFUUI.Core modules import successfully.
    - If verify-app reports any failure, the SUMMARY records a HALT and the phase is NOT marked complete.
  </acceptance_criteria>
  <done>Phase test exits 0 and verify-app BLOCKING gate passes (or HALT recorded on failure).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Version metadata + changelog edits and a read-only verification gate. No code behavior, network, auth, or untrusted-input path is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-05 | Tampering | version.json / psd1 / ApplyFFU version strings | accept | Version metadata only; the verify-app gate (BLOCKING) catches any parse/import regression introduced by the phase before completion. |
| T-52-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase. |

Threat surface is minimal — version metadata and a verification gate.
</threat_model>

<verification>
- Version-check command prints `OK` (1.12.1 / 0.0.22 / ApplyFFU 1.12.1 / CHANGELOG v1.12.1).
- `Test-Phase52DriverGridFixes.ps1` exits 0.
- verify-app BLOCKING gate passes.
</verification>

<success_criteria>
- FFUUI.Core 0.0.22 and main 1.12.1 synchronized across psd1 + version.json; ApplyFFU.ps1 and CHANGELOG_FORK updated.
- Full DGRID-01/02/03 automated suite green and verify-app gate green.
</success_criteria>

<output>
Create `.planning/phases/52-driver-grid-ui-fixes/52-04-SUMMARY.md` when done.
</output>
