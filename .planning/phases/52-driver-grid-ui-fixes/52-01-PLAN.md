---
phase: 52-driver-grid-ui-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - FFUDevelopment/Tests/Test-Phase52DriverGridFixes.ps1
  - FFUDevelopment/FFUUI.Core/FFUUI.Core.Shared.psm1
autonomous: true
requirements: [DGRID-01, DGRID-02, DGRID-03]

must_haves:
  truths:
    - "Running Test-Phase52DriverGridFixes.ps1 produces deterministic PASS/FAIL output and a 0/1 exit code"
    - "After this plan, sorting the driver list while a filter is active preserves the filter (DGRID-01)"
    - "The DGRID-01 source-content assertions in the phase test pass"
  artifacts:
    - path: "FFUDevelopment/Tests/Test-Phase52DriverGridFixes.ps1"
      provides: "Custom Write-TestResult content+logic assertions for DGRID-01/02/03"
      contains: "Write-TestResult"
    - path: "FFUDevelopment/FFUUI.Core/FFUUI.Core.Shared.psm1"
      provides: "Invoke-ListViewSort with CollectionView filter capture/reapply (b4305a1)"
      contains: "existingFilter"
  key_links:
    - from: "FFUDevelopment/Tests/Test-Phase52DriverGridFixes.ps1"
      to: "FFUDevelopment/FFUUI.Core/FFUUI.Core.Shared.psm1"
      via: "Get-Content -Raw source-content regex assertions"
      pattern: "newView\\.Filter\\s*=\\s*\\$existingFilter"
    - from: "Invoke-ListViewSort"
      to: "CollectionViewSource.GetDefaultView"
      via: "filter predicate capture before sort, reapply after ItemsSource reassignment"
      pattern: "GetDefaultView"
---

<objective>
Establish the Wave-0 automated test scaffold for Phase 52 and port the first upstream
commit (b4305a1, DGRID-01) so sorting a filtered driver list keeps the filter applied.

This plan does two things, in order:
1. Create `Test-Phase52DriverGridFixes.ps1` covering the full DGRID-01/02/03 automated
   assertion set up front (content-match + pure-logic). Assertions for DGRID-02/03 will
   be RED until later plans land — that is expected Nyquist behavior.
2. Port b4305a1 into the SHARED `Invoke-ListViewSort` (`FFUUI.Core.Shared.psm1`) — the
   first commit in the locked layering order; later Shared.psm1 edits build on it.

Purpose: Per CONTEXT.md D-09 and D-10, the b4305a1 fix is a backward-compatible filter
capture/reapply in the shared sort utility (the no-filter path is unchanged for all
grids), and the test scaffold gives every downstream plan an automated verify target.
Output: New test file + edited Shared.psm1 (Invoke-ListViewSort only).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-driver-grid-ui-fixes/52-CONTEXT.md
@.planning/phases/52-driver-grid-ui-fixes/52-RESEARCH.md
@.planning/phases/52-driver-grid-ui-fixes/52-PATTERNS.md
@.planning/phases/52-driver-grid-ui-fixes/52-VALIDATION.md
@./CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Test-Phase52DriverGridFixes.ps1 (Wave-0 scaffold)</name>
  <files>FFUDevelopment/Tests/Test-Phase52DriverGridFixes.ps1</files>
  <read_first>
    - `FFUDevelopment/Tests/Test-Phase51Correctness.ps1` — the exact analog: copy its
      header block, `$script:PassCount`/`$script:FailCount`/`$script:TestResults` counters,
      `Write-TestResult` function (lines 31-59), setup block (lines 63-79), section
      banners, content-match try/catch pattern, pure-function logic test pattern
      (lines 432-465), and summary+exit pattern (lines 472-487) VERBATIM in structure.
    - `52-PATTERNS.md` section "Test-Phase52DriverGridFixes.ps1 (new test file)" — the
      authoritative header/synopsis text, setup paths ($SharedPsm1, $DriversPsm1,
      $InitPsm1, $BuildScript), and the `Test-DriverSelectionSource-Local` logic test.
    - `52-VALIDATION.md` "Per-Task Verification Map" + "Manual-Only Verifications" — which
      behaviors are automated (content/logic) vs human-UAT-only (do NOT attempt to
      automate WPF visual behaviors).
    - `52-RESEARCH.md` "Phase Requirements → Test Map" — the automated rows define the
      assertions to write.
  </read_first>
  <action>
    Create the test file using the project's custom `Write-TestResult` framework (NOT
    Pester — no `.Tests.ps1` exist; modeled on Test-Phase51Correctness.ps1). Resolve
    source paths from `Split-Path $PSScriptRoot -Parent` to FFUUI.Core\FFUUI.Core.Shared.psm1,
    FFUUI.Core\FFUUI.Core.Drivers.psm1, FFUUI.Core\FFUUI.Core.Initialize.psm1, and
    BuildFFUVM.ps1. Implement these automated assertions, each via `Get-Content -Raw`
    inside try/catch:
    DGRID-01 (Shared.psm1): filter-capture preamble present (regex `\$existingFilter\s*=\s*\$null`);
    CollectionView captured before sort (`existingCollectionView.*GetDefaultView`);
    filter reapplied after sort (`newView\.Filter\s*=\s*\$existingFilter`).
    DGRID-02 (Shared.psm1): `-HeaderSelectionAffectsVisibleItemsOnly` switch present in
    `Add-SelectableGridViewColumn` param block; `HeaderSelectionAffectsVisibleItemsOnly`
    read in `Update-SelectAllHeaderCheckBoxState`; header wrapped in GridViewColumnHeader
    (`New-Object System.Windows.Controls.GridViewColumnHeader`).
    DGRID-02 (Initialize.psm1): exactly ONE call site carries
    `-HeaderSelectionAffectsVisibleItemsOnly` (count `Add-SelectableGridViewColumn`
    occurrences vs occurrences with the switch; assert switch count == 1).
    DGRID-02 (Drivers.psm1): `Save-DriversJson` reads `$State.Data.allDriverModels` with
    null-guard (`allDriverModels`), and no longer relies solely on `lstDriverModels.Items`.
    DGRID-02 (logic): include the `Test-DriverSelectionSource-Local` pure-function test
    from 52-PATTERNS.md asserting non-null returns allDriverModels, null falls back to lstItems.
    DGRID-03 (BuildFFUVM.ps1): throw wording present
    (`-CopyDrivers is set to .* -BuildUSBDrive is not set`); `-not $USBOnlyMode` guard
    present on the CopyDrivers condition; the throw appears at a line number GREATER than
    the config-load point (compute the line index of the throw and assert it is > the
    line index of the config-file load / > 775) to prove END-block placement.
    Finish with the summary block and `exit 0`/`exit 1`. It is EXPECTED that DGRID-02/03
    assertions are RED at the end of this plan (those ports land in plans 02/03).
  </action>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File .\FFUDevelopment\Tests\Test-Phase52DriverGridFixes.ps1; echo "exit=$LASTEXITCODE"</automated>
  </verify>
  <acceptance_criteria>
    - File `FFUDevelopment/Tests/Test-Phase52DriverGridFixes.ps1` exists and runs without a parse/runtime exception (it produces [PASS]/[FAIL] lines and a numeric exit code).
    - It defines `Write-TestResult` and the `$script:PassCount`/`$script:FailCount` counters and ends with `exit 0` (all pass) / `exit 1` (any fail).
    - It contains content assertions for all three of: `\$existingFilter` (DGRID-01), `HeaderSelectionAffectsVisibleItemsOnly` (DGRID-02), and the CopyDrivers throw wording (DGRID-03).
    - It uses the custom framework only — no `Describe`/`It`/`Should` Pester constructs.
    - At this point the DGRID-01 assertions are RED (DGRID-01 port lands in Task 2 of this same plan, after which they go green).
  </acceptance_criteria>
  <done>Test file created with all automated DGRID-01/02/03 assertions; runs to a deterministic exit code.</done>
</task>

<task type="auto">
  <name>Task 2: Port b4305a1 into Invoke-ListViewSort (DGRID-01)</name>
  <files>FFUDevelopment/FFUUI.Core/FFUUI.Core.Shared.psm1</files>
  <read_first>
    - `FFUDevelopment/FFUUI.Core/FFUUI.Core.Shared.psm1` — `Invoke-ListViewSort` starts at
      line 501; the pre-fix item-collection block is at lines 537-545; the final
      `$listView.ItemsSource = $newSortedList.ToArray()` is at line 629.
    - Upstream reference: `git show b4305a1` (remote `upstream` = https://github.com/rbalsleyMSFT/FFU.git) — the verbatim filter-capture/reapply source.
    - `52-PATTERNS.md` section "Invoke-ListViewSort (DGRID-01, b4305a1)" — the three exact
      insertions (preamble after param block, replacement of lines 537-545, reapply after
      line 629) with the precise code.
    - `52-RESEARCH.md` "DGRID-01: b4305a1" porting map + Pitfall 3 (do NOT re-call
      Search-DriverModels) and Pitfall 4 (iterate GetDefaultView, not ItemsSource).
    - CONTEXT.md D-09 — fix the SHARED function; no SortDescriptions rewrite; no-filter
      path must remain unchanged (backward compatible for all 5+ grids).
  </read_first>
  <action>
    Apply b4305a1 verbatim at fork line numbers to `Invoke-ListViewSort` per D-09:
    (1) Insert the filter-capture preamble (`$existingFilter`/`$existingCollectionView`
    via `[System.Windows.Data.CollectionViewSource]::GetDefaultView($listView.ItemsSource)`,
    capturing `.Filter` when present) after the param block (~after line 506, before the
    `$State.Flags` check). (2) Replace the pre-fix item-collection block (lines 537-545)
    with the filter-aware version that, when a filter is active, enumerates the
    `$existingCollectionView` to build `$itemsToSort`, else falls back to
    `@($currentItemsSource)`, else `@($listView.Items)`. (3) Append the filter
    reapplication after the final `$listView.ItemsSource = $newSortedList.ToArray()`
    (line 629): re-acquire `GetDefaultView` on the new ItemsSource and assign
    `$newView.Filter = $existingFilter` when `$existingFilter` is non-null. Do NOT touch
    the selected-pinned-on-top ordering. Do NOT modify any other function in this file in
    this task. The no-filter branch (when `$existingFilter` is null) must be behaviorally
    identical to the pre-fix code.
  </action>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File .\FFUDevelopment\Tests\Test-Phase52DriverGridFixes.ps1</automated>
  </verify>
  <acceptance_criteria>
    - `Invoke-ListViewSort` captures `$existingFilter` from `CollectionViewSource.GetDefaultView($listView.ItemsSource)` BEFORE reassigning ItemsSource, and re-applies it via `$newView.Filter = $existingFilter` AFTER the ItemsSource reassignment.
    - When a filter is active, items to sort are enumerated from the filtered CollectionView (not `@($listView.ItemsSource)`); when no filter is active the code path matches pre-fix behavior.
    - The DGRID-01 assertions in `Test-Phase52DriverGridFixes.ps1` now PASS (`$existingFilter` preamble, GetDefaultView capture, `newView.Filter = $existingFilter` reapply).
    - No other function in `FFUUI.Core.Shared.psm1` is modified.
    - `Import-Module .\FFUDevelopment\FFUUI.Core\FFUUI.Core.psd1 -Force` succeeds (no parse errors).
  </acceptance_criteria>
  <done>Invoke-ListViewSort preserves and reapplies the active filter across sort; DGRID-01 test assertions green; module imports cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan edits local WPF sort logic and a local test script. No network, auth, file-write of untrusted data, or new external-input path is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-01 | Tampering | Invoke-ListViewSort filter predicate | accept | Predicate is an in-process WPF delegate captured/reapplied within the same UI thread; no external input crosses a boundary. Backward-compatible no-filter path preserved. |
| T-52-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH.md "Package Legitimacy Audit: Not applicable"). |

Threat surface for this plan is minimal — local WPF view-state manipulation only.
</threat_model>

<verification>
- `powershell -NoProfile -ExecutionPolicy Bypass -File .\FFUDevelopment\Tests\Test-Phase52DriverGridFixes.ps1` runs; DGRID-01 assertions PASS (DGRID-02/03 may still be RED until later plans).
- `Import-Module .\FFUDevelopment\FFUUI.Core\FFUUI.Core.psd1 -Force` succeeds.
</verification>

<success_criteria>
- Test scaffold exists with all DGRID-01/02/03 automated assertions and a deterministic exit code.
- Invoke-ListViewSort preserves an active CollectionView filter across a sort (DGRID-01).
- No regression to the no-filter sort path or to other Shared.psm1 functions.
</success_criteria>

<output>
Create `.planning/phases/52-driver-grid-ui-fixes/52-01-SUMMARY.md` when done.
</output>
