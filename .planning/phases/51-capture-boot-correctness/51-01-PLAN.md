---
phase: 51-capture-boot-correctness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - FFUDevelopment/Modules/FFU.Imaging/FFU.Imaging.psm1
  - FFUDevelopment/Modules/FFU.Imaging/FFU.Imaging.psd1
  - FFUDevelopment/BuildFFUVM.ps1
  - FFUDevelopment/Tests/Test-Phase51Correctness.ps1
autonomous: true
requirements: [CORRECT-01, CORRECT-04]
must_haves:
  truths:
    - "The image index is selected via EditionId (primary) then exact ImageName -eq (fallback), never via a localized name substring (CORRECT-04, D-05)."
    - "A complete SKU->EditionId map covers every $clientSKUs / $LTSCSKUs / $ServerSKUs friendly name; map completeness is enforced by a test gate (CORRECT-04, D-06)."
    - "Server Desktop Experience vs Server Core is disambiguated by InstallationType (CORRECT-04, D-07)."
    - "The Read-Host / while($true) fallback loop is deleted; a single relevant candidate auto-selects non-interactively and >=2 candidates WriteLog the editions list then throw (CORRECT-01, D-01/D-02/D-03)."
    - "After a fallback edition is chosen, $WindowsSKU is reassigned to the selected edition and runtime state recomputed, so naming/caching/servicing use the selected edition (CORRECT-01, D-04)."
  artifacts:
    - path: "FFUDevelopment/Modules/FFU.Imaging/FFU.Imaging.psm1"
      provides: "Get-WindowsImageSelection (replaces Get-Index) + Get-ResolvedWindowsSKUFromImage companion"
      contains: "function Get-WindowsImageSelection"
    - path: "FFUDevelopment/BuildFFUVM.ps1"
      provides: "Get-WindowsTargetRuntimeState helper + caller propagation at the Get-Index call site"
      contains: "function Get-WindowsTargetRuntimeState"
    - path: "FFUDevelopment/Tests/Test-Phase51Correctness.ps1"
      provides: "Custom Write-TestResult suite covering CORRECT-01/04 + SKU->EditionId map completeness"
      contains: "Get-WindowsImageSelection"
  key_links:
    - from: "FFUDevelopment/BuildFFUVM.ps1 (Get-Index call site ~4404)"
      to: "Get-WindowsImageSelection"
      via: "rich PSCustomObject return; $WindowsSKU = $selection.ResolvedWindowsSKU"
      pattern: "Get-WindowsImageSelection"
    - from: "FFUDevelopment/Modules/FFU.Imaging/FFU.Imaging.psd1"
      to: "Get-WindowsImageSelection / Get-ResolvedWindowsSKUFromImage"
      via: "FunctionsToExport"
      pattern: "Get-WindowsImageSelection"
---

<objective>
Port CORRECT-04 (`b2a7ef5`) and CORRECT-01 (`5aaa1ad`) as one unit by rewriting `Get-Index` into `Get-WindowsImageSelection` in FFU.Imaging, adding the `Get-ResolvedWindowsSKUFromImage` reverse-map companion, and propagating the actually-selected edition back into the build context at the caller.

Purpose: Builds capture the correct edition from non-English / multi-edition media (EditionId/InstallationType selection, not a localized substring), and when a fallback edition is auto-selected the FFU is named, cached, and serviced as that selected edition. Also eliminates the ThreadJob deadlock from the interactive `Read-Host` fallback.

Output: Rewritten `Get-WindowsImageSelection` + `Get-ResolvedWindowsSKUFromImage` (FFU.Imaging.psm1), `Get-WindowsTargetRuntimeState` + caller propagation (BuildFFUVM.ps1), updated FFU.Imaging.psd1 exports/version, and a new `Test-Phase51Correctness.ps1` suite (CORRECT-01/04 + map-completeness gate).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-capture-boot-correctness/51-CONTEXT.md
@.planning/phases/51-capture-boot-correctness/51-RESEARCH.md
@.planning/phases/51-capture-boot-correctness/51-PATTERNS.md
@.planning/phases/51-capture-boot-correctness/51-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite Get-Index into Get-WindowsImageSelection + add Get-ResolvedWindowsSKUFromImage</name>
  <read_first>
    - FFUDevelopment/Modules/FFU.Imaging/FFU.Imaging.psm1 (current Get-Index ~490-587: substring derivation 532-545, exact -eq match 551, relevant-index filter 560, Read-Host/while($true) loop 562-585)
    - FFUDevelopment/Modules/FFU.ArtifactScanner/FFU.ArtifactScanner.psm1:201-215 (proven Get-WindowsImage -Index 1 -> .EditionId pattern to reuse)
    - 51-RESEARCH.md "Complete SKU -> EditionId Map (D-06)" table (all 24 mappings), "Upstream Commit Evidence" b2a7ef5 switch + InstallationType disambiguation, 5aaa1ad reverse EditionId->SKU switch, and "Code Examples" Pattern 1
    - 51-PATTERNS.md "Get-WindowsImageSelection rewrite" (new signatures, rich return shape, WriteLog+throw idiom) and "Shared Patterns" (WriteLog ThreadJob-safe logging, [CmdletBinding()]/[OutputType()])
    - 51-CONTEXT.md decisions D-01..D-08
  </read_first>
  <behavior>
    - SKU->EditionId switch covers every entry in the map table (Home->Core, Pro->Professional, Home Single Language->CoreSingleLanguage, N variants->*N, Enterprise LTSC->EnterpriseS, Enterprise 2016 LTSB->EnterpriseS, IoT Enterprise LTSC->IoTEnterpriseS, Standard->ServerStandard, Datacenter->ServerDatacenter, plus the EditionId-alias forms) and returns @() for unknown SKUs.
    - Per-image metadata is read with Get-WindowsImage -Index N (EditionId + InstallationType per index), null entries filtered out.
    - Selection order: EditionId-candidate match (filtered by InstallationType when the SKU is a Server variant: Desktop Experience -> 'Server', otherwise 'Server Core') -> exact ImageName -eq fallback (D-08) -> single relevant candidate (ImageName -match '(10|11|2016|2019|202\d)') auto-select (D-02) -> >=2 candidates WriteLog full editions list then throw (D-03).
    - Get-WindowsImageSelection returns a PSCustomObject { ImageIndex, ImageName, ImageSize, EditionId, InstallationType, ResolvedWindowsSKU }, NOT a bare [int].
    - Get-ResolvedWindowsSKUFromImage maps EditionId back to the friendly SKU (EnterpriseS -> 'Enterprise 2016 LTSB' when WindowsRelease -eq 2016 or ImageName matches 'LTSB' else 'Enterprise LTSC'; ServerStandard/ServerDatacenter -> Desktop Experience variant when InstallationType -eq 'Server') and returns $null for unknown EditionId.
    - No Read-Host, no Write-Host, no while($true). WriteLog only.
  </behavior>
  <files>FFUDevelopment/Modules/FFU.Imaging/FFU.Imaging.psm1, FFUDevelopment/Modules/FFU.Imaging/FFU.Imaging.psd1</files>
  <action>
    In FFU.Imaging.psm1 rename `Get-Index` to `Get-WindowsImageSelection` with params `-WindowsImagePath` (mandatory string), `-WindowsSKU` (mandatory string), `-WindowsRelease` (mandatory int); REMOVE the `-ISOPath` parameter entirely (pitfall 2). Delete the `Substring(0,10)`/`Substring(0,19)` derivation block (lines ~532-545) and the entire `Read-Host`/`while($true)` loop (lines ~562-585) per D-01. Add the SKU->EditionId `switch` from the RESEARCH.md map table (all 24 entries plus EditionId-alias forms; strip a trailing '(Desktop Experience)' suffix into an `$isDesktopExperienceRequested` flag and set `$preferredInstallationType` = 'Server' / 'Server Core' for Standard/Datacenter SKUs per D-07). Loop the indexes with `Get-WindowsImage -ImagePath $WindowsImagePath -Index $imageIndex.ImageIndex`, build PSCustomObjects (ImageIndex/ImageName/ImageSize/EditionId/InstallationType/ResolvedWindowsSKU) filtering nulls. Implement the selection tiers: EditionId candidate match (apply `$preferredInstallationType` filter when set), else exact `ImageName -eq` (retain the safe -eq tier per D-08), else single relevant candidate auto-select (D-02), else WriteLog the full available-editions list (`[ImageIndex] ImageName (EditionId=...)` joined by `[System.Environment]::NewLine`) and `throw` (D-03). Return the rich PSCustomObject (D-04). Add the new function `Get-ResolvedWindowsSKUFromImage` (params EditionId/InstallationType/ImageName/WindowsRelease, all mandatory) implementing the reverse switch from 5aaa1ad, returning `$null` for unknown EditionId. Both functions get `[CmdletBinding()]` and `[OutputType()]`, OTBS, 4-space indent, full cmdlet names, WriteLog for all logging. In FFU.Imaging.psd1: replace `'Get-Index'` in `FunctionsToExport` with `'Get-WindowsImageSelection'`, add `'Get-ResolvedWindowsSKUFromImage'`, bump `ModuleVersion` to `1.4.0` (verify current value first; PATTERNS.md records 1.3.3), and add the v1.4.0 ReleaseNotes block (Phase 51 CORRECT-01/03/04 — note that Add-BootFiles changes from Plan 02 are also covered by this 1.4.0 entry so Plan 02 does not re-touch the psd1).
  </action>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -Command "Import-Module .\FFUDevelopment\Modules\FFU.Imaging\FFU.Imaging.psd1 -Force; $e=(Get-Module FFU.Imaging).ExportedFunctions.Keys; if (('Get-WindowsImageSelection' -in $e) -and ('Get-ResolvedWindowsSKUFromImage' -in $e) -and ('Get-Index' -notin $e)) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    - `FFU.Imaging.psm1` contains `function Get-WindowsImageSelection` and `function Get-ResolvedWindowsSKUFromImage`; no `function Get-Index` remains.
    - `FFU.Imaging.psm1` contains no `Read-Host`, no `while ($true)`, and no `.Substring(0, 10)` / `.Substring(0, 19)` derivation inside the selection function.
    - `FFU.Imaging.psm1` contains the EditionId tokens `Core`, `Professional`, `EnterpriseS`, `IoTEnterpriseS`, `ServerStandard`, `ServerDatacenter` (map presence).
    - `FFU.Imaging.psd1` `FunctionsToExport` lists `Get-WindowsImageSelection` and `Get-ResolvedWindowsSKUFromImage`, not `Get-Index`; `ModuleVersion = '1.4.0'`.
    - Module import succeeds and the automated verify command exits 0.
    - `Invoke-ScriptAnalyzer -Path .\FFUDevelopment\Modules\FFU.Imaging\FFU.Imaging.psm1 -Severity Error` reports no new errors.
  </acceptance_criteria>
  <done>Get-WindowsImageSelection selects by EditionId/InstallationType with the exact-name and single-candidate fallbacks, throws (never prompts) on ambiguity, returns a rich object, and is exported; Get-ResolvedWindowsSKUFromImage resolves EditionId back to friendly SKU.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Get-WindowsTargetRuntimeState and propagate selected edition at the caller</name>
  <read_first>
    - FFUDevelopment/BuildFFUVM.ps1 (Get-Index call site ~4403-4404 currently passing -ISOPath; $clientSKUs/$LTSCSKUs/$ServerSKUs ~926-963; SKU legality validation ~960-966; $installationType ~2388 + $isLTSC ~2400-2408 initial derivation; $WindowsSKU consumers ~4087/4339/4643/4698/5192/5301/5340/5636/5696)
    - 51-RESEARCH.md "Upstream Commit Evidence" 5aaa1ad (Get-WindowsTargetRuntimeState verbatim + caller block), "$WindowsSKU Consumer Trace" table, "Code Examples" Patterns 2 and 3
    - 51-PATTERNS.md "Get-WindowsTargetRuntimeState new function" and "caller update at Get-Index call site"
    - 51-CONTEXT.md decision D-04
  </read_first>
  <behavior>
    - Get-WindowsTargetRuntimeState(WindowsRelease, WindowsSKU, CurrentWindowsVersion, UpdateLatestCU) returns { InstallationType, WindowsVersion, IsLTSC, IsWindows10LtscClient, InstallLatestCuInVm }: Server SKUs -> InstallationType 'Server' with 2016->1607/2019->1809/2022->21H2/2025->24H2; '*LTS*' SKUs -> IsLTSC true with 2016->1607/2019->1809/2021->21H2/2024->24H2; IsWindows10LtscClient when Client + WindowsRelease in 2016/2019/2021 + LTSC.
    - At the caller, the bare [int] index return is replaced by the rich selection object; $index = $selection.ImageIndex; when $selection.ResolvedWindowsSKU is non-empty, $WindowsSKU is reassigned to it and $installationType/$WindowsVersion/$isLTSC/$isWindows10LtscClient/$installLatestCuInVm are recomputed via Get-WindowsTargetRuntimeState; a WriteLog records any requested->resolved SKU change.
    - The -ISOPath argument is removed from the call.
  </behavior>
  <files>FFUDevelopment/BuildFFUVM.ps1</files>
  <action>
    Add a script-level `function Get-WindowsTargetRuntimeState` in BuildFFUVM.ps1 (verbatim from RESEARCH.md Pattern 2 / 5aaa1ad; params WindowsRelease int, WindowsSKU string, CurrentWindowsVersion string, UpdateLatestCU bool, all mandatory) returning the PSCustomObject described in behavior. Do NOT alter the existing initial derivation at ~2388-2408 — this helper is called only at the post-selection site. At the `Get-Index` call site (~4404), replace `Get-Index -WindowsImagePath $wimPath -WindowsSKU $WindowsSKU -ISOPath $ISOPath` with a call to `Get-WindowsImageSelection -WindowsImagePath $wimPath -WindowsSKU $WindowsSKU -WindowsRelease $WindowsRelease`; capture `$requestedWindowsSKU` first, set `$index = $windowsImageSelection.ImageIndex`, and when `-not [string]::IsNullOrWhiteSpace($windowsImageSelection.ResolvedWindowsSKU)` reassign `$WindowsSKU = $windowsImageSelection.ResolvedWindowsSKU` and recompute `$installationType`, `$WindowsVersion`, `$isLTSC`, `$isWindows10LtscClient`, `$installLatestCuInVm` from `Get-WindowsTargetRuntimeState` (pass `-UpdateLatestCU:$UpdateLatestCU`). WriteLog the requested->resolved change when they differ. This single reassignment fixes all 9 downstream `$WindowsSKU` consumers (no per-site edits) per D-04. Use ThreadJob-safe idioms (WriteLog, no Read-Host), OTBS, full cmdlet names.
  </action>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -Command "$c=Get-Content .\FFUDevelopment\BuildFFUVM.ps1 -Raw; $ok = ($c -match 'function Get-WindowsTargetRuntimeState') -and ($c -match 'Get-WindowsImageSelection') -and ($c -match 'ResolvedWindowsSKU') -and ($c -notmatch 'Get-Index -WindowsImagePath'); $t=$null; [void][System.Management.Automation.Language.Parser]::ParseInput($c,[ref]$null,[ref]$t); if ($ok -and $t.Count -eq 0) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    - `BuildFFUVM.ps1` contains `function Get-WindowsTargetRuntimeState` and a call to `Get-WindowsImageSelection` (not `Get-Index`).
    - The selection caller reassigns `$WindowsSKU = $windowsImageSelection.ResolvedWindowsSKU` guarded by an `IsNullOrWhiteSpace` check and recomputes `$installationType`/`$WindowsVersion`/`$isLTSC` via `Get-WindowsTargetRuntimeState`.
    - No remaining `Get-Index -WindowsImagePath` call and no `-ISOPath` argument on the selection call.
    - `BuildFFUVM.ps1` parses with zero tokenizer errors (automated verify exits 0).
    - `Invoke-ScriptAnalyzer -Path .\FFUDevelopment\BuildFFUVM.ps1 -Severity Error` reports no new errors.
  </acceptance_criteria>
  <done>The caller consumes the rich selection object, reassigns $WindowsSKU to the resolved edition, and recomputes runtime state so naming/caching/servicing all use the selected edition.</done>
</task>

<task type="auto">
  <name>Task 3: Create Test-Phase51Correctness.ps1 with CORRECT-01/04 + map-completeness gate</name>
  <read_first>
    - FFUDevelopment/Tests/Test-OSPartitionLookup.ps1 (custom Write-TestResult framework, content-match assertions, exit 0/1 tail) and Test-ErrorHandling.ps1 (module-load + PSModulePath setup pattern)
    - 51-PATTERNS.md "Test-Phase51Correctness.ps1 new test file" (header, Write-TestResult, module-path setup, content-match, SKU->EditionId map coverage) and "Critical Implementation Notes for Planner" (custom framework, NOT Pester; use -match assertions)
    - 51-VALIDATION.md "Per-Task Verification Map" + "Wave 0 Requirements" (map-completeness gate D-06)
  </read_first>
  <files>FFUDevelopment/Tests/Test-Phase51Correctness.ps1</files>
  <action>
    Create `FFUDevelopment/Tests/Test-Phase51Correctness.ps1` using the repo's custom `Write-TestResult` framework (NOT Pester — the repo has no Pester/Tests/Unit infrastructure; follow Test-OSPartitionLookup.ps1 exactly, including `$script:PassCount`/`$script:FailCount` and the `exit 0` / `exit 1` summary tail). Add the PSModulePath setup block and `Import-Module FFU.Imaging` load tests. Cover, for this plan, the CORRECT-04 and CORRECT-01 surfaces via content-match and module-load assertions: (a) module loads and exports `Get-WindowsImageSelection` + `Get-ResolvedWindowsSKUFromImage`, and `Get-Index` is NOT exported; (b) SKU->EditionId map-completeness gate (D-06) — assert FFU.Imaging.psm1 content contains each mandatory EditionId token: Core, CoreN, CoreSingleLanguage, Education, EducationN, Professional, ProfessionalN, ProfessionalEducation, ProfessionalEducationN, ProfessionalWorkstation, ProfessionalWorkstationN, Enterprise, EnterpriseN, EnterpriseS, EnterpriseSN, IoTEnterpriseS, IoTEnterpriseSN, ServerStandard, ServerDatacenter; (c) no `Read-Host` and no `while ($true)` in FFU.Imaging.psm1; (d) the throw-on-ambiguity message (`No matching Windows image found`) is present; (e) the caller in BuildFFUVM.ps1 reassigns `$WindowsSKU = $windowsImageSelection.ResolvedWindowsSKU` and defines `Get-WindowsTargetRuntimeState`. Leave clearly-labelled placeholder section headers for CORRECT-03 (Plan 02) and CORRECT-02 (Plan 03) so later plans append their assertions. Header `.NOTES` must state the run command and that real non-English-media / Secure-Boot-2023 behaviors are human-UAT only.
  </action>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File .\FFUDevelopment\Tests\Test-Phase51Correctness.ps1</automated>
  </verify>
  <acceptance_criteria>
    - `FFUDevelopment/Tests/Test-Phase51Correctness.ps1` exists, uses `Write-TestResult` (not `Describe`/`It`), and `exit`s 0 when all its current tests pass.
    - Running the file exits 0 (all CORRECT-01/04 + map-completeness assertions green) given Tasks 1-2 are complete.
    - The file contains a map-completeness assertion referencing each mandatory EditionId token and an assertion that `Get-Index` is not exported.
    - Placeholder section markers for CORRECT-03 and CORRECT-02 are present for later plans to extend.
  </acceptance_criteria>
  <done>A runnable content-match suite proves CORRECT-01/04 source-and-export state and enforces SKU->EditionId map completeness, exiting non-zero on any regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user config -> build ($WindowsSKU) | Friendly SKU string from user config drives image-index selection |
| WIM/ESD media -> capture | DISM-reported EditionId/InstallationType selects which edition is captured |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-01 | Spoofing/Tampering | Wrong-edition capture from multi-edition/non-English media | mitigate | EditionId+InstallationType selection (D-05/D-07) replaces locale-fragile substring; exact `ImageName -eq` fallback (D-08) cannot manufacture a false positive; ambiguity throws rather than guessing (D-03) — a wrong/unintended edition is a licensing/trust failure. |
| T-51-02 | Tampering | Stale $WindowsSKU after fallback mislabels FFU naming/cache/servicing | mitigate | Single guarded reassignment of $WindowsSKU + runtime-state recompute (D-04) ensures the artifact is labelled as the edition actually captured. |
| T-51-03 | Denial of Service | Read-Host fallback deadlocks unattended ThreadJob builds | mitigate | Delete the interactive loop (D-01); auto-select single candidate or throw — no path blocks on console input. |
| T-51-IV | Input Validation | Unknown/invalid $WindowsSKU | accept | SKU legality is already gated at BuildFFUVM.ps1:~965; the map returns @() for unknown SKUs and selection fails closed (throw). No new validation required. |
</threat_model>

<verification>
- `powershell -NoProfile -ExecutionPolicy Bypass -File .\FFUDevelopment\Tests\Test-Phase51Correctness.ps1` exits 0.
- `Import-Module .\FFUDevelopment\Modules\FFU.Imaging\FFU.Imaging.psd1 -Force` succeeds; exports include the two new functions, not `Get-Index`.
- `Invoke-ScriptAnalyzer` on FFU.Imaging.psm1 and BuildFFUVM.ps1 reports no new errors.
- Manual-only (human UAT, deferred): correct edition captured from a real non-English / multi-edition ISO; single-edition ISO salvage names the FFU for the present edition.
</verification>

<success_criteria>
- CORRECT-04: image index selected via EditionId/InstallationType (not substring); SKU->EditionId map complete and test-gated.
- CORRECT-01: fallback edition auto-selected non-interactively; $WindowsSKU reassigned so naming/caching/servicing use the selected edition; Read-Host loop removed.
- Module imports cleanly; new test suite green for CORRECT-01/04.
</success_criteria>

<output>
Create `.planning/phases/51-capture-boot-correctness/51-01-SUMMARY.md` when done.
</output>
