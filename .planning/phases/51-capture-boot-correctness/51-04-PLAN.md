---
phase: 51-capture-boot-correctness
plan: 04
type: execute
wave: 4
depends_on: ["51-01", "51-02", "51-03"]
files_modified:
  - FFUDevelopment/version.json
  - CHANGELOG_FORK.md
  - FFUDevelopment/WinPEDeployFFUFiles/ApplyFFU.ps1
autonomous: true
requirements: [CORRECT-01, CORRECT-02, CORRECT-03, CORRECT-04]
must_haves:
  truths:
    - "version.json (single source of truth) reflects the milestone version and the bumped FFU.Imaging (1.4.0) and FFU.Preflight (1.7.0) module versions."
    - "CHANGELOG_FORK.md documents all four Phase 51 correctness fixes (CORRECT-01..04)."
    - "verify-app passes: all repo tests (including Test-Phase51Correctness.ps1) succeed, no new PSScriptAnalyzer errors, all modules import (project BLOCKING gate)."
  artifacts:
    - path: "FFUDevelopment/version.json"
      provides: "Main version + module version bumps for FFU.Imaging and FFU.Preflight"
      contains: "FFU.Imaging"
    - path: "CHANGELOG_FORK.md"
      provides: "Phase 51 changelog entry covering CORRECT-01..04"
      contains: "CORRECT-0"
  key_links:
    - from: "FFUDevelopment/version.json"
      to: "FFU.Imaging.psd1 / FFU.Preflight.psd1"
      via: "module version sync (1.4.0 / 1.7.0)"
      pattern: "1.4.0"
---

<objective>
Finalize Phase 51: synchronize version.json with the module bumps made in Plans 01-02, record all four correctness fixes in CHANGELOG_FORK.md, update the hardcoded WinPE `$version`, and run the project's BLOCKING verify-app regression gate.

Purpose: The project version policy (CLAUDE.md, config.json) requires version.json as the single source of truth, a CHANGELOG_FORK entry, and a passing verify-app run before any code phase can be marked complete. This plan closes those obligations for Phase 51.

Output: Updated version.json (main + FFU.Imaging 1.4.0 + FFU.Preflight 1.7.0), CHANGELOG_FORK.md Phase 51 section, updated ApplyFFU.ps1 `$version`, and a green verify-app result.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-capture-boot-correctness/51-CONTEXT.md
@.planning/phases/51-capture-boot-correctness/51-RESEARCH.md
@.planning/phases/51-capture-boot-correctness/51-PATTERNS.md
@CLAUDE.md
@docs/PM_CHECKLIST.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump version.json, ApplyFFU.ps1 $version, and write the CHANGELOG_FORK entry</name>
  <read_first>
    - FFUDevelopment/version.json (current main version 1.11.0; modules section — locate FFU.Imaging and FFU.Preflight entries and their current versions)
    - CHANGELOG_FORK.md (head — most recent entry format under "## [1.11.0]"; match the heading + section style)
    - FFUDevelopment/WinPEDeployFFUFiles/ApplyFFU.ps1 (hardcoded `$version` near the top — runs in WinPE without version.json access)
    - CLAUDE.md "Versioning Policy" + "Version Increment Checklist" (MINOR bump for user-facing correctness features; module change requires version.json update + release notes)
    - 51-PATTERNS.md "version.json — version bump" (target module values FFU.Imaging 1.4.0, FFU.Preflight 1.7.0)
  </read_first>
  <files>FFUDevelopment/version.json, FFUDevelopment/WinPEDeployFFUFiles/ApplyFFU.ps1, CHANGELOG_FORK.md</files>
  <action>
    In FFUDevelopment/version.json: bump the main `version` to `1.12.0` (MINOR — milestone v1.12.0 correctness features; confirm the current value is 1.11.0 first and adjust if it has moved), set `buildDate` to the current date, and update the `modules` section so the FFU.Imaging entry reads `1.4.0` and the FFU.Preflight entry reads `1.7.0`, each with a Phase 51 description (FFU.Imaging: "Phase 51 CORRECT-01/03/04: Get-WindowsImageSelection EditionId matching + Add-BootFiles ADK bcdboot"; FFU.Preflight: "Phase 51 CORRECT-03: ADK bcdboot.exe preflight check in Test-FFUADK"). Ensure these match the `.psd1` ModuleVersion values set in Plans 01/02 exactly. Update the hardcoded `$version` in FFUDevelopment/WinPEDeployFFUFiles/ApplyFFU.ps1 to the same main version (per Version Increment Checklist item 4). In CHANGELOG_FORK.md, add a new `## [1.12.0] - <date>` section above the 1.11.0 entry titled for Phase 51 (Capture/Boot Correctness) with four bullet sub-entries: CORRECT-01 (selected-edition propagation after fallback), CORRECT-02 (LTSC year normalization for OEM drivers), CORRECT-03 (ADK bcdboot for Secure Boot 2023), CORRECT-04 (EditionId/InstallationType image selection), each citing the upstream commit (5aaa1ad / 04dfb5f / 6c0ee8a / b2a7ef5) and noting the ThreadJob-safe deletion of the Read-Host fallback under CORRECT-01. Match the existing changelog heading/section style.
  </action>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -Command "$j=Get-Content .\FFUDevelopment\version.json -Raw | ConvertFrom-Json; $cl=Get-Content .\CHANGELOG_FORK.md -Raw; $imgOk = $j.modules.'FFU.Imaging'.version -eq '1.4.0'; $prefOk = $j.modules.'FFU.Preflight'.version -eq '1.7.0'; $clOk = ($cl -match 'CORRECT-01') -and ($cl -match 'CORRECT-02') -and ($cl -match 'CORRECT-03') -and ($cl -match 'CORRECT-04'); if ($imgOk -and $prefOk -and $clOk) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    - version.json `modules.FFU.Imaging.version` is `1.4.0` and `modules.FFU.Preflight.version` is `1.7.0`, matching the respective `.psd1` ModuleVersion values.
    - version.json main `version` bumped (MINOR) and `buildDate` updated; ApplyFFU.ps1 `$version` matches.
    - CHANGELOG_FORK.md has a Phase 51 section referencing CORRECT-01, CORRECT-02, CORRECT-03, CORRECT-04 with their upstream commit IDs.
    - Automated verify exits 0.
  </acceptance_criteria>
  <done>version.json, ApplyFFU.ps1, and CHANGELOG_FORK.md are consistent and document all four Phase 51 fixes.</done>
</task>

<task type="auto">
  <name>Task 2: Run the BLOCKING verify-app regression gate</name>
  <read_first>
    - CLAUDE.md "Mandatory Verification (BLOCKING)" + "GSD Command Integration" (verify-app is BLOCKING; Pester tests pass, no new PSScriptAnalyzer errors, all modules import)
    - .claude/agents/verify-app.md (verify-app agent contract)
    - 51-VALIDATION.md "Sampling Rate" (before verify-work: targeted module suites green + PSScriptAnalyzer clean + modules import)
    - .claude/projects/D--claude-FFUBuilder/memory/pester-suite-targeted-not-full.md (full suite has known pre-existing env-dependent reds; verify with targeted files)
  </read_first>
  <files>(verification only — no files modified)</files>
  <action>
    Invoke verify-app per CLAUDE.md (Task tool with subagent_type="general-purpose", prompt: "Act as verify-app agent (read .claude/agents/verify-app.md). Run full regression verification."). Then independently confirm the Phase 51 gates: (1) run `powershell -NoProfile -ExecutionPolicy Bypass -File .\FFUDevelopment\Tests\Test-Phase51Correctness.ps1` and confirm exit 0; (2) run `Invoke-ScriptAnalyzer` on FFU.Imaging.psm1, FFU.Preflight.psm1, and BuildFFUVM.ps1 with `-Severity Error` and confirm no NEW errors vs baseline; (3) confirm `Import-Module` succeeds for FFU.Imaging and FFU.Preflight. Per the targeted-tests memory note, treat known pre-existing env-dependent reds (e.g. WimMount) in the full suite as NOT Phase 51 regressions — confirm there are no NEW failures attributable to Phase 51 changes. If any NEW Phase 51 failure, new PSScriptAnalyzer error, or import failure is found, HALT and return to the offending plan to fix before marking the phase complete (project BLOCKING halt condition).
  </action>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File .\FFUDevelopment\Tests\Test-Phase51Correctness.ps1</automated>
  </verify>
  <acceptance_criteria>
    - Test-Phase51Correctness.ps1 exits 0.
    - verify-app reports no new Pester/test failures, no new PSScriptAnalyzer errors, and all modules import (BLOCKING halt condition cleared).
    - Any failure is either a documented pre-existing env-dependent red (not Phase 51) or has been fixed.
  </acceptance_criteria>
  <done>The project's BLOCKING verification gate passes for Phase 51; the phase may be marked complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| version metadata -> build/runtime | version.json and the WinPE-hardcoded $version label artifacts and surface in the UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-04A | Repudiation | Module .psd1 versions drift from version.json (untraceable artifact provenance) | mitigate | version.json module entries are set to exactly match the .psd1 ModuleVersion values; verify gate cross-checks. |
| T-51-04N | N/A | Documentation + verification only | accept | No code-execution, network, or boot-trust surface introduced by this plan; the verify-app gate is the safety control. |
| T-51-SC | Tampering | npm/pip/cargo installs | accept | This phase installs no external packages (RESEARCH.md: Package Legitimacy Audit not applicable); no supply-chain surface. |
</threat_model>

<verification>
- version.json module versions match the .psd1 values; main version + buildDate + ApplyFFU.ps1 $version updated.
- CHANGELOG_FORK.md documents CORRECT-01..04.
- verify-app BLOCKING gate green: Phase 51 suite exits 0, no new PSScriptAnalyzer errors, all modules import.
</verification>

<success_criteria>
- All four CORRECT-01..04 fixes are versioned, changelogged, and pass the project's BLOCKING verify-app regression gate; Phase 51 is complete.
</success_criteria>

<output>
Create `.planning/phases/51-capture-boot-correctness/51-04-SUMMARY.md` when done.
</output>
