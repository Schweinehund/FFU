---
phase: 47-hypervisor-conditional-logic-auto-remediation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - FFUDevelopment/Modules/FFU.Preflight/FFU.Preflight.psm1
  - FFUDevelopment/Modules/FFU.Preflight/FFU.Preflight.psd1
autonomous: true

must_haves:
  truths:
    - "Repair-FFUWimMount callable as standalone function returns PSCustomObject with Succeeded, Message, DurationMs"
    - "Repair-FFUDismState callable as standalone function runs DISM RestoreHealth and returns result"
    - "Repair-FFUNetwork callable as standalone function clears DNS cache and tests connectivity"
    - "All three repair functions are exported from FFU.Preflight module"
  artifacts:
    - path: "FFUDevelopment/Modules/FFU.Preflight/FFU.Preflight.psm1"
      provides: "Repair-FFUWimMount, Repair-FFUDismState, Repair-FFUNetwork functions"
      contains: "function Repair-FFUWimMount"
    - path: "FFUDevelopment/Modules/FFU.Preflight/FFU.Preflight.psd1"
      provides: "Module manifest exporting repair functions"
      contains: "Repair-FFUWimMount"
  key_links:
    - from: "FFU.Preflight.psm1 Repair-FFUWimMount"
      to: "Test-FFUWimMount existing repair logic"
      via: "Extracted auto-repair sequence (registry, service, fltmc)"
      pattern: "fltmc.*load.*WimMount"
---

<objective>
Create standalone repair functions in FFU.Preflight for one-click auto-remediation from the dashboard.

Purpose: The dashboard needs callable repair functions for safe auto-fixes (REM-01). The WIMMount repair logic already exists inside Test-FFUWimMount but is not callable separately. DISM restore health and DNS cache clear need wrapper functions.

Output: Three new exported functions in FFU.Preflight: Repair-FFUWimMount, Repair-FFUDismState, Repair-FFUNetwork.
</objective>

<execution_context>
@C:\Users\joanderson\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\joanderson\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-hypervisor-conditional-logic-auto-remediation/47-RESEARCH.md
@FFUDevelopment/Modules/FFU.Preflight/FFU.Preflight.psm1
@FFUDevelopment/Modules/FFU.Preflight/FFU.Preflight.psd1
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create repair functions in FFU.Preflight.psm1</name>
  <files>FFUDevelopment/Modules/FFU.Preflight/FFU.Preflight.psm1</files>
  <action>
Add three new public functions to FFU.Preflight.psm1. Place them after the existing Test-FFU* functions, before the helpers section.

**1. Repair-FFUWimMount**
Extract the auto-repair logic from Test-FFUWimMount (the 6-step repair sequence: verify wimmount.sys, create registry entries, create filter instance, start service, load filter, re-verify). The function should:
- Accept no mandatory parameters (repair is self-contained)
- Use `[CmdletBinding()]` and `[OutputType([PSCustomObject])]`
- Track duration using `[System.Diagnostics.Stopwatch]::StartNew()`
- Return `[PSCustomObject]@{ Succeeded = [bool]; Message = [string]; DurationMs = [int] }`
- Wrap all operations in try/catch — return Succeeded=$false on any exception
- NOT call Test-FFUWimMount (avoid circular dependency) — perform the repair steps directly
- Use the same repair strategies from lines 2064-2132 of the existing Test-FFUWimMount: registry repair, service start via sc.exe, fltmc load WimMount
- After repair, verify success by running `fltmc.exe filters` and checking for 'WimMount' in output

**2. Repair-FFUDismState**
Create a wrapper for DISM RestoreHealth:
- Accept no mandatory parameters
- Use `[CmdletBinding()]` and `[OutputType([PSCustomObject])]`
- Track duration with Stopwatch
- Execute: `dism.exe /Online /Cleanup-Image /RestoreHealth` using `& dism.exe` (not Invoke-Expression)
- Capture exit code and output
- Return `[PSCustomObject]@{ Succeeded = ($LASTEXITCODE -eq 0); Message = [string]; DurationMs = [int] }`
- On success: Message = "DISM component store repair completed successfully"
- On failure: Message = "DISM repair failed (exit code: $LASTEXITCODE)" with captured output

**3. Repair-FFUNetwork**
Create a DNS cache clear + connectivity test:
- Accept no mandatory parameters
- Use `[CmdletBinding()]` and `[OutputType([PSCustomObject])]`
- Track duration with Stopwatch
- Execute: `Clear-DnsClientCache` (cmdlet)
- Then test with `Test-NetConnection -ComputerName 'www.microsoft.com' -Port 443 -WarningAction SilentlyContinue`
- Return `[PSCustomObject]@{ Succeeded = $result.TcpTestSucceeded; Message = [string]; DurationMs = [int] }`
- On success: "DNS cache cleared and network connectivity verified"
- On failure: "DNS cache cleared but network connectivity test failed"

All three functions must follow PowerShell style: OTBS braces, full cmdlet names, 4-space indentation, proper comment-based help with .SYNOPSIS, .DESCRIPTION, .OUTPUTS, .EXAMPLE sections.
  </action>
  <verify>
Run: `pwsh -NoProfile -Command "Import-Module C:/claude/FFUBuilder/FFUDevelopment/Modules/FFU.Preflight/FFU.Preflight.psd1 -Force; Get-Command Repair-FFUWimMount, Repair-FFUDismState, Repair-FFUNetwork"`
All three commands should resolve without error.
  </verify>
  <done>Three repair functions exist in FFU.Preflight.psm1, each returning PSCustomObject with Succeeded/Message/DurationMs properties.</done>
</task>

<task type="auto">
  <name>Task 2: Export repair functions from FFU.Preflight manifest</name>
  <files>FFUDevelopment/Modules/FFU.Preflight/FFU.Preflight.psd1</files>
  <action>
Add the three new repair functions to the FunctionsToExport array in FFU.Preflight.psd1.

Add these entries after the existing 'Invoke-FFUDISMCleanup' line in the export list:
- 'Repair-FFUWimMount'
- 'Repair-FFUDismState'
- 'Repair-FFUNetwork'

Add a comment above them: `# Phase 47: Auto-remediation repair functions`

Do NOT update the module version yet — that happens in Plan 04.
  </action>
  <verify>
Run: `pwsh -NoProfile -Command "Import-Module C:/claude/FFUBuilder/FFUDevelopment/Modules/FFU.Preflight/FFU.Preflight.psd1 -Force; (Get-Module FFU.Preflight).ExportedFunctions.Keys | Where-Object { $_ -like 'Repair-*' }"`
Should output: Repair-FFUWimMount, Repair-FFUDismState, Repair-FFUNetwork
  </verify>
  <done>FFU.Preflight.psd1 exports all three Repair-* functions.</done>
</task>

</tasks>

<verification>
1. All three Repair-* functions importable: `Import-Module FFU.Preflight -Force; Get-Command Repair-FFU*`
2. Each function has proper CmdletBinding and OutputType attributes
3. Each function returns PSCustomObject with Succeeded, Message, DurationMs properties
4. No PSScriptAnalyzer errors on new functions: `Invoke-ScriptAnalyzer -Path FFU.Preflight.psm1 -IncludeRule PSUseCmdletCorrectly`
</verification>

<success_criteria>
- Three standalone repair functions exist in FFU.Preflight and are exported
- Functions are callable independently without requiring Test-FFU* check functions
- Return format is consistent: PSCustomObject with Succeeded (bool), Message (string), DurationMs (int)
- WimMount repair uses the proven registry+service+fltmc approach from existing code
</success_criteria>

<output>
After completion, create `.planning/phases/47-hypervisor-conditional-logic-auto-remediation/47-01-SUMMARY.md`
</output>
