---
phase: 47-hypervisor-conditional-logic-auto-remediation
plan: 04
type: execute
wave: 3
depends_on: [47-01, 47-02, 47-03]
files_modified:
  - Tests/Unit/FFUUI.Core.Dashboard.AutoRemediation.Tests.ps1
  - FFUDevelopment/Modules/FFU.Preflight/FFU.Preflight.psd1
  - FFUDevelopment/FFUUI.Core/FFUUI.Core.psd1
  - FFUDevelopment/version.json
  - CHANGELOG_FORK.md
autonomous: true

must_haves:
  truths:
    - "All Pester tests pass including new auto-remediation and hypervisor visibility tests"
    - "FFU.Preflight module version bumped and release notes updated"
    - "FFUUI.Core module version bumped and release notes updated"
    - "Main version.json version bumped (PATCH minimum)"
    - "CHANGELOG_FORK.md has Phase 47 entry"
    - "All modules import successfully"
    - "No new PSScriptAnalyzer errors"
  artifacts:
    - path: "Tests/Unit/FFUUI.Core.Dashboard.AutoRemediation.Tests.ps1"
      provides: "Pester tests for Phase 47 features"
      contains: "Describe"
    - path: "FFUDevelopment/version.json"
      provides: "Updated version numbers"
      contains: "1.11.2"
    - path: "CHANGELOG_FORK.md"
      provides: "Phase 47 changelog entry"
      contains: "Phase 47"
  key_links:
    - from: "Tests/Unit/FFUUI.Core.Dashboard.AutoRemediation.Tests.ps1"
      to: "FFUUI.Core.Dashboard.psm1"
      via: "Import-Module and function testing"
      pattern: "Import-Module.*Dashboard"
---

<objective>
Create Pester tests for Phase 47 features, bump versions, update changelog, and run verification.

Purpose: Final plan ensures all Phase 47 work is tested, versioned, and documented before marking the phase complete. Tests validate the dashboard module functions (repair maps, command extraction, duration formatting, hypervisor visibility), version bumps follow the project's versioning policy, and the changelog documents the new capabilities.

Output: Passing test suite, updated versions, changelog entry.
</objective>

<execution_context>
@C:\Users\joanderson\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\joanderson\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-hypervisor-conditional-logic-auto-remediation/47-01-SUMMARY.md
@.planning/phases/47-hypervisor-conditional-logic-auto-remediation/47-02-SUMMARY.md
@.planning/phases/47-hypervisor-conditional-logic-auto-remediation/47-03-SUMMARY.md
@FFUDevelopment/FFUUI.Core/FFUUI.Core.Dashboard.psm1
@FFUDevelopment/Modules/FFU.Preflight/FFU.Preflight.psd1
@FFUDevelopment/FFUUI.Core/FFUUI.Core.psd1
@FFUDevelopment/version.json
@CHANGELOG_FORK.md
@Tests/Unit/FFUUI.Core.Dashboard.Tests.ps1
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Pester tests for Phase 47 dashboard features</name>
  <files>Tests/Unit/FFUUI.Core.Dashboard.AutoRemediation.Tests.ps1</files>
  <action>
Create a new Pester test file: `Tests/Unit/FFUUI.Core.Dashboard.AutoRemediation.Tests.ps1`

Follow the same pattern as the existing `Tests/Unit/FFUUI.Core.Dashboard.Tests.ps1` for setup (module import, mock state creation).

**Test structure:**

```powershell
BeforeAll {
    # Import the dashboard module directly (not via FFUUI.Core.psd1 which needs WPF assemblies)
    $dashboardPath = Join-Path $PSScriptRoot '../../FFUDevelopment/FFUUI.Core/FFUUI.Core.Dashboard.psm1'
    Import-Module $dashboardPath -Force
}
```

**Test groups to include:**

1. **Describe 'Get-SafeRepairMap'**
   - It 'returns a hashtable' — confirm type is hashtable
   - It 'contains WimMount mapping' — `$map['WimMount']` should be 'Repair-FFUWimMount'
   - It 'contains DISMState mapping' — should be 'Repair-FFUDismState'
   - It 'contains DISMCleanup mapping' — should be 'Invoke-FFUDISMCleanup'
   - It 'contains Network mapping' — should be 'Repair-FFUNetwork'
   - It 'does not contain ADK' — ADK is NOT auto-fixable per research decision

2. **Describe 'Get-UnsafeRemediationMap'**
   - It 'returns a hashtable' — confirm type
   - It 'contains HyperV mapping' — `$map['HyperV']` should have Command, RequiresReboot, ConfirmMessage
   - It 'HyperV requires reboot' — `$map['HyperV'].RequiresReboot` should be `$true`
   - It 'HyperV command contains Enable-WindowsOptionalFeature' — confirm command text

3. **Describe 'Extract-PowerShellCommands' (internal but testable via InModuleScope)**
   Use `InModuleScope` to test the internal function:
   - It 'extracts commands from standard remediation block' — provide a sample New-FFURemediationBlock format string, verify commands extracted
   - It 'skips comment lines' — comments (# lines) should not be in output
   - It 'skips blank lines' — empty lines filtered out
   - It 'returns full text as fallback when no FIX section found' — non-standard text returns the full text

4. **Describe 'Format-CheckDuration' (internal but testable via InModuleScope)**
   Use `InModuleScope`:
   - It 'returns empty string for zero duration' — 0 -> ''
   - It 'returns empty string for negative duration' — -1 -> ''
   - It 'formats milliseconds to seconds with 1 decimal' — 1234 -> ' (1.2s)'
   - It 'formats sub-second durations' — 500 -> ' (0.5s)'
   - It 'formats multi-second durations' — 15678 -> ' (15.7s)'

5. **Describe 'Update-HypervisorCategoryVisibility'**
   Create mock state with PSCustomObject:
   ```powershell
   $mockState = [PSCustomObject]@{
       Controls = @{
           borderHypervisorInfo = [PSCustomObject]@{ Visibility = 'Collapsed'; Text = '' }
           txtHypervisorInfo = [PSCustomObject]@{ Text = '' }
       }
   }
   ```
   - It 'shows info banner for HyperV selection' — Visibility should be Visible
   - It 'sets correct text for HyperV' — should contain 'Hyper-V' and 'VMware checks skipped'
   - It 'shows info banner for VMware selection' — Visibility should be Visible
   - It 'sets correct text for VMware' — should contain 'VMware Workstation' and 'Hyper-V checks skipped'
   - It 'shows info banner for Auto selection' — Visibility should be Visible
   - It 'sets correct text for Auto' — should contain 'Auto-detected'

6. **Describe 'Update-DashboardCheckUI with DurationMs'**
   Use the mock state pattern from existing Dashboard.Tests.ps1:
   - It 'appends duration to message for passed check' — message should end with '(X.Xs)'
   - It 'does not append duration when DurationMs is 0' — message should not contain '('
   - It 'creates Fix button for WimMount failure' — find button named btnFix_WimMount in panel
   - It 'creates unsafe Fix button for HyperV failure' — find button named btnFixUnsafe_HyperV
   - It 'creates Details expander for failed check with remediation' — find Expander in panel
   - It 'does not create Fix button for ADK failure' — ADK not in SafeRepairMap

7. **Describe 'Invoke-DashboardRemediation'**
   Mock the repair functions to avoid actual system changes:
   - It 'returns failure for unknown check name' — Succeeded should be $false
   - It 'calls correct repair function for WimMount' — Mock Repair-FFUWimMount, verify called
   - It 'returns result with Succeeded, Message, DurationMs properties' — verify output shape

Target: 25-30 tests covering all new functionality.

**Important:** Use `InModuleScope 'FFUUI.Core.Dashboard'` for testing internal functions (Extract-PowerShellCommands, Format-CheckDuration). For public functions, call directly after import.

For tests that need WPF types (like checking StackPanel.Children), skip those tests with `It 'description' -Skip` if running in non-interactive context. Focus on testable logic (maps, parsing, formatting).
  </action>
  <verify>
Run: `pwsh -NoProfile -Command "Invoke-Pester -Path 'C:/claude/FFUBuilder/Tests/Unit/FFUUI.Core.Dashboard.AutoRemediation.Tests.ps1' -Output Detailed -PassThru | Select-Object TotalCount, PassedCount, FailedCount"`
All tests should pass. Zero failures.
  </verify>
  <done>Pester test file created with 25+ tests covering repair maps, command extraction, duration formatting, hypervisor visibility, and dashboard UI enhancements. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Bump versions, update changelog, run verify-app</name>
  <files>
    FFUDevelopment/Modules/FFU.Preflight/FFU.Preflight.psd1
    FFUDevelopment/FFUUI.Core/FFUUI.Core.psd1
    FFUDevelopment/version.json
    CHANGELOG_FORK.md
  </files>
  <action>
**A. Bump FFU.Preflight version:**
In FFU.Preflight.psd1:
- ModuleVersion: `1.6.0` -> `1.7.0` (MINOR — new Repair-* functions are new user-facing features)
- Add release notes for v1.7.0 at top of ReleaseNotes:
```
## v1.7.0 (2026-02-06)
### Phase 47: Auto-Remediation Repair Functions
- **NEW**: Repair-FFUWimMount - Standalone WIMMount repair (registry, service, fltmc)
- **NEW**: Repair-FFUDismState - DISM component store RestoreHealth wrapper
- **NEW**: Repair-FFUNetwork - DNS cache clear and connectivity verification
- All repair functions return consistent PSCustomObject (Succeeded, Message, DurationMs)
- Extracted from existing check logic for dashboard one-click fix integration
```

**B. Bump FFUUI.Core version:**
In FFUUI.Core.psd1:
- ModuleVersion: `0.1.0` -> `0.2.0` (MINOR — new dashboard functions for remediation, hypervisor visibility)
- Add release notes for v0.2.0 at top of ReleaseNotes:
```
v0.2.0: Phase 47 - Hypervisor conditional logic & auto-remediation
- NEW: Update-HypervisorCategoryVisibility - Shows/hides hypervisor info banner
- NEW: Invoke-DashboardRemediation - Executes safe repair functions
- NEW: Get-SafeRepairMap / Get-UnsafeRemediationMap - Expose repair mappings
- ENHANCED: Update-DashboardCheckUI - Fix button, Details expander, duration display
- Fix button inline for safe issues (WimMount, DISM, Network)
- Unsafe fix shows MessageBox confirmation (Hyper-V enablement)
- Details expander with monospace PowerShell commands and Copy button
- Duration display "(X.Xs)" on every check result
```

**C. Bump main version.json:**
- version: `1.11.1` -> `1.11.2` (PATCH — subcomponent version changes)
- buildDate: `2026-02-06` (current date)
- Update modules section:
  - FFU.Preflight version: `1.7.0`, description: "Phase 47: Auto-remediation repair functions (Repair-FFUWimMount, Repair-FFUDismState, Repair-FFUNetwork)"
  - FFUUI.Core version: `0.2.0`, description: "Phase 47: Hypervisor conditional logic, one-click fix, Details expander, duration display"

**D. Update CHANGELOG_FORK.md:**
Add Phase 47 entry at the top (after the header, before Phase 46 entry):
```markdown
## Phase 47: Hypervisor Conditional Logic & Auto-Remediation (v1.11.2)
*Date: 2026-02-06*

### Hypervisor Conditional Logic (HYP-01 through HYP-05)
- Dashboard shows inline info banner indicating active hypervisor ("Validating for: VMware Workstation")
- Hypervisor info banner indicates which checks are skipped ("Hyper-V checks skipped")
- Backend already conditional (Invoke-FFUPreflight) — dashboard now surfaces this visually

### Auto-Remediation (REM-01 through REM-04)
- One-click Fix button for safe issues: WIMMount repair, DISM cleanup, DISM restore health, DNS cache clear
- Fix button shows "Fixing..." disabled state during execution, re-runs single check on completion
- Unsafe remediations (Hyper-V enablement) show WPF MessageBox with exact command and reboot warning
- Declined fixes show "Fix available" indicator distinct from pass/fail states
- Failed checks have expandable Details section with copy-paste PowerShell commands in monospace textbox
- Copy button copies commands to clipboard with "Copied!" feedback (2-second async reset)
- Every completed check displays execution duration: "Passed (1.2s)" or "Failed (0.8s)"

### Modules Updated
- FFU.Preflight v1.7.0: Repair-FFUWimMount, Repair-FFUDismState, Repair-FFUNetwork
- FFUUI.Core v0.2.0: 4 new functions, enhanced Update-DashboardCheckUI
```

**E. Run verify-app agent:**
Invoke verify-app to run full regression verification:
1. Run all Pester tests: `.\Tests\Unit\Invoke-PesterTests.ps1`
2. Run PSScriptAnalyzer on modified files
3. Verify all modules import successfully
4. Confirm no regressions

If verify-app finds issues, fix them before completing this task.
  </action>
  <verify>
1. `pwsh -NoProfile -Command "(Import-PowerShellDataFile 'C:/claude/FFUBuilder/FFUDevelopment/Modules/FFU.Preflight/FFU.Preflight.psd1').ModuleVersion"` should return `1.7.0`
2. `pwsh -NoProfile -Command "(Import-PowerShellDataFile 'C:/claude/FFUBuilder/FFUDevelopment/FFUUI.Core/FFUUI.Core.psd1').ModuleVersion"` should return `0.2.0`
3. `pwsh -NoProfile -Command "(Get-Content 'C:/claude/FFUBuilder/FFUDevelopment/version.json' | ConvertFrom-Json).version"` should return `1.11.2`
4. All Pester tests pass (0 failures)
5. No new PSScriptAnalyzer errors on modified files
  </verify>
  <done>Versions bumped (FFU.Preflight 1.7.0, FFUUI.Core 0.2.0, main 1.11.2), changelog updated, all tests pass, verify-app confirms no regressions.</done>
</task>

</tasks>

<verification>
1. All existing tests still pass (no regressions)
2. New tests pass (25+ tests in Dashboard.AutoRemediation.Tests.ps1)
3. Module versions consistent between .psd1 files and version.json
4. CHANGELOG_FORK.md has Phase 47 entry
5. All modules import without errors
6. PSScriptAnalyzer clean on modified files
</verification>

<success_criteria>
- Phase 47 fully tested with passing Pester suite
- All version numbers updated per versioning policy
- Changelog documents all 9 requirements (HYP-01-05, REM-01-04)
- No regressions in existing test suites
- verify-app confirms clean state
</success_criteria>

<output>
After completion, create `.planning/phases/47-hypervisor-conditional-logic-auto-remediation/47-04-SUMMARY.md`
</output>
