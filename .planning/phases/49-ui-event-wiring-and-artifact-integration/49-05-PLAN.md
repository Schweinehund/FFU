---
phase: 49-ui-event-wiring-and-artifact-integration
plan: 05
type: execute
wave: 1
depends_on: ["49-03"]
files_modified:
  - FFUDevelopment/BuildFFUVM_UI.ps1
  - Tests/Unit/Phase49.Tests.ps1
autonomous: true
gap_closure: true
requirements: [DISC-02, DISC-03, USB-02, USB-03]

must_haves:
  truths:
    - "After cancel in USB Mode, button shows 'Create USB' (not 'Build FFU')"
    - "Pester test detects any hardcoded 'Build FFU' button label in BuildFFUVM_UI.ps1"
  artifacts:
    - path: "FFUDevelopment/BuildFFUVM_UI.ps1"
      provides: "Mode-aware button label in cleanup timer"
      contains: "if.*rbUSBMode.*IsChecked.*Create USB.*Build FFU"
    - path: "Tests/Unit/Phase49.Tests.ps1"
      provides: "Hardcoded button label regression test for BuildFFUVM_UI.ps1"
      contains: "hardcoded Build FFU assignment in BuildFFUVM_UI"
  key_links:
    - from: "FFUDevelopment/BuildFFUVM_UI.ps1"
      to: "$script:uiState.Controls.rbUSBMode"
      via: "IsChecked check in cleanup DispatcherTimer Tick handler"
      pattern: "rbUSBMode.*IsChecked"
---

<objective>
Fix the 4th hardcoded "Build FFU" button label in the cancel cleanup DispatcherTimer Tick handler and add a Pester test to prevent regression.

Purpose: When a USB Mode job is cancelled, the cleanup timer resets the button to "Build FFU" instead of mode-aware "Create USB". This is the single production gap from Phase 49 verification.
Output: Mode-aware button label at line 430 + regression test in Phase49.Tests.ps1
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-ui-event-wiring-and-artifact-integration/49-VERIFICATION.md
@.planning/phases/49-ui-event-wiring-and-artifact-integration/49-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix hardcoded button label in cleanup DispatcherTimer Tick handler</name>
  <files>FFUDevelopment/BuildFFUVM_UI.ps1</files>
  <read_first>
    - FFUDevelopment/BuildFFUVM_UI.ps1 (lines 420-435 — the cleanup DispatcherTimer Tick handler with the hardcoded label)
    - FFUDevelopment/FFUUI.Core/FFUUI.Core.StateRecovery.psm1 (lines 140-145 — reference implementation of mode-aware button label pattern)
  </read_first>
  <action>
Replace line 430 of FFUDevelopment/BuildFFUVM_UI.ps1:

BEFORE:
```powershell
$btn.Content = "Build FFU"
```

AFTER:
```powershell
$btn.Content = if ($null -ne $script:uiState.Controls.rbUSBMode -and $script:uiState.Controls.rbUSBMode.IsChecked) { 'Create USB' } else { 'Build FFU' }
```

This matches the exact pattern used in FFUUI.Core.StateRecovery.psm1 Reset-FFUUIToIdle (lines 143-144) — null-safe check on rbUSBMode control before reading IsChecked, with 'Create USB' for USB Mode and 'Build FFU' for Full Build.

Do NOT change any other lines. This is a single-line replacement.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$c = Get-Content 'FFUDevelopment/BuildFFUVM_UI.ps1' -Raw; if ($c -match 'rbUSBMode.*IsChecked.*Create USB.*Build FFU') { Write-Host 'PASS: Mode-aware label found' } else { Write-Error 'FAIL: Mode-aware label not found'; exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    - Line 430 of BuildFFUVM_UI.ps1 contains `rbUSBMode` and `IsChecked` and `Create USB` and `Build FFU` on the same line
    - No bare `$btn.Content = "Build FFU"` remains in the cleanup DispatcherTimer Tick handler (lines 420-435)
    - The surrounding lines (429: `$btn = $script:uiState.Controls.btnRun`, 431: `$btn.IsEnabled = $true`) are unchanged
  </acceptance_criteria>
  <done>Cleanup DispatcherTimer Tick handler uses mode-aware button label identical to StateRecovery pattern</done>
</task>

<task type="auto">
  <name>Task 2: Add Pester test for BuildFFUVM_UI.ps1 hardcoded button label detection</name>
  <files>Tests/Unit/Phase49.Tests.ps1</files>
  <read_first>
    - Tests/Unit/Phase49.Tests.ps1 (lines 247-252 — existing 'Mode-Aware Button Labels' Context with the StateRecovery test, to add the new test alongside it)
  </read_first>
  <action>
In Tests/Unit/Phase49.Tests.ps1, inside the existing `Context 'Mode-Aware Button Labels' -Tag 'ButtonLabels'` block, add a new `It` block AFTER the existing StateRecovery test (after line 252):

```powershell
        It 'Should not have hardcoded Build FFU assignment in BuildFFUVM_UI cancel cleanup path' {
            # BuildFFUVM_UI.ps1 cleanup timer must be mode-aware (not hardcode "Build FFU")
            # Pattern matches both $btnRun.Content and $btn.Content assignments
            $buildUIContent | Should -Not -BeNullOrEmpty
            $buildUIContent | Should -Not -Match "\`$btn\.Content\s*=\s*[`'`"]Build FFU[`'`"]"
        }
```

Verify that `$buildUIContent` is already defined in the test file's BeforeAll or BeforeDiscovery block as a read of BuildFFUVM_UI.ps1. If not, add it in the BeforeAll block at the top of the Describe:

```powershell
$buildUIContent = Get-Content -Path "$PSScriptRoot/../../FFUDevelopment/BuildFFUVM_UI.ps1" -Raw -ErrorAction SilentlyContinue
```

The regex `\$btn\.Content\s*=\s*['"]Build FFU['"]` catches the exact pattern that was missed: a local `$btn` variable with hardcoded "Build FFU" string assignment.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "Invoke-Pester -Path 'Tests/Unit/Phase49.Tests.ps1' -TagFilter 'ButtonLabels' -Output Detailed -PassThru | ForEach-Object { if ($_.FailedCount -gt 0) { exit 1 } }"</automated>
  </verify>
  <acceptance_criteria>
    - Phase49.Tests.ps1 contains an `It` block with text matching `hardcoded Build FFU assignment in BuildFFUVM_UI`
    - The test uses `$buildUIContent | Should -Not -Match` with a pattern matching `$btn.Content` followed by `Build FFU`
    - The test is inside the existing `Context 'Mode-Aware Button Labels'` block
    - Running `Invoke-Pester -Path Tests/Unit/Phase49.Tests.ps1 -TagFilter ButtonLabels` passes all tests (including the new one, after Task 1 fix is applied)
  </acceptance_criteria>
  <done>Pester test detects any future hardcoded "Build FFU" button label in BuildFFUVM_UI.ps1 cleanup timer path</done>
</task>

</tasks>

<verification>
1. `powershell -NoProfile -Command "Select-String -Path 'FFUDevelopment/BuildFFUVM_UI.ps1' -Pattern '\$btn\.Content\s*=\s*[''\""]Build FFU[''\""]' | Measure-Object | ForEach-Object { if ($_.Count -gt 0) { Write-Error 'FAIL: Bare hardcoded label still exists'; exit 1 } else { Write-Host 'PASS: No hardcoded labels' } }"` — confirms no bare hardcoded `$btn.Content = "Build FFU"` remains
2. `Invoke-Pester -Path Tests/Unit/Phase49.Tests.ps1 -TagFilter ButtonLabels -Output Detailed` — all Mode-Aware Button Labels tests pass
</verification>

<success_criteria>
- Zero instances of bare `$btn.Content = "Build FFU"` in BuildFFUVM_UI.ps1 (all mode-aware)
- Phase49.Tests.ps1 ButtonLabels tag produces 2 passing tests (StateRecovery + BuildFFUVM_UI)
- Both gaps from 49-VERIFICATION.md resolved
</success_criteria>

<output>
After completion, create `.planning/phases/49-ui-event-wiring-and-artifact-integration/49-05-SUMMARY.md`
</output>
