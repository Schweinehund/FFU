---
phase: 49
slug: ui-event-wiring-and-artifact-integration
status: draft
shadcn_initialized: false
preset: none
created: 2026-03-24
revised: 2026-03-24
---

# Phase 49 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none |
| Preset | not applicable |
| Component library | WPF (System.Windows.Controls) — no third-party component library |
| Icon library | Unicode glyphs inline in XAML (e.g. &#9650; &#9660; for expander arrows) |
| Font | System default (WPF inherits Segoe UI from OS) |

**Note:** This project is a WPF/PowerShell desktop application. shadcn, Tailwind, and web design systems do not apply. All design values are expressed as WPF XAML property values. Source: codebase scan — no `components.json`, `tailwind.config.*`, or `postcss.config.*` found.

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | WPF Margin/Padding syntax | Usage |
|-------|-------|--------------------------|-------|
| xs | 4px | `Margin="4"` or `Margin="0,0,4,0"` | Icon gaps, inline label-to-value gaps |
| sm | 8px | `Margin="8"` or `Margin="0,0,8,0"` | Inter-row spacing within a card |
| md | 16px | `Margin="16"` or `Margin="0,0,16,0"` | Default element spacing between cards |
| lg | 24px | `Margin="0,0,0,24"` | Section breaks between GroupBoxes (new elements only) |
| xl | 32px | — | Layout gaps (not currently used in USB Mode tab) |

**Rule for Phase 49:** This phase introduces ZERO new spacing values outside the 4-point grid. All non-multiple-of-4 values listed below are inherited from existing Phase 48 XAML and MUST NOT be changed. Any new elements added in this phase (Rescan button, Target USB Drive GroupBox) MUST use only grid-aligned values (xs / sm / md / lg).

### Legacy Carry-Forward Values — Read-Only

These values exist in the committed Phase 48 XAML (`BuildFFUVM_UI.xaml`). They are NOT part of the spacing scale for new work. Do not introduce additional non-multiple-of-4 values.

| Value | WPF property | Location | Origin |
|-------|-------------|----------|--------|
| 15px | `Margin="0,0,0,15"` | GroupBox separation in USB Mode tab | Phase 48 XAML (line 944 area) — matches existing pattern throughout file |
| 20px | `Margin="20,0,0,N"` | Artifact card internal indent for status/path/size rows | Phase 48 XAML (lines 960–992) |
| 10px | `Margin="0,10"` | Separator elements between artifact cards in a GroupBox | Phase 48 XAML (lines 944–995) |
| 10px | `Margin="10"` / `Padding="10"` | Window outer grid and TabControl padding | Established by existing layout before Phase 48 |

Source: Direct measurement from `BuildFFUVM_UI.xaml` lines 944–995 (USB Mode tab artifact card layout).

---

## Typography

All font sizes match the existing XAML established in Phases 48 and prior. Do not introduce new sizes.

**Declared scale: 4 sizes.**

| Role | Size | Weight | WPF LineHeight | Usage in Phase 49 |
|------|------|--------|----------------|-------------------|
| Tab section heading | 18px | Bold | default (~1.2) | "USB from Existing Artifacts" header (line 936) |
| GroupBox section label | 16px | Bold | default | "Build USB Drive Settings" pattern — replicate for "Target USB Drive" GroupBox label |
| Artifact card title / new button labels | 13px | Bold (card titles) / Normal (buttons) | default | CheckBox labels (`usbFFUInclude`, etc. — lines 955, 1008, 1055); Rescan button; USB drive GroupBox button, ListBox, CheckBox labels in Phase 49 additions |
| Artifact card metadata | 11px | Normal | default | All status, path, size, age, version, SKU, arch TextBlocks in artifact cards |

14px is inherited by the TabControl via WPF property inheritance from existing Phase 48 XAML (RadioButtons at lines 78–79, TabControl at line 83) — no Phase 49 additions use this size and it is not re-declared.

**Rule for Phase 49 new elements:** New XAML additions (Rescan button `usbRescanArtifacts`, Target USB Drive GroupBox controls `usbCheckUSBDrives` / `usbUSBDriveList` / `usbSelectAllDrives`) MUST use `FontSize="13"`. Do not use 14px for any Phase 49 additions. The 14px size is inherited by the TabControl via property inheritance and must not be re-declared on new controls within the USB Mode tab StackPanel.

Source: `BuildFFUVM_UI.xaml` lines 77–79, 83, 936, 955, 960–992, 1008, 1055.

---

## Color

All color values match existing XAML. No new color values may be introduced in Phase 49.

| Role | Value | WPF property | Usage |
|------|-------|-------------|-------|
| Dominant (60%) | `#FFFFFF` / OS default | Background (system) | Window background, tab content area — WPF default, not explicitly set |
| Secondary (30%) | `#888888` | `Foreground="#888888"` | Artifact card metadata labels (Status, Path, Size, Age, arch labels and values in idle state) |
| Muted text | `#666666` | `Foreground="#666666"` | Tab subtitle text ("Create deployment USB media from pre-built...") |
| Accent (10%) | WPF system accent | default Button/CheckBox style | Browse buttons, CheckBoxes, mode RadioButtons — WPF system chrome only |
| Destructive | not applicable | — | No destructive actions in Phase 49 |

Accent reserved for: Browse buttons (system button chrome), CheckBox tick (system style), RadioButton selection (system style). No custom accent hex value is used — the project relies entirely on OS/WPF system colors for interactive chrome.

**Primary visual anchor — USB Mode tab:** The "Required Artifacts" GroupBox is the focal point of the USB Mode tab. It appears first, contains the two mandatory artifacts (FFU Image and WinPE Deploy ISO), and is the only section where validation failure blocks USB creation. The executor should ensure the Required Artifacts GroupBox renders above the fold without scrolling at 1080p.

**Dynamic foreground states for artifact card status TextBlock (`usbFFUStatus`, etc.):**
- Idle / pre-scan: `Foreground="#888888"` with `FontStyle="Italic"` — text: `(scanning...)` or `(not scanned)`
- Found: `Foreground="Green"` — text: `Found`
- Missing: `Foreground="OrangeRed"` — text: `Missing`
- User-overridden and file verified: `Foreground="Green"` — text: `Found (user path)`
- User-overridden but file not found: `Foreground="OrangeRed"` — text: `Missing (user path)`

Source: Phase 48 XAML establishes `#888888` and `#666666`; `Green`/`OrangeRed` for status are standard WPF named colors consistent with existing hypervisor status at line 134 (`Foreground="Gray"`).

---

## Copywriting Contract

All copy values are prescriptive. Do not paraphrase.

| Element | Copy |
|---------|------|
| Primary CTA — USB Mode idle | `Create USB` |
| Primary CTA — Full Build idle | `Build FFU` |
| Primary CTA — any mode executing | `Cancel` |
| Rescan button label | `Rescan Artifacts` |
| USB Mode tab header | `USB from Existing Artifacts` |
| USB Mode tab subtitle | `Create deployment USB media from pre-built artifacts without running the full build pipeline.` |
| Target USB Drive GroupBox header | `Target USB Drive` |
| Artifact card status — pre-scan | `(not scanned)` |
| Artifact card status — scanning | `(scanning...)` |
| Artifact card status — found (auto) | `Found` |
| Artifact card status — missing | `Missing` |
| Artifact card status — found (user path) | `Found (user path)` |
| Artifact card status — missing (user path) | `Missing (user path)` |
| Empty state — no FFUDevelopmentPath set | `Scan requires FFUDevelopmentPath to be configured.` (shown in a single TextBlock replacing card status; no full empty-state panel) |
| Error dialog — FFU not found/unchecked | `An FFU image is required. Select an FFU file and ensure it is checked.` |
| Error dialog — Deploy ISO not found/unchecked | `The WinPE Deploy ISO is required to create a bootable USB. Select an ISO file and ensure it is checked.` |
| Error dialog — no USB drive selected | `Select at least one USB drive before creating USB media.` |
| Error dialog title (all three) | `Validation Error` |
| Path display — not yet scanned | `(not scanned)` |
| Path display — file missing | `(not found)` |

**"Cancel" label note:** `Cancel` is a single-word label without a noun, matching the existing Full Build behavior where `btnRun` transitions to `Cancel` during execution. This is an intentional carry-forward locked by D-15 (CONTEXT.md). It is not a copywriting gap — both modes share a single button whose cancel behavior is mode-agnostic. No change required.

**Error dialogs:** Displayed via `[System.Windows.MessageBox]::Show($message, 'Validation Error', 'OK', 'Warning')`. No custom WPF dialog window.

Source: D-14, D-15, D-20 from CONTEXT.md; tab header/subtitle from Phase 48 XAML lines 936–937; status copy conventions from Phase 48 XAML initial values at lines 961, 972.

---

## Interaction Contract

This section describes state machine transitions and interaction sequences required for Phase 49. Executor must implement exactly these behaviors.

### Mode Switch — USB Mode activation (D-25)

Trigger: `rbUSBMode.Checked`

Sequence (order-dependent — execute in this order to prevent flicker):
1. Set `Visibility="Collapsed"` on: `tabVMSettings`, `tabWindowsSettings`, `tabUpdates`, `tabApplications`, `tabM365AppsOffice`, `tabDrivers`, `tabBuild`
2. Set `Visibility="Visible"` on `usbModeTab`
3. Set `MainTabControl.SelectedItem = usbModeTab`
4. Call `Invoke-USBArtifactScan -State $localState`
5. Set `btnRun.Content = 'Create USB'`

### Mode Switch — Full Build activation (D-26)

Trigger: `rbFullBuild.Checked`

Sequence:
1. Set `Visibility="Visible"` on all 7 Full Build tabs listed above
2. Set `Visibility="Collapsed"` on `usbModeTab`
3. Set `MainTabControl.SelectedItem = tabVMSettings`
4. Set `btnRun.Content = 'Build FFU'`

### Artifact Scan (D-01 through D-05)

Trigger: Mode switch to USB Mode OR `usbRescanArtifacts` button click.

Pre-condition: `txtFFUDevPath.Text` must be a non-empty path that exists on disk. If not, log warning and return without scanning — do not show an error dialog at scan time.

Scan behavior:
- For each artifact card where `source != 'user'`: set status TextBlock to `(scanning...)` with `Foreground="#888888"` and `FontStyle="Italic"` before calling `Find-FFUArtifacts`
- For each artifact card where `source == 'user'`: leave status and path TextBlocks unchanged; re-verify file existence at end
- Call `Find-FFUArtifacts -FFUDevelopmentPath $ffuDevPath` (synchronous, UI thread — per D-02)
- After scan returns, update each auto-detect card:
  - Found: set status to `Found` with `Foreground="Green"` and `FontStyle="Normal"`; set path TextBlock to full path; set size and age TextBlocks
  - Missing: set status to `Missing` with `Foreground="OrangeRed"` and `FontStyle="Normal"`; set path TextBlock to `(not found)`
- For FFU artifact specifically: also populate `usbFFUVersion`, `usbFFUSKU`, `usbFFUArch` from `ArtifactResult.Metadata`
- After all cards updated: enable Browse buttons (`IsEnabled=$true` for all 7); enable CheckBoxes only for Found artifacts (`IsEnabled=$true`); leave CheckBoxes for Missing artifacts `IsEnabled=$false`

### Browse Override (D-06 through D-09)

Trigger: `usbFFUBrowse` (or any of the 7 Browse buttons) click.

Behavior:
1. Call `Invoke-BrowseAction` with appropriate `Type` and `Filter` per artifact type (see table below)
2. If user selects a path: update `$State.Data.usbArtifactState[type].path = $selectedPath` and `source = 'user'`
3. Update path TextBlock with selected path
4. Set status to `Found (user path)` with `Foreground="Green"` and `FontStyle="Normal"`
5. Enable the artifact's CheckBox
6. For FFU artifact: re-run `Get-ArtifactMetadata` on the selected file and update version/SKU/arch TextBlocks

Browse dialog parameters per artifact type:

| Artifact | Button | Type | Filter |
|----------|--------|------|--------|
| FFU Image | `usbFFUBrowse` | `OpenFile` | `FFU files (*.ffu)|*.ffu` |
| Deploy ISO | `usbDeployISOBrowse` | `OpenFile` | `ISO files (*.iso)|*.iso` |
| Drivers | `usbDriversBrowse` | `Folder` | n/a |
| PPKG | `usbPPKGBrowse` | `OpenFile` | `Provisioning packages (*.ppkg)|*.ppkg` |
| Unattend | `usbUnattendBrowse` | `OpenFile` | `XML files (*.xml)|*.xml` |
| Autopilot | `usbAutopilotBrowse` | `OpenFile` | `JSON files (*.json)|*.json` |
| Apps ISO | `usbAppsISOBrowse` | `OpenFile` | `ISO files (*.iso)|*.iso` |

### Rescan (D-04, D-07)

Trigger: `usbRescanArtifacts` button click.

Behavior: Re-runs `Invoke-USBArtifactScan`. For artifacts with `source='user'`: only re-verify file existence (update status to `Found (user path)` or `Missing (user path)`). Does not clear user-selected paths.

### Create USB Pre-Launch Validation (D-19 through D-20)

Trigger: `btnRun` click while `rbUSBMode.IsChecked = $true`.

Validation gates (in order — stop at first failure, show dialog):
1. `usbFFUStatus` must be `Found` or `Found (user path)` AND `usbFFUInclude.IsChecked = $true` → else show "An FFU image is required…" dialog
2. `usbDeployISOStatus` must be `Found` or `Found (user path)` AND `usbDeployISOInclude.IsChecked = $true` → else show "The WinPE Deploy ISO is required…" dialog
3. At least one item selected in `usbUSBDriveList` → else show "Select at least one USB drive…" dialog

If all gates pass: launch `BuildFFUVM.ps1 -USBOnlyMode` via `Start-ThreadJob` using same pattern as Full Build. Set `btnRun.Content = 'Cancel'`.

### Reset to Idle (D-17)

`Reset-FFUUIToIdle` must check `$State.Controls.rbUSBMode.IsChecked`:
- `$true`: set `btnRun.Content = 'Create USB'`
- `$false` (or null): set `btnRun.Content = 'Build FFU'`

### Config Persistence (D-22 through D-24)

On save (`Build-UIConfiguration`):
- Write `ActiveMode`: `'USBMode'` if `rbUSBMode.IsChecked`, else `'FullBuild'`
- For each artifact where `$State.Data.usbArtifactState[type].source == 'user'`: write path to `USBMode.Artifacts.{Type}.Path`

On load (`Update-UIFromConfig`):
- Read `ActiveMode`: set `rbUSBMode.IsChecked = $true` or `rbFullBuild.IsChecked = $true`; fire the Checked handler (triggers scan if USB Mode)
- For each `USBMode.Artifacts.{Type}.Path` that is non-empty: set `$State.Data.usbArtifactState[type].path` and `source = 'user'`; update path TextBlock; scan will verify existence

---

## XAML Additions Required

Phase 49 requires two XAML changes to `BuildFFUVM_UI.xaml` (USB Mode tab). These are specified here so the executor does not need to infer them.

### Addition 1: Rescan button (D-04)

Location: After the closing `</TextBlock>` for the subtitle (line 937), before the "Required Artifacts" `<GroupBox>`.

```xml
<!-- Rescan button -->
<Button x:Name="usbRescanArtifacts" Content="Rescan Artifacts" HorizontalAlignment="Left"
        Width="140" Margin="0,0,0,16" FontSize="13"/>
```

Note: `Margin="0,0,0,16"` uses the grid-aligned md (16px) value. Do not use 15px here — that is a legacy carry-forward for existing GroupBox separators only.

### Addition 2: Target USB Drive GroupBox (D-10, D-13)

Location: After the closing `</GroupBox>` of "Optional Artifacts", at the bottom of the USB Mode `<StackPanel>`.

Pattern to replicate: Build tab USB drive section (lines 851–923), with control names prefixed `usb` instead of the build tab names.

```xml
<!-- Target USB Drive GroupBox -->
<GroupBox Header="Target USB Drive" Margin="0,0,0,16" Padding="8">
    <StackPanel>
        <Button x:Name="usbCheckUSBDrives" Content="Check for USB Drives"
                HorizontalAlignment="Left" Width="160" Margin="0,0,0,8" FontSize="13"/>
        <ListBox x:Name="usbUSBDriveList" Height="80" Margin="0,0,0,8" FontSize="13"
                 SelectionMode="Multiple"/>
        <CheckBox x:Name="usbSelectAllDrives" Content="Select All Drives"
                  Margin="0,0,0,4" FontSize="13"/>
    </StackPanel>
</GroupBox>
```

Exact pixel widths and Heights must match the Build tab equivalents. If the Build tab values differ from the above, use the Build tab values.

---

## Per-Artifact State Tracking (Claude's Discretion)

Internal state for each artifact is tracked in `$State.Data.usbArtifactState` as a hashtable of PSCustomObjects:

```powershell
$State.Data.usbArtifactState = @{
    FFU        = [PSCustomObject]@{ source = 'auto'; path = $null }
    DeployISO  = [PSCustomObject]@{ source = 'auto'; path = $null }
    Drivers    = [PSCustomObject]@{ source = 'auto'; path = $null }
    PPKG       = [PSCustomObject]@{ source = 'auto'; path = $null }
    Unattend   = [PSCustomObject]@{ source = 'auto'; path = $null }
    Autopilot  = [PSCustomObject]@{ source = 'auto'; path = $null }
    AppsISO    = [PSCustomObject]@{ source = 'auto'; path = $null }
}
```

Initialized in the state setup block before `Register-EventHandlers` is called. Keys match the `ArtifactType` enum values from `FFU.ArtifactScanner.Classes.ps1`.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — WPF project |
| third-party | none | not applicable |

No registry safety gate applies. This is a WPF/PowerShell desktop application with no npm, no shadcn, and no third-party component registries.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
