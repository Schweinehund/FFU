---
phase: 43-deployment-improvements-and-nice-to-haves
plan: 03
type: execute
wave: 2
depends_on: ["43-01", "43-02"]
files_modified:
  - Tests/Unit/ApplyFFU.Deployment.Tests.ps1
  - Tests/Unit/Orchestrator.SecurityPlatformDelay.Tests.ps1
autonomous: true

must_haves:
  truths:
    - "Pester tests verify Get-HardDrive multi-disk selection menu logic via AST extraction"
    - "Pester tests verify Get-USBDrive BusType-based detection with UniqueId logging via AST"
    - "Pester tests verify empty driver folder detection skips injection for folders without .inf files"
    - "Pester tests verify skip-driver Y/N prompt structure exists in ApplyFFU.ps1"
    - "Pester tests verify Orchestrator.ps1 contains Security Platform delay block"
    - "All tests pass with Invoke-Pester"
  artifacts:
    - path: "Tests/Unit/ApplyFFU.Deployment.Tests.ps1"
      provides: "Pester tests for DEPLOY-01, DEPLOY-03, NICE-01, NICE-02"
      contains: "Get-HardDrive"
    - path: "Tests/Unit/Orchestrator.SecurityPlatformDelay.Tests.ps1"
      provides: "Pester tests for DEPLOY-02 Security Platform delay"
      contains: "Security Platform"
  key_links:
    - from: "ApplyFFU.Deployment.Tests.ps1"
      to: "ApplyFFU.ps1"
      via: "AST function extraction for testable function definitions"
      pattern: "FunctionDefinitionAst"
    - from: "Orchestrator.SecurityPlatformDelay.Tests.ps1"
      to: "Orchestrator.ps1"
      via: "Content-based validation of delay block structure"
      pattern: "securityPlatformDelay"
---

<objective>
Create Pester test suites covering all five Phase 43 requirements: multi-disk selection (DEPLOY-01), empty driver folders (DEPLOY-03), Security Platform delay (DEPLOY-02), USB UniqueId (NICE-01), and skip-driver option (NICE-02).

Purpose: Ensure all deployment improvements are correctly implemented and guard against regressions.
Output: Two test files covering ApplyFFU.ps1 enhancements and Orchestrator.ps1 delay.
</objective>

<execution_context>
@C:\Users\joanderson\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\joanderson\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-deployment-improvements-and-nice-to-haves/43-RESEARCH.md
@.planning/phases/43-deployment-improvements-and-nice-to-haves/43-01-SUMMARY.md
@.planning/phases/43-deployment-improvements-and-nice-to-haves/43-02-SUMMARY.md
@FFUDevelopment/WinPEDeployFFUFiles/ApplyFFU.ps1
@FFUDevelopment/Apps/Orchestration/Orchestrator.ps1
@Tests/Unit/ApplyFFU.DriverMatching.Tests.ps1
@Tests/Unit/Orchestrator.DependencyDetection.Tests.ps1
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ApplyFFU.Deployment.Tests.ps1 for DEPLOY-01, DEPLOY-03, NICE-01, NICE-02</name>
  <files>Tests/Unit/ApplyFFU.Deployment.Tests.ps1</files>
  <action>
    Create `Tests/Unit/ApplyFFU.Deployment.Tests.ps1` following the established AST-based testing pattern from `ApplyFFU.DriverMatching.Tests.ps1`.

    **File structure:**
    ```
    #Requires -Version 5.1
    #Requires -Modules @{ ModuleName = 'Pester'; ModuleVersion = '5.0.0' }
    ```

    **BeforeAll block:**
    - Parse ApplyFFU.ps1 via AST: `[System.Management.Automation.Language.Parser]::ParseFile($scriptPath, [ref]$null, [ref]$null)`
    - Extract function definitions: `$ast.FindAll({ param($node) $node -is [FunctionDefinitionAst] }, $true)`
    - Define `Get-HardDrive` and `Get-USBDrive` via `Invoke-Expression $func.Extent.Text` for functions that can be tested in isolation
    - Store full AST and raw content for structural verification tests

    **Test Describe blocks (aim for ~30-40 tests total):**

    **1. Describe 'DEPLOY-01: Multi-Disk Selection' -Tag 'Unit', 'Deployment', 'DEPLOY-01'**
    - Context 'Get-HardDrive Structure':
      - It 'Should wrap physical disk query in @() for array coercion' - AST check for `@(Get-CimInstance` pattern
      - It 'Should check diskDrives.Count -gt 1 for multi-disk detection' - AST/content check
      - It 'Should display Format-Table with Number, Model, SizeGB, Index columns' - content match for these column names
      - It 'Should use Read-Host for disk selection' - AST check for `Read-Host.*disk number`
      - It 'Should have do/until validation loop for input' - structural check matching FFU file pattern
      - It 'Should log selected disk' - content check for `WriteLog.*selected disk` or similar
    - Context 'VM Detection Unchanged':
      - It 'Should check for Microsoft Corporation manufacturer' - content match
      - It 'Should check for Virtual Machine model' - content match
      - It 'Should filter for Index -eq 0 and SCSILogicalUnit -eq 0' - content match
      - It 'Should not present selection menu for VM disks' - verify VM branch does not contain Format-Table
    - Context 'Return Type':
      - It 'Should return PSCustomObject with DeviceID property' - AST check for return block
      - It 'Should return PSCustomObject with BytesPerSector property' - AST check
      - It 'Should return PSCustomObject with DiskSize property' - AST check

    **2. Describe 'NICE-01: USB UniqueId Detection' -Tag 'Unit', 'Deployment', 'NICE-01'**
    - Context 'Get-USBDrive Structure':
      - It 'Should use Get-Disk with BusType filter as primary detection' - content match for `BusType.*USB`
      - It 'Should log UniqueId for audit trail' - content match for `UniqueId`
      - It 'Should have fallback to volume-based Removable detection' - content match for `DriveType.*Removable` or similar
      - It 'Should have fallback to Deploy-labeled fixed volume' - content match for `FileSystemLabel.*Deploy`
      - It 'Should call Stop-Script on complete failure' - content match for `Stop-Script`
      - It 'Should return drive letter with trailing backslash' - AST check for return pattern with `:\`

    **3. Describe 'DEPLOY-03: Empty Driver Folder Detection' -Tag 'Unit', 'Deployment', 'DEPLOY-03'**
    - Context 'Empty Folder Check':
      - It 'Should check for .inf files recursively' - content match for `Get-ChildItem.*-Recurse.*-Include.*\.inf` or `-Include \*\.inf`
      - It 'Should only check Folder type, not WIM' - content match for `DriverSourceType -eq.*Folder` near the .inf check
      - It 'Should set DriverSourcePath to null when empty' - content match for `\$DriverSourcePath = \$null` after inf check
      - It 'Should log descriptive skip message for empty folders' - content match for `contains no \.inf files`
      - It 'Should log .inf file count when drivers found' - content match for `driver \.inf file\(s\)`

    **4. Describe 'NICE-02: Skip Driver Installation' -Tag 'Unit', 'Deployment', 'NICE-02'**
    - Context 'Skip Driver Prompt':
      - It 'Should initialize $skipDrivers variable to $false' - content match
      - It 'Should prompt with Read-Host for Y/N input' - content match for `Read-Host.*Install drivers.*Y/N` or similar
      - It 'Should accept Y to continue with drivers' - content match for `\^[Yy]` regex
      - It 'Should accept N to skip drivers' - content match for `\^[Nn]` regex
      - It 'Should have do/until validation loop' - structural check
      - It 'Should log when user elects to skip' - content match for `elected to skip`
      - It 'Should set DriverSourcePath to null when skipping' - content/AST check

    **Testing approach notes:**
    - ApplyFFU.ps1 is a WinPE deployment script - it cannot be executed in a test environment (requires bare metal, DISM, diskpart, etc.)
    - Use AST analysis to verify function structure without execution (same pattern as existing tests)
    - Use raw content matching for inline code changes (empty folder check, skip-drivers prompt) that are not inside extractable functions
    - Mock WriteLog function for any extracted function tests that need to be invoked
  </action>
  <verify>
    Run: `Invoke-Pester -Path Tests/Unit/ApplyFFU.Deployment.Tests.ps1 -Output Detailed`
    All tests should pass. Expect approximately 30-40 tests across 4 Describe blocks.
  </verify>
  <done>
    - ApplyFFU.Deployment.Tests.ps1 exists with tests for all 4 ApplyFFU.ps1 requirements
    - All tests pass via Invoke-Pester
    - Tests use established AST extraction pattern from ApplyFFU.DriverMatching.Tests.ps1
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Orchestrator.SecurityPlatformDelay.Tests.ps1 for DEPLOY-02</name>
  <files>Tests/Unit/Orchestrator.SecurityPlatformDelay.Tests.ps1</files>
  <action>
    Create `Tests/Unit/Orchestrator.SecurityPlatformDelay.Tests.ps1` following the established content-based testing pattern from `Orchestrator.DependencyDetection.Tests.ps1`.

    **File structure:**
    ```
    #Requires -Version 5.1
    #Requires -Modules @{ ModuleName = 'Pester'; ModuleVersion = '5.0.0' }
    ```

    **BeforeAll block:**
    - Resolve path to Orchestrator.ps1: `Join-Path $script:ProjectRoot 'FFUDevelopment\Apps\Orchestration\Orchestrator.ps1'`
    - Verify file exists
    - Load raw content for pattern matching: `$script:OrchestratorContent = Get-Content -Path $script:OrchestratorPath -Raw`

    **Test Describe blocks (aim for ~10-15 tests):**

    **1. Describe 'DEPLOY-02: Security Platform Delay' -Tag 'Unit', 'Orchestrator', 'DEPLOY-02'**
    - Context 'Delay Block Structure':
      - It 'Should define securityPlatformDelay variable set to 30' - content match for `\$securityPlatformDelay\s*=\s*30`
      - It 'Should use Start-Sleep for delay' - content match for `Start-Sleep`
      - It 'Should have countdown loop' - content match for `for.*\$i.*securityPlatformDelay.*\$i -gt 0`
      - It 'Should display countdown with Write-Host -NoNewline' - content match for `Write-Host.*NoNewline`
      - It 'Should indicate completion after delay' - content match for `delay complete` or `complete`
    - Context 'Logging':
      - It 'Should log delay start with Write-OrchestratorLog' - content match for `Write-OrchestratorLog.*Security Platform`
      - It 'Should log delay completion' - content match for `Write-OrchestratorLog.*complete`
      - It 'Should include explanation of why delay exists' - content match for `Security Platform.*initializ`
    - Context 'Placement':
      - It 'Should appear after integrity verification block' - verify delay section appears after `SECURITY:.*verified` pattern
      - It 'Should appear before script list definition' - verify delay section appears before `\$scriptList\s*=\s*@\(`
      - It 'Should not modify script execution loop' - content match for `foreach.*\$script.*\$scriptList` still present
    - Context 'Comment Documentation':
      - It 'Should have DEPLOY-02 reference in comments' - content match for `DEPLOY-02`
      - It 'Should explain Security Platform in comments' - content match for comment containing `Security Platform`

    **Testing approach notes:**
    - Orchestrator.ps1 runs in audit mode inside a VM - cannot be executed in test environment
    - Use content-based validation (same pattern as Orchestrator.DependencyDetection.Tests.ps1)
    - Verify structural correctness: block placement, variable definitions, logging calls
  </action>
  <verify>
    Run: `Invoke-Pester -Path Tests/Unit/Orchestrator.SecurityPlatformDelay.Tests.ps1 -Output Detailed`
    All tests should pass. Expect approximately 10-15 tests.
  </verify>
  <done>
    - Orchestrator.SecurityPlatformDelay.Tests.ps1 exists with tests for DEPLOY-02
    - All tests pass via Invoke-Pester
    - Tests use established content-based validation pattern from Orchestrator.DependencyDetection.Tests.ps1
  </done>
</task>

</tasks>

<verification>
After both test files are created:

1. Run all Phase 43 tests:
   ```powershell
   Invoke-Pester -Path Tests/Unit/ApplyFFU.Deployment.Tests.ps1, Tests/Unit/Orchestrator.SecurityPlatformDelay.Tests.ps1 -Output Detailed
   ```
2. Verify all tests pass (0 failures)
3. Verify existing tests are not broken:
   ```powershell
   Invoke-Pester -Path Tests/Unit/ApplyFFU.DriverMatching.Tests.ps1, Tests/Unit/Orchestrator.DependencyDetection.Tests.ps1 -Output Detailed
   ```
4. Check test count: expect approximately 40-55 total tests across both new files
</verification>

<success_criteria>
- Two new test files created covering all 5 requirements (DEPLOY-01, DEPLOY-02, DEPLOY-03, NICE-01, NICE-02)
- All new tests pass with Invoke-Pester
- No existing tests are broken
- Tests follow established patterns (AST extraction for ApplyFFU.ps1, content matching for Orchestrator.ps1)
- Test tags allow selective execution by requirement ID
</success_criteria>

<output>
After completion, create `.planning/phases/43-deployment-improvements-and-nice-to-haves/43-03-SUMMARY.md`
</output>
