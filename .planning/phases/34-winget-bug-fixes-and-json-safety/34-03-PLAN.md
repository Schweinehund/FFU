---
phase: 34-winget-bug-fixes-and-json-safety
plan: 03
type: execute
wave: 3
depends_on: ["34-01", "34-02"]
files_modified:
  - Tests/Unit/FFU.Common.Winget.Tests.ps1
autonomous: true

must_haves:
  truths:
    - "Pester tests verify mutex-protected JSON writes prevent corruption under parallel writes"
    - "Pester tests verify MSI and EXE paths with spaces are correctly quoted in WinGetWin32Apps.json"
    - "Pester tests verify duplicate app detection inside the mutex lock"
    - "All tests pass with Invoke-Pester"
  artifacts:
    - path: "Tests/Unit/FFU.Common.Winget.Tests.ps1"
      provides: "Comprehensive Pester 5 tests for BUGFIX-01 and BUGFIX-03"
      min_lines: 100
  key_links:
    - from: "Tests/Unit/FFU.Common.Winget.Tests.ps1"
      to: "FFUDevelopment/FFU.Common/FFU.Common.Winget.psm1"
      via: "Import-Module and function calls"
      pattern: "Import-Module.*FFU.Common.Winget"
---

<objective>
Create comprehensive Pester 5 tests verifying both BUGFIX-01 (mutex-protected JSON writes) and BUGFIX-03 (path quoting for installers with spaces).

Purpose: Validate the mutex and path quoting fixes from Plans 01 and 02 are working correctly. Tests should cover single writes, duplicate detection, the override section's mutex usage, EXE path quoting, MSI path quoting, and JSON round-trip integrity. Concurrent write testing requires careful setup since PowerShell 5.1 doesn't have ForEach-Object -Parallel (PS7+ only).

Output: Tests/Unit/FFU.Common.Winget.Tests.ps1 with all tests passing.
</objective>

<execution_context>
@C:\Users\joanderson\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\joanderson\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-winget-bug-fixes-and-json-safety/34-01-SUMMARY.md
@.planning/phases/34-winget-bug-fixes-and-json-safety/34-02-SUMMARY.md

Key source files:
@FFUDevelopment/FFU.Common/FFU.Common.Winget.psm1 (modified by plans 01 and 02)
@Tests/Unit/_Template.Tests.ps1 (test structure template)
@Tests/Unit/FFU.Drivers.Tests.ps1 (example of mocking WriteLog and module imports)
@FFUDevelopment/Apps/Orchestration/Install-Win32Apps.ps1 (execution context for path quoting)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Pester tests for mutex-protected JSON writes (BUGFIX-01)</name>
  <files>Tests/Unit/FFU.Common.Winget.Tests.ps1</files>
  <action>
Create a new Pester 5 test file at Tests/Unit/FFU.Common.Winget.Tests.ps1 following the project template pattern.

**BeforeAll setup:**
```powershell
$TestRoot = Split-Path $PSScriptRoot -Parent
$ProjectRoot = Split-Path $TestRoot -Parent
$ModulePath = Join-Path $ProjectRoot 'FFUDevelopment\FFU.Common'
$ModuleFile = Join-Path $ModulePath 'FFU.Common.Winget.psm1'
```

Note: FFU.Common.Winget.psm1 is NOT a manifest module — it's a standalone .psm1 file in FFU.Common/. Import it directly. Mock `WriteLog` since it's defined externally.

**Test categories to create:**

1. **Describe 'Add-Win32SilentInstallCommand - Mutex JSON Safety'**
   - `It 'Should create WinGetWin32Apps.json with single app entry'` — Call with a mock app folder containing an .exe and .yaml, verify JSON is valid and contains the app
   - `It 'Should prevent duplicate entries when called twice with same app'` — Call twice with same AppFolder, verify only one entry exists
   - `It 'Should assign sequential priorities'` — Call with two different apps, verify Priority values are 1 and 2
   - `It 'Should use named mutex WinGetWin32AppsJsonLock'` — Verify by checking the source code contains the mutex name (grep the file)

2. **Describe 'Add-Win32SilentInstallCommand - Concurrent Writes'**
   - `It 'Should handle sequential writes without JSON corruption'` — Write 5 apps sequentially, verify JSON is valid with 5 entries
   - Note: True parallel testing requires Start-Job which is complex in Pester context. Use sequential stress testing as a pragmatic alternative and document why. If running PowerShell 7+, optionally use ForEach-Object -Parallel.

**Mock requirements:**
- Mock `WriteLog` as a no-op: `function WriteLog { param([string]$Message) }` in BeforeAll
- Create test app folders with .exe/.msi files and minimal .yaml in $TestDrive
- Each test should create its own OrchestrationPath in $TestDrive to avoid cross-test interference

**YAML format for test fixtures:**
```yaml
PackageIdentifier: Test.App
PackageVersion: 1.0.0
Installers:
  - Architecture: x64
    InstallerType: exe
    NestedInstallerFiles:
      - RelativeFilePath: setup.exe
    InstallerSwitches:
      Silent: /S
```

Simplification: The function reads YAML with regex, not a YAML parser. The key regex patterns look for `RelativeFilePath:` and `Silent:` lines. Create minimal YAML that satisfies these patterns.
  </action>
  <verify>
Run: `Invoke-Pester -Path Tests/Unit/FFU.Common.Winget.Tests.ps1 -Output Detailed`
All mutex/JSON safety tests should pass.
  </verify>
  <done>Pester tests verify mutex-protected JSON writes: single app creation, duplicate prevention, sequential priority assignment, and named mutex usage</done>
</task>

<task type="auto">
  <name>Task 2: Create Pester tests for path quoting (BUGFIX-03)</name>
  <files>Tests/Unit/FFU.Common.Winget.Tests.ps1</files>
  <action>
Add test categories to the existing test file for BUGFIX-03 path quoting.

**Test categories to add:**

3. **Describe 'Add-Win32SilentInstallCommand - EXE Path Quoting'**
   - `It 'Should quote EXE CommandLine path containing spaces'` — Create app folder with space in name (e.g., "Test App With Spaces"), add .exe and .yaml, call function, verify CommandLine in JSON contains quotes around the path
   - `It 'Should handle EXE paths without spaces correctly'` — Create app folder without spaces, verify CommandLine is still properly quoted (no harm from defensive quoting)

4. **Describe 'Add-Win32SilentInstallCommand - MSI Path Quoting'**
   - `It 'Should quote MSI path in Arguments field with spaces'` — Create app folder with space, add .msi and .yaml, call function, verify Arguments contains `/i "...path..."` with embedded quotes
   - `It 'Should produce valid msiexec arguments'` — Verify Arguments field starts with `/i "` and contains the MSI path
   - `It 'Should not have trailing whitespace in Arguments when silent switch is empty'` — Create .yaml with no Silent line, verify Arguments does not end with whitespace

5. **Describe 'Add-Win32SilentInstallCommand - JSON Round-Trip Integrity'**
   - `It 'Should survive JSON serialization and deserialization with quoted paths'` — Write an app with spaces in path, read JSON back with ConvertFrom-Json, verify CommandLine and Arguments values preserve their quotes correctly
   - `It 'Should produce valid JSON file'` — After writing multiple apps (mix of EXE and MSI), verify Get-Content | ConvertFrom-Json does not throw

**Mock app folder setup for space tests:**
```powershell
$appFolderWithSpaces = Join-Path $TestDrive 'Test App With Spaces'
New-Item -Path $appFolderWithSpaces -ItemType Directory -Force
# Create mock .exe or .msi
New-Item -Path (Join-Path $appFolderWithSpaces 'setup.exe') -ItemType File -Force
# Create minimal YAML
$yamlContent = @"
Installers:
  - Architecture: x64
    InstallerType: exe
    NestedInstallerFiles:
      - RelativeFilePath: setup.exe
    InstallerSwitches:
      Silent: /S
"@
Set-Content -Path (Join-Path $appFolderWithSpaces 'manifest.yaml') -Value $yamlContent
```

Note: The function expects to find .yaml and .exe/.msi in AppFolderPath. The path building uses `$basePath = "D:\win32\$AppFolder"` — this is hardcoded for the VM context. Our tests verify the JSON OUTPUT structure and quoting, not actual execution on D:\ drive. The AppFolderPath parameter provides the local path to read files from, while the JSON stores D:\win32\ paths.
  </action>
  <verify>
Run: `Invoke-Pester -Path Tests/Unit/FFU.Common.Winget.Tests.ps1 -Output Detailed`
All path quoting tests should pass.
  </verify>
  <done>Pester tests verify EXE and MSI path quoting with spaces, JSON round-trip integrity, and no trailing whitespace in MSI arguments</done>
</task>

<task type="auto">
  <name>Task 3: Run full test suite and verify no regressions</name>
  <files>Tests/Unit/FFU.Common.Winget.Tests.ps1</files>
  <action>
Run the complete test file and verify all tests pass. Also run PSScriptAnalyzer on the test file.

1. Run: `Invoke-Pester -Path Tests/Unit/FFU.Common.Winget.Tests.ps1 -Output Detailed`
   - All tests must pass
   - If any fail, diagnose and fix the test or the source code

2. Run: `Invoke-ScriptAnalyzer -Path Tests/Unit/FFU.Common.Winget.Tests.ps1`
   - No errors or warnings should be reported

3. Also verify the source module still imports cleanly:
   `Import-Module FFUDevelopment/FFU.Common/FFU.Common.Winget.psm1 -Force`

If any tests fail:
- Check if the function's YAML parsing regex matches the test YAML format
- Check if the function expects specific file structure in AppFolderPath
- Adjust test fixtures to match what the function actually reads
- Do NOT change the source code fixes from Plans 01/02 to make tests pass — fix the test setup instead
  </action>
  <verify>
1. All Pester tests pass (100% pass rate)
2. PSScriptAnalyzer reports no issues on test file
3. Module imports cleanly
  </verify>
  <done>All FFU.Common.Winget.Tests.ps1 tests pass, PSScriptAnalyzer clean, module imports successfully</done>
</task>

</tasks>

<verification>
1. `Invoke-Pester -Path Tests/Unit/FFU.Common.Winget.Tests.ps1 -Output Detailed` — all tests pass
2. Test file follows project template structure (BeforeAll, Describe blocks, It blocks)
3. Tests cover both BUGFIX-01 (mutex) and BUGFIX-03 (path quoting)
4. Tests use $TestDrive for isolation (no side effects)
5. WriteLog mocked to avoid external dependencies
6. `Invoke-ScriptAnalyzer -Path Tests/Unit/FFU.Common.Winget.Tests.ps1` — no issues
</verification>

<success_criteria>
- Tests/Unit/FFU.Common.Winget.Tests.ps1 exists with 10+ test cases
- All tests pass on `Invoke-Pester`
- Tests cover: single write, duplicate prevention, sequential priorities, EXE quoting, MSI quoting, JSON round-trip, no trailing whitespace
- No PSScriptAnalyzer errors
- Tests are isolated (use $TestDrive, mock WriteLog)
</success_criteria>

<output>
After completion, create `.planning/phases/34-winget-bug-fixes-and-json-safety/34-03-SUMMARY.md`
</output>
