---
phase: 36-cu-skip-esd-bits
plan: 03
type: tdd
wave: 2
depends_on: ["36-01", "36-02"]
files_modified:
  - Tests/Unit/FFU.Updates.CUSkip.Tests.ps1
  - Tests/Unit/FFU.Common.BitsPriority.Tests.ps1
autonomous: true

must_haves:
  truths:
    - "Pester tests verify CU skip version comparison logic for equal, newer, and older ESD versions"
    - "Pester tests verify version parse failure falls back to safe default (download CU)"
    - "Pester tests verify BITS priority cascade (parameter > env > script > default)"
    - "Pester tests verify Set-BitsTransferPriority sets both script var and env var"
  artifacts:
    - path: "Tests/Unit/FFU.Updates.CUSkip.Tests.ps1"
      provides: "CU skip version comparison tests"
      min_lines: 80
    - path: "Tests/Unit/FFU.Common.BitsPriority.Tests.ps1"
      provides: "BITS priority configuration tests"
      min_lines: 60
  key_links:
    - from: "Tests/Unit/FFU.Updates.CUSkip.Tests.ps1"
      to: "FFUDevelopment/Modules/FFU.Updates/FFU.Updates.psm1"
      via: "Tests mock and verify Get-WindowsESDMetadata behavior"
      pattern: "Get-WindowsESDMetadata"
    - from: "Tests/Unit/FFU.Common.BitsPriority.Tests.ps1"
      to: "FFUDevelopment/FFU.Common/FFU.Common.Core.psm1"
      via: "Tests verify Set-BitsTransferPriority and priority cascade"
      pattern: "Set-BitsTransferPriority"
---

<objective>
Create Pester tests for CU skip version comparison logic and BITS priority configuration.

Purpose: Verify version comparison edge cases (equal, newer, older, parse failures, missing versions) and BITS priority cascade behavior to prevent regressions.

Output: Two test files covering CU skip logic and BITS priority configuration.
</objective>

<execution_context>
@C:\Users\joanderson\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\joanderson\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-cu-skip-esd-bits/36-RESEARCH.md
@.planning/phases/36-cu-skip-esd-bits/36-01-SUMMARY.md
@.planning/phases/36-cu-skip-esd-bits/36-02-SUMMARY.md
@FFUDevelopment/Modules/FFU.Updates/FFU.Updates.psm1
@FFUDevelopment/FFU.Common/FFU.Common.Core.psm1
@Tests/Unit/FFU.Updates.Tests.ps1
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CU skip version comparison Pester tests</name>
  <files>
    Tests/Unit/FFU.Updates.CUSkip.Tests.ps1
  </files>
  <action>
    Create `Tests/Unit/FFU.Updates.CUSkip.Tests.ps1` with Pester 5.x tests covering:

    **Describe "Get-WindowsESDMetadata":**
    - Mock the HTTP/cab download and XML parsing to test metadata extraction
    - Test: Returns PSCustomObject with FileUrl, FileName, LocalPath, Version properties
    - Test: Parses 4-part version from ESD filename (e.g., "10.0.26100.1742_amd64_en-us_consumer.esd" -> "10.0.26100.1742")
    - Test: Returns $null Version when filename has no parseable version
    - Test: Returns $null on exception (does not throw)
    - Test: Only resolves for valid WindowsRelease values (10, 11)

    **Describe "Get-KBLink Version Extraction":**
    - Mock Invoke-WebRequest to return controlled HTML content
    - Test: Extracts KB ID and version from catalog HTML containing `(KB5046613) (10.0.26100.2454)`
    - Test: Extracts KB ID without version when format is `(KB5046613)` only
    - Test: Sets $global:LastKBWindowsVersion with extracted version
    - Test: Sets $global:LastKBWindowsVersion to $null when no version found
    - Test: Does not extract KB for Defender/Edge updates
    - Test: KBWindowsVersion property included in return object

    **Describe "CU Version Comparison Logic":**
    These tests should test the version comparison logic directly (not end-to-end through BuildFFUVM.ps1):

    - Test: ESD version equals CU version -> skip (equal versions should skip)
      - Input: esdVersion="10.0.26100.2454", cuVersion="10.0.26100.2454"
      - Expected: versions equal, skip is triggered
    - Test: ESD version newer than CU version -> skip
      - Input: esdVersion="10.0.26100.2894", cuVersion="10.0.26100.2454"
      - Expected: ESD newer, skip is triggered
    - Test: CU version newer than ESD version -> download
      - Input: esdVersion="10.0.26100.1742", cuVersion="10.0.26100.2454"
      - Expected: CU newer, download should proceed
    - Test: ESD version unparseable -> safe default (download CU)
      - Input: esdVersion="invalid-version", cuVersion="10.0.26100.2454"
      - Expected: parse fails, download CU anyway
    - Test: CU version unparseable -> safe default (download CU)
      - Input: esdVersion="10.0.26100.2454", cuVersion="invalid"
      - Expected: parse fails, download CU anyway
    - Test: Both versions null -> no comparison, no skip
    - Test: Only ESD version available (no CU found) -> no comparison

    **Test pattern:** Use mock functions and script blocks to isolate version comparison logic. For WriteLog, mock it with `function WriteLog { param($msg) }`. Follow existing test patterns from `Tests/Unit/FFU.Updates.Tests.ps1`.

    **Important:** Use `BeforeAll` for module setup and `BeforeEach`/`AfterEach` for per-test state cleanup. Clean up `$global:LastKBWindowsVersion` in AfterEach blocks.
  </action>
  <verify>
    - File exists: `Test-Path Tests/Unit/FFU.Updates.CUSkip.Tests.ps1`
    - Pester tests pass: `Invoke-Pester -Path Tests/Unit/FFU.Updates.CUSkip.Tests.ps1 -Output Detailed`
    - At least 10 test cases covering version comparison edge cases
  </verify>
  <done>
    CU skip Pester tests cover equal/newer/older version comparisons, parse failures with safe defaults, missing versions, KB version extraction, and ESD metadata resolution. All tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create BITS priority configuration Pester tests</name>
  <files>
    Tests/Unit/FFU.Common.BitsPriority.Tests.ps1
  </files>
  <action>
    Create `Tests/Unit/FFU.Common.BitsPriority.Tests.ps1` with Pester 5.x tests covering:

    **Describe "Set-BitsTransferPriority":**
    - Test: Sets script-level variable to specified priority
    - Test: Sets FFU_BITS_PRIORITY environment variable
    - Test: Accepts all valid values: Foreground, High, Normal, Low
    - Test: Rejects invalid values (ValidateSet enforcement)
    - Test: Logs priority change via WriteLog

    **Describe "BITS Priority Resolution Cascade":**
    Test the priority resolution logic in Start-BitsTransferWithRetry. Since the actual BITS transfer requires network access, mock Start-BitsTransfer and verify the resolved priority.

    - Test: Explicit -Priority parameter takes highest precedence
      - Set env var to 'Low', call with -Priority 'Foreground' -> uses Foreground
    - Test: FFU_BITS_PRIORITY env var used when no parameter specified
      - Set env var to 'High', call without -Priority -> uses High
    - Test: Script variable used when no parameter or env var
      - Clear env var, set script var to 'Low', call without -Priority -> uses Low
    - Test: Default 'Normal' when nothing configured
      - Clear env var and script var -> uses Normal

    **Describe "BITS Priority Environment Propagation":**
    - Test: Set-BitsTransferPriority persists value in env:FFU_BITS_PRIORITY
    - Test: After setting priority, new module scope reads from env var
    - Test: Env var survives across function calls (simulates ThreadJob propagation)

    **Describe "BITS Priority Module Initialization":**
    - Test: Module reads FFU_BITS_PRIORITY from environment on load
    - Test: Module defaults to 'Normal' when no env var set

    **Test pattern:** Mock `Start-BitsTransfer`, `Start-ResilientDownload`, and `WriteLog`. Save and restore `$env:FFU_BITS_PRIORITY` in BeforeEach/AfterEach blocks to avoid test pollution. Follow existing test patterns.

    **Important:** Clean up environment variables in AfterEach/AfterAll blocks to prevent test pollution.
  </action>
  <verify>
    - File exists: `Test-Path Tests/Unit/FFU.Common.BitsPriority.Tests.ps1`
    - Pester tests pass: `Invoke-Pester -Path Tests/Unit/FFU.Common.BitsPriority.Tests.ps1 -Output Detailed`
    - At least 8 test cases covering priority cascade and configuration
  </verify>
  <done>
    BITS priority Pester tests cover Set-BitsTransferPriority behavior, priority resolution cascade, environment propagation, and module initialization. All tests pass.
  </done>
</task>

</tasks>

<verification>
1. Both test files exist and follow Pester 5.x patterns
2. CU skip tests cover: equal versions, newer ESD, older ESD, parse failures, missing versions
3. BITS priority tests cover: cascade resolution, env var propagation, module initialization
4. All tests pass: `Invoke-Pester -Path Tests/Unit/FFU.Updates.CUSkip.Tests.ps1, Tests/Unit/FFU.Common.BitsPriority.Tests.ps1 -Output Detailed`
5. No PSScriptAnalyzer errors in test files
</verification>

<success_criteria>
- FFU.Updates.CUSkip.Tests.ps1 has 10+ tests covering version comparison edge cases
- FFU.Common.BitsPriority.Tests.ps1 has 8+ tests covering priority cascade behavior
- All tests pass with Pester 5.x
- Tests properly isolate from network (mock HTTP calls)
- Tests clean up global/env state in AfterEach blocks
</success_criteria>

<output>
After completion, create `.planning/phases/36-cu-skip-esd-bits/36-03-SUMMARY.md`
</output>
