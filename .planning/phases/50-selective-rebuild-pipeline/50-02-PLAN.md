---
phase: 50-selective-rebuild-pipeline
plan: 02
type: execute
wave: 2
depends_on: [50-01]
files_modified:
  - FFUDevelopment/BuildFFUVM_UI.xaml
  - FFUDevelopment/BuildFFUVM_UI.ps1
  - FFUDevelopment/FFUUI.Core/FFUUI.Core.Handlers.psm1
  - .planning/phases/50-selective-rebuild-pipeline/50-UI-SPEC.md
autonomous: true
requirements: [REBUILD-01]
must_haves:
  truths:
    - "Each USB-Mode artifact card shows a single disposition ComboBox replacing the Phase-49 include checkbox (D-01)"
    - "ComboBox item set is tiered: FFU (required) = single disabled Reuse; DeployISO (required-buildable) = enabled Reuse/Rebuild (no Skip); buildable (Drivers, AppsISO) = Reuse/Rebuild/Skip; user-authored (PPKG, Unattend, Autopilot) = Reuse/Skip (D-02, D-04, D-05, D-06, D-12)"
    - "FFU card renders a disabled single-item Reuse ComboBox preserving the hard-lock + inline helper text pointing to Full Build (D-03, D-09); DeployISO card renders an ENABLED 2-item Reuse/Rebuild ComboBox (required = no Skip, buildable = offers Rebuild) (D-04, D-12)"
    - "Scanner UI renders all 4 ArtifactStatus values: Found (Green), Degraded (DarkOrange + warning text), Error (OrangeRed, control disabled), Missing (OrangeRed) (D-11, F7)"
    - "Found/Degraded enable the ComboBox; Error/Missing disable it; default disposition follows D-10 (Found/Degraded->Reuse, missing-optional->Skip)"
  artifacts:
    - path: "FFUDevelopment/BuildFFUVM_UI.xaml"
      provides: "7 disposition ComboBoxes + 7 warning TextBlocks + FFU helper text"
      contains: "usbDriversDisposition"
    - path: "FFUDevelopment/BuildFFUVM_UI.ps1"
      provides: "usbArtifactState extended with a per-artifact disposition property (init site)"
      contains: "disposition"
    - path: "FFUDevelopment/FFUUI.Core/FFUUI.Core.Handlers.psm1"
      provides: "$artifactMap dispCtrl/tier/warnCtrl rework, 4-status switch, per-card SelectionChanged handlers"
      contains: "dispCtrl"
  key_links:
    - from: "FFUUI.Core.Handlers.psm1 Invoke-USBArtifactScan"
      to: "$State.Data.usbArtifactState[Type].disposition"
      via: "SelectionChanged handler writes SelectedItem.Tag"
      pattern: "disposition\\s*="
    - from: "BuildFFUVM_UI.xaml ComboBox"
      to: "Handlers $artifactMap dispCtrl"
      via: "x:Name usb{Type}Disposition matches dispCtrl value"
      pattern: "usb\\w+Disposition"
---

<objective>
Replace the Phase-49 per-artifact include CheckBox with a tiered disposition ComboBox on all 7 USB-Mode artifact cards, and widen the scanner result rendering from binary Found/Missing to the full 4-status model (Found / Degraded / Error / Missing). Implements REBUILD-01 and decisions D-01, D-02, D-03, D-04, D-05, D-06, D-09, D-10, D-11, D-12.

CRITICAL tier resolution (orchestrator BLOCKER-3 authoritative ruling): DeployISO is BOTH required AND buildable. Its ComboBox offers exactly TWO items — `Reuse` and `Rebuild` — and is ENABLED. It does NOT offer `Skip` (a deployment USB cannot ship without boot media; required = never excludable), and it DOES offer Rebuild (D-04 buildable + plan-05 `$rebuildDeployISO`). The `$artifactMap` `tier` for DeployISO is `required-buildable`. This satisfies D-04 (Rebuild offered -> buildable, so plan-05's `$rebuildDeployISO = (Disposition -eq 'Rebuild')` is reachable, not dead code) AND the required nature (no Skip path). FFU remains UNCHANGED: a single disabled `Reuse` item only (D-06/D-08 — no inline FFU rebuild); its `$artifactMap` tier stays `required`.

Purpose: The ComboBox is the user-facing control that drives the entire selective-rebuild contract; the 4-status widening surfaces degraded artifacts honestly (D-11). DeployISO's enabled Reuse/Rebuild item set is what makes the plan-05 DeployISO rebuild path live config-reachable.
Output: Modified XAML (7 cards), the usbArtifactState init site (BuildFFUVM_UI.ps1), the Handlers module ($artifactMap, scan switch, SelectionChanged handlers), and the 50-UI-SPEC.md DeployISO copywriting + tier rows (brought into agreement with this ruling).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-selective-rebuild-pipeline/50-UI-SPEC.md
@.planning/phases/50-selective-rebuild-pipeline/50-PATTERNS.md
@.planning/phases/50-selective-rebuild-pipeline/50-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap CheckBoxes for tiered disposition ComboBoxes + warning rows in XAML (7 cards), and reconcile the 50-UI-SPEC DeployISO tier</name>
  <read_first>
    - FFUDevelopment/BuildFFUVM_UI.xaml (the file being modified: usbModeTab cards lines 932-1243; FFU card 947-997; DeployISO card 1001-1043; existing usbFFUInclude line 959, usbDeployISOInclude line 1012)
    - .planning/phases/50-selective-rebuild-pipeline/50-UI-SPEC.md (XAML Additions Required section lines 343-455 — exact per-card markup, tiers, helper text, warning TextBlocks; Copywriting Contract Disposition ComboBox Items table lines 123-135 — the DeployISO tier row this task corrects)
    - .planning/phases/50-selective-rebuild-pipeline/50-PATTERNS.md (XAML CheckBox-to-ComboBox swap section lines 407-463 — Row 0 structure + Margin guidance)
  </read_first>
  <action>
    In FFUDevelopment/BuildFFUVM_UI.xaml, for each of the 7 artifact cards (FFU, DeployISO, Drivers, AppsISO, PPKG, Unattend, Autopilot) replace the Row 0 `<CheckBox x:Name="usb{Type}Include" .../>` with a horizontal StackPanel containing a bold 13px label TextBlock (preserve the exact label text: "FFU Image", "WinPE Deploy ISO", "Drivers", "Applications ISO", "Provisioning Package", "Unattend.xml", "Autopilot Profile") plus a `<ComboBox x:Name="usb{Type}Disposition" Width="120" FontSize="11" SelectedIndex="0" VerticalAlignment="Center">` per the UI-SPEC.
    Item sets and enabled state by tier:
      FFU (tier required): a single `<ComboBoxItem Content="Reuse" Tag="Reuse"/>`, ComboBox `IsEnabled="False"` (locked, disabled — D-06/D-08).
      DeployISO (tier required-buildable, per BLOCKER-3 ruling): exactly TWO items — `<ComboBoxItem Content="Reuse" Tag="Reuse"/>` and `<ComboBoxItem Content="Rebuild" Tag="Rebuild"/>` — and the ComboBox is left ENABLED at the XAML level (IsEnabled defaults true; scan logic in Task 2 manages enable/disable like the other scan-driven cards). It MUST NOT contain a Skip item (required = never excludable).
      Drivers and AppsISO (tier buildable): three items Reuse/Rebuild/Skip; `IsEnabled="False"` initially (scan enables).
      PPKG, Unattend, Autopilot (tier user-authored): two items Reuse/Skip only; `IsEnabled="False"` initially (scan enables).
    On the FFU card only, add the inline helper text TextBlock below the Row 0 StackPanel: Text="To rebuild the OS image, switch to Full Build" FontSize="11" Foreground="#888888" FontStyle="Italic" (D-09).
    On EVERY card add a warning TextBlock as a new final row: `<TextBlock x:Name="usb{Type}Warning" Text="" FontSize="11" Foreground="DarkOrange" Visibility="Collapsed" TextWrapping="Wrap" Margin="20,0,0,3"/>`. Add the corresponding `<RowDefinition Height="Auto"/>` to each card Grid (FFU card adds two: helper text row + warning row) and re-number existing Grid.Row attributes as needed.
    SPACING FIX (orchestrator critical guidance #4): use grid-aligned margins from the 4px/8px scale for the new StackPanel and helper text — use Margin="0,0,8,0" for the label-to-ComboBox gap and Margin="0,0,0,8" (sm) or Margin="0,0,0,4" (xs) instead of any non-grid 5px/2px values; the warning TextBlock keeps the legacy card-indent Margin="20,0,0,3" per UI-SPEC. Do NOT introduce new 5px/2px non-grid margins on the new elements.
    The XAML must remain parseable by [Windows.Markup.XamlReader]::Load(). Do not change the GroupBox separators, card outer Margin="5", or Separator Margin="0,10" legacy values.
    SPEC RECONCILIATION (BLOCKER-3): also update .planning/phases/50-selective-rebuild-pipeline/50-UI-SPEC.md so the SPEC agrees with this ruling — in the Copywriting Contract "Disposition ComboBox Items" table change the "WinPE Deploy ISO" row to tier `required-buildable`, item labels `Reuse`, `Rebuild`, and Enabled column "enabled when Found or Degraded (scan-driven, like Drivers)"; and replace the "Note on DeployISO tier" paragraph (lines ~135) with the resolved ruling (DeployISO = required-buildable: 2 enabled items Reuse/Rebuild, never Skip). Keep the DeployISO XAML Additions snippet (lines ~377-389) consistent: 2 items Reuse+Rebuild, no IsEnabled="False" hard-lock.
  </action>
  <verify>
    <automated>cd "D:/claude/FFUBuilder/FFUDevelopment" && pwsh -NoProfile -Command "Add-Type -AssemblyName PresentationFramework; $x=[System.IO.File]::ReadAllText('BuildFFUVM_UI.xaml'); $sr=New-Object System.IO.StringReader($x); $xr=[System.Xml.XmlReader]::Create($sr); [Windows.Markup.XamlReader]::Load($xr) | Out-Null; Write-Host 'XAML-PARSE-OK'" ; echo "EXIT=$?"</automated>
  </verify>
  <acceptance_criteria>
    - XAML contains exactly 7 ComboBoxes named usbFFUDisposition, usbDeployISODisposition, usbDriversDisposition, usbAppsISODisposition, usbPPKGDisposition, usbUnattendDisposition, usbAutopilotDisposition.
    - FFU ComboBox has exactly one ComboBoxItem (Tag="Reuse") and IsEnabled="False".
    - DeployISO ComboBox has exactly TWO ComboBoxItems (Tag="Reuse" and Tag="Rebuild"), NO Skip item, and is NOT IsEnabled="False" in XAML (scan-driven enable). (BLOCKER-3.)
    - Drivers and AppsISO have three items (Reuse/Rebuild/Skip). PPKG/Unattend/Autopilot have two items (Reuse/Skip).
    - XAML contains 7 warning TextBlocks named usb{Type}Warning with Visibility="Collapsed" and Foreground="DarkOrange".
    - FFU card contains the helper text "To rebuild the OS image, switch to Full Build".
    - No `usb{Type}Include` CheckBox remains in the USB Mode tab (grep on the file for `usbFFUInclude`, comments excluded, returns 0).
    - 50-UI-SPEC.md "Disposition ComboBox Items" table shows DeployISO as required-buildable with Reuse/Rebuild items, and the old "treated as required-locked Reuse only" DeployISO note is replaced by the required-buildable ruling.
    - XAML parse verify prints XAML-PARSE-OK (exit 0).
  </acceptance_criteria>
  <done>All 7 cards use tiered disposition ComboBoxes + warning rows; DeployISO = enabled Reuse/Rebuild (no Skip); FFU helper text present; XAML parses; 50-UI-SPEC DeployISO rows reconciled.</done>
</task>

<task type="auto">
  <name>Task 2: Rework $artifactMap and widen Invoke-USBArtifactScan to 4-status switch</name>
  <read_first>
    - FFUDevelopment/FFUUI.Core/FFUUI.Core.Handlers.psm1 (the file being modified: $artifactMap lines 26-34; binary Found/else branch lines 107-161; includeCtrl references lines 94,99,125,150)
    - .planning/phases/50-selective-rebuild-pipeline/50-PATTERNS.md (Handlers section lines 220-305 — exact $artifactMap replacement, switch widening, includeCtrl->dispCtrl rename)
    - .planning/phases/50-selective-rebuild-pipeline/50-UI-SPEC.md (Scan result->ComboBox enable rule lines 206-213; Dynamic Foreground States lines 105-115; Warning TextBlock display lines 264-289; $artifactMap extension lines 291-318)
    - FFUDevelopment/Modules/FFU.ArtifactScanner/Classes/ArtifactScanner.Classes.ps1 (source: ArtifactResult.ErrorMessage / Warnings field for the degraded warning text)
  </read_first>
  <action>
    In FFUUI.Core.Handlers.psm1 replace the $artifactMap (lines 26-34): for each artifact rename the `includeCtrl = 'usb{Type}Include'` key to `dispCtrl = 'usb{Type}Disposition'`, and add `tier` and `warnCtrl = 'usb{Type}Warning'`. Tier values per the BLOCKER-3 ruling: FFU = 'required'; DeployISO = 'required-buildable'; Drivers and AppsISO = 'buildable'; PPKG, Unattend, Autopilot = 'user-authored'. Use the map from 50-UI-SPEC.md lines 296-318 but set DeployISO tier to 'required-buildable' (the SPEC line 301 'required' is corrected to 'required-buildable' per BLOCKER-3 — Task 1 reconciles the SPEC narrative; this map value is authoritative).
    Replace the binary `if ($result.Status -eq 'Found') {...} else {...}` block (lines ~107-161) with a `switch ($result.Status.ToString())` having four cases:
      'Found' -> status Text 'Found', Foreground Green, FontStyle Normal, enable $State.Controls[$map.dispCtrl], hide warnCtrl, default disposition Reuse.
      'Degraded' -> status Text 'Found (degraded)', Foreground DarkOrange, enable dispCtrl, set warnCtrl Text to $result.ErrorMessage (or $result.Warnings[0] — use the ArtifactResult field that exists) and warnCtrl Visibility Visible, default disposition Reuse (D-11).
      'Error' -> status Text 'Error (scanner)', Foreground OrangeRed, disable dispCtrl, hide warnCtrl; for optional tier default disposition Skip, for required/required-buildable tier leave Reuse (IsReady gate blocks launch).
      default ('Missing') -> status Text 'Missing', Foreground OrangeRed, disable dispCtrl, hide warnCtrl; optional tier default Skip.
    Tier handling note: treat tier 'required' and 'required-buildable' the same for the Error/Missing default-disposition fallback (both leave Reuse, never auto-Skip) — only 'buildable' and 'user-authored' fall back to Skip. The DeployISO enable/disable on Found/Degraded works through the same dispCtrl path as the other scan-driven cards (its XAML ComboBox is enabled, unlike FFU which stays IsEnabled=False from XAML).
    Change ALL `$map.includeCtrl` references to `$map.dispCtrl` (lines ~94, 99, 125, 150). Use typed WPF Brushes/FontStyles (System.Windows.Media.Brushes::Green/DarkOrange/OrangeRed, System.Windows.FontStyles::Normal) — no bare strings (Phase 49 decision). Add a small helper to set the ComboBox default by matching ComboBoxItem.Tag (or inline the loop) since these are ComboBoxes not checkboxes.
    Use `if ($function:WriteLog) { WriteLog ... }` for any logging. Keep CLAUDE.md PowerShell style (OTBS, 4-space, full cmdlet names).
  </action>
  <verify>
    <automated>cd "D:/claude/FFUBuilder/FFUDevelopment" && pwsh -NoProfile -Command "Invoke-Pester -Path 'Tests/Unit/FFUUI.Core.Handlers.Tests.ps1' -Output Minimal" ; echo "EXIT=$?"</automated>
  </verify>
  <acceptance_criteria>
    - $artifactMap source contains `dispCtrl`, `tier`, and `warnCtrl` for all 7 artifacts and contains no `includeCtrl` key.
    - DeployISO's $artifactMap entry has `tier='required-buildable'` (BLOCKER-3).
    - Invoke-USBArtifactScan contains a `switch` with explicit `'Degraded'` and `'Error'` cases and references DarkOrange.
    - The Error/Missing default-disposition fallback treats 'required' and 'required-buildable' identically (never auto-Skip); only 'buildable'/'user-authored' fall back to Skip.
    - No `$map.includeCtrl` references remain (grep returns 0 excluding comments).
    - The Phase 50 Handlers tests added in plan 01 (artifactMap keys + 4-status rendering) pass.
    - Module imports without error: `Import-Module FFUUI.Core/FFUUI.Core.Handlers.psm1 -Force` succeeds (covered by the test BeforeAll).
  </acceptance_criteria>
  <done>$artifactMap reworked to dispCtrl/tier/warnCtrl (DeployISO=required-buildable); 4-status switch implemented; plan-01 Handlers tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Extend usbArtifactState init (BuildFFUVM_UI.ps1) + add per-card SelectionChanged handlers writing disposition</name>
  <read_first>
    - FFUDevelopment/BuildFFUVM_UI.ps1 (the file being modified for the state init: $State.Data.usbArtifactState construction at lines 72-80 — each artifact is `[PSCustomObject]@{ path = $null; source = 'auto' }`)
    - FFUDevelopment/FFUUI.Core/FFUUI.Core.Handlers.psm1 (the file being modified for handlers: Register-EventHandlers; isLoadingConfig guard idiom line 20)
    - .planning/phases/50-selective-rebuild-pipeline/50-PATTERNS.md (SelectionChanged idiom analogs: cmbBitsPriority lines 552-565, cmbVMSwitchName lines 414-430; new handler template lines 276-289; Window.Tag state retrieval shared pattern lines 653-660)
    - .planning/phases/50-selective-rebuild-pipeline/50-UI-SPEC.md (SelectionChanged handler trigger lines 215-221; Per-Artifact State Tracking Extension lines 459-477)
  </read_first>
  <action>
    State-init edit (CONCRETE FILE per BLOCKER-2): the `$State.Data.usbArtifactState` hashtable is constructed in FFUDevelopment/BuildFFUVM_UI.ps1 at lines 72-80 (NOT in any .psm1 and NOT in FFUUI.Core.Config.psm1 which plan 03 owns). Add a `disposition = 'Reuse'` property to each of the 7 `[PSCustomObject]@{ path = $null; source = 'auto' }` entries (FFU, DeployISO, Drivers, PPKG, Unattend, Autopilot, AppsISO) so every entry becomes `[PSCustomObject]@{ path = $null; source = 'auto'; disposition = 'Reuse' }`. Do NOT touch FFUUI.Core.Config.psm1 (plan 03). BuildFFUVM_UI.ps1 is listed in this plan's files_modified and does not collide with plan 03's Config.psm1.
    Handler edits (FFUUI.Core.Handlers.psm1): Register an `Add_SelectionChanged` handler for each of the 7 disposition ComboBoxes mirroring the cmbBitsPriority idiom: retrieve $localState via `[System.Windows.Window]::GetWindow($eventSource).Tag` (never closure-capture $State); return immediately if `$localState.Flags.isLoadingConfig` (Pitfall 3 guard); return if `$selectionChangedEventArgs.AddedItems.Count -eq 0`; read `$eventSource.SelectedItem.Tag`; write it to `$localState.Data.usbArtifactState['{Type}'].disposition`.
    The FFU ComboBox is IsEnabled=False so its handler fires only during programmatic config-load selection — the isLoadingConfig guard suppresses those writes; still register it for symmetry. DeployISO is ENABLED (required-buildable) so its handler fires on genuine user Reuse/Rebuild selection and must write disposition like the buildable cards.
    Use the established three-line Window.Tag retrieval pattern. CLAUDE.md PowerShell style throughout.
  </action>
  <verify>
    <automated>cd "D:/claude/FFUBuilder/FFUDevelopment" && pwsh -NoProfile -Command "Invoke-Pester -Path 'Tests/Unit/FFUUI.Core.Handlers.Tests.ps1' -Output Minimal" ; echo "EXIT=$?"</automated>
  </verify>
  <acceptance_criteria>
    - BuildFFUVM_UI.ps1 usbArtifactState init (lines ~72-80) gives all 7 entries a `disposition = 'Reuse'` property.
    - Handlers source contains `Add_SelectionChanged` registrations referencing all 7 `usb{Type}Disposition` controls.
    - Each handler contains the `isLoadingConfig` guard and `AddedItems.Count -eq 0` guard, and writes `usbArtifactState['{Type}'].disposition`.
    - The DeployISO SelectionChanged handler writes disposition (it is enabled, not suppressed like FFU).
    - FFUUI.Core.Handlers.Tests.ps1 passes (including the Phase 50 cases from plan 01); module imports cleanly.
  </acceptance_criteria>
  <done>usbArtifactState carries disposition (init at BuildFFUVM_UI.ps1:72-80); 7 disposition SelectionChanged handlers registered with guards; Handlers tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI control -> in-memory state | ComboBox SelectedItem.Tag is written into $State.Data.usbArtifactState.disposition. Tag values are static XAML literals (Reuse/Rebuild/Skip), not free user input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-02 | Tampering | Disposition value entering state | mitigate | ComboBox items are fixed XAML literals with Tag in {Reuse,Rebuild,Skip}; the user cannot inject arbitrary values via the control. Config-file injection of bad Disposition values is validated downstream in the pipeline gate (plan 04, enum allow-list). |
| T-50-03 | Information Disclosure | Degraded warning text from scanner | accept | warnCtrl text comes from ArtifactResult.ErrorMessage produced by the trusted in-process scanner; no external/PII content. N/A: local desktop, no disclosure surface. |
| T-50-04 | Elevation of Privilege | N/A | accept | UI plan executes no artifacts and runs no Start-Process; no privilege boundary. N/A justification: pure UI wiring. |
| T-50-SC | Tampering | npm/pip/cargo installs | accept | No package installs. |
</threat_model>

<verification>
- XAML parses via XamlReader.Load.
- FFUUI.Core.Handlers.Tests.ps1 green (artifactMap keys, 4-status rendering, SelectionChanged guards).
- Run `.\Tests\Unit\Invoke-PesterTests.ps1 -Module 'FFUUI.Core'` — no regressions.
</verification>

<success_criteria>
- 7 tiered disposition ComboBoxes replace the include checkboxes; FFU locked Reuse (disabled); DeployISO enabled Reuse/Rebuild (no Skip); FFU helper text present (D-01/02/03/04/05/06/09/12).
- 4-status scanner rendering with Degraded warning surfacing (D-10/D-11/F7).
- disposition state wired through SelectionChanged with config-load guard; usbArtifactState carries disposition; 50-UI-SPEC DeployISO tier reconciled.
</success_criteria>

<output>
Create `.planning/phases/50-selective-rebuild-pipeline/50-02-SUMMARY.md` when done.
</output>
