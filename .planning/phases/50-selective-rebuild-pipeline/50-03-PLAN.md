---
phase: 50-selective-rebuild-pipeline
plan: 03
type: execute
wave: 3
depends_on: [50-02]
files_modified:
  - FFUDevelopment/FFUUI.Core/FFUUI.Core.Config.psm1
autonomous: true
requirements: [REBUILD-01]
must_haves:
  truths:
    - "Build-UIConfiguration writes a per-artifact Disposition string (read from the ComboBox SelectedItem.Tag) into config.USBMode.Artifacts and no longer writes the non-schema Include boolean (F2, D-01)"
    - "Update-UIFromConfig restores each artifact's disposition by matching ComboBoxItem.Tag to the saved Disposition value, guarded by isLoadingConfig so SelectionChanged does not corrupt state during load (Pitfall 3)"
    - "A disposition saved in one session is present after reload (round-trip survives) (REBUILD-01, DISC-03 contract)"
  artifacts:
    - path: "FFUDevelopment/FFUUI.Core/FFUUI.Core.Config.psm1"
      provides: "Disposition save (Build-UIConfiguration) and restore (Update-UIFromConfig)"
      contains: "Disposition"
  key_links:
    - from: "Build-UIConfiguration"
      to: "config.USBMode.Artifacts[Type].Disposition"
      via: "ComboBox SelectedItem.Tag read"
      pattern: "SelectedItem\\.Tag"
    - from: "Update-UIFromConfig"
      to: "usb{Type}Disposition.SelectedItem"
      via: "ComboBoxItem.Tag match restore"
      pattern: "\\.Tag\\s*-eq"
---

<objective>
Complete the disposition config round-trip: rewrite Build-UIConfiguration to persist each artifact's Disposition (read from its ComboBox Tag) instead of the static `Disposition='Reuse'` + non-schema `Include` it currently writes, and rewrite Update-UIFromConfig to restore the ComboBox selection from the saved Disposition. This is the shared contract the pipeline (plans 04/05) reads. Implements REBUILD-01 and decision D-01; closes the F2 config half.

Purpose: The schema Disposition enum is the single source of truth; this plan removes the dual Include/Disposition write and makes the ComboBox state durable across sessions.
Output: Modified FFUUI.Core.Config.psm1 (save + restore blocks).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-selective-rebuild-pipeline/50-PATTERNS.md
@.planning/phases/50-selective-rebuild-pipeline/50-RESEARCH.md
@.planning/phases/50-selective-rebuild-pipeline/50-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Build-UIConfiguration to save Disposition from ComboBox (drop Include)</name>
  <read_first>
    - FFUDevelopment/FFUUI.Core/FFUUI.Core.Config.psm1 (the file being modified: existing USB artifact save block lines 166-188 — the static `Disposition='Reuse'` + `Include=$includeChecked` write to replace)
    - .planning/phases/50-selective-rebuild-pipeline/50-PATTERNS.md (Config.psm1 section lines 309-351 — exact replacement reading ComboBox Tag)
    - .planning/phases/50-selective-rebuild-pipeline/50-RESEARCH.md (F2 Build-UIConfiguration code example lines 738-750)
  </read_first>
  <action>
    In FFUUI.Core.Config.psm1, inside the existing `foreach ($artifactType in @('FFU','DeployISO','Drivers','PPKG','Unattend','Autopilot','AppsISO'))` loop of Build-UIConfiguration, replace the include-checkbox read and the static disposition write (lines ~177-188) with: compute `$dispCtrlName = "usb${artifactType}Disposition"`; default `$disposition = 'Reuse'`; if `$State.Controls[$dispCtrlName]` and its `.SelectedItem` are non-null, set `$disposition = $State.Controls[$dispCtrlName].SelectedItem.Tag`. Write the artifact entry as `@{ Path = $pathToSave; Disposition = $disposition }` — remove the `Include` key entirely and remove the hardcoded `Disposition = 'Reuse'`.
    Preserve the existing $pathToSave / source='user' logic and the ActiveMode write above the loop. Do not touch unrelated config sections.
    CLAUDE.md PowerShell style; use WriteLog where the existing block logs.
  </action>
  <verify>
    <automated>cd "D:/claude/FFUBuilder/FFUDevelopment" && pwsh -NoProfile -Command "Invoke-Pester -Path 'Tests/Unit/SelectiveRebuild.Tests.ps1' -Output Minimal" ; echo "EXIT=$?"</automated>
  </verify>
  <acceptance_criteria>
    - Build-UIConfiguration reads `usb${artifactType}Disposition` SelectedItem.Tag and writes it as `Disposition`.
    - The artifact entry hashtable no longer contains an `Include` key (grep for `Include` in the USB save block excluding comments returns 0).
    - No `Disposition = 'Reuse'` hardcoded literal remains as the written value (the value now comes from the control).
    - The SelectiveRebuild.Tests round-trip "writes Rebuild disposition" and "no Include key" assertions pass.
  </acceptance_criteria>
  <done>Build-UIConfiguration persists ComboBox-derived Disposition; Include write removed; round-trip save test green.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite Update-UIFromConfig to restore ComboBox from saved Disposition (guarded)</name>
  <read_first>
    - FFUDevelopment/FFUUI.Core/FFUUI.Core.Config.psm1 (the file being modified: existing USB artifact restore block lines 639-665 — Include checkbox restore to replace; cmbVMwareNicType Tag-based restore analog lines 621-636)
    - .planning/phases/50-selective-rebuild-pipeline/50-PATTERNS.md (Config.psm1 restore section lines 353-403 — exact Tag-match restore + isLoadingConfig ordering note)
    - .planning/phases/50-selective-rebuild-pipeline/50-RESEARCH.md (F2 Update-UIFromConfig code example lines 752-766)
  </read_first>
  <action>
    In FFUUI.Core.Config.psm1, replace the Include-checkbox restore (lines ~657-663) with a Disposition restore that mirrors the cmbVMwareNicType Tag-based pattern: if `$entry` has a `Disposition` property (PSObject.Properties.Match('Disposition').Count -gt 0), compute `$dispCtrlName = "usb${key}Disposition"`; if `$State.Controls[$dispCtrlName]` is non-null, iterate its `.Items`, and for the ComboBoxItem whose `.Tag -eq $entry.Disposition` set `$State.Controls[$dispCtrlName].SelectedItem = $item` and break.
    Ensure the `isLoadingConfig` guard is set to `$true` before this restore and reset afterward (Update-UIFromConfig already sets this per the Phase-49 pattern — confirm it wraps the USB restore so the SelectionChanged handlers from plan 02 short-circuit during programmatic selection; if the guard scope does not already cover the disposition restore, extend it). Remove the old `$entry.Include` restore.
    CLAUDE.md PowerShell style; WriteLog mirroring the cmbVMwareNicType restore log line.
  </action>
  <verify>
    <automated>cd "D:/claude/FFUBuilder/FFUDevelopment" && pwsh -NoProfile -Command "Invoke-Pester -Path 'Tests/Unit/SelectiveRebuild.Tests.ps1','Tests/Unit/FFUUI.Core.ConfigValidation.Tests.ps1' -Output Minimal" ; echo "EXIT=$?"</automated>
  </verify>
  <acceptance_criteria>
    - Update-UIFromConfig restores disposition via ComboBoxItem `.Tag -eq $entry.Disposition` matching and sets SelectedItem.
    - No `$entry.Include` restore remains for USB artifacts (grep returns 0 excluding comments).
    - The disposition restore executes within the isLoadingConfig=$true window (guard covers the restore).
    - SelectiveRebuild + ConfigValidation Disposition-restore tests pass.
  </acceptance_criteria>
  <done>Update-UIFromConfig restores ComboBox disposition by Tag under isLoadingConfig guard; Include restore removed; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config JSON file -> UI control state | Update-UIFromConfig reads Disposition strings from a persisted config file (user-editable on disk) and selects a matching ComboBox item. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-05 | Tampering | Disposition value from config file | mitigate | Restore only sets SelectedItem when a ComboBoxItem with a matching Tag exists; an unknown/malformed Disposition value in the file matches nothing and leaves the default (Reuse) selected. No code executes from the value. Pipeline-side enum validation (plan 04) is the second layer. |
| T-50-06 | Spoofing | N/A | accept | No auth/identity in scope. N/A: local desktop config round-trip. |
| T-50-SC | Tampering | npm/pip/cargo installs | accept | No package installs. |
</threat_model>

<verification>
- SelectiveRebuild.Tests.ps1 green (save + restore round-trip).
- `.\Tests\Unit\Invoke-PesterTests.ps1 -Module 'FFUUI.Core'` — no regressions to existing config tests.
</verification>

<success_criteria>
- Disposition persisted from ComboBox and restored to ComboBox; Include fully removed from the USB save/restore path (D-01).
- Round-trip survives reload; restore is guarded against SelectionChanged corruption.
</success_criteria>

<output>
Create `.planning/phases/50-selective-rebuild-pipeline/50-03-SUMMARY.md` when done.
</output>
