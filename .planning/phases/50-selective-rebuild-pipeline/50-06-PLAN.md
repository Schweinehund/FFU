---
phase: 50-selective-rebuild-pipeline
plan: 06
type: execute
wave: 4
depends_on: [50-02, 50-03, 50-04, 50-05]
files_modified:
  - FFUDevelopment/version.json
  - FFUDevelopment/FFUUI.Core/FFUUI.Core.psd1
  - CHANGELOG_FORK.md
autonomous: false
requirements: [REBUILD-01, REBUILD-02, REBUILD-03]
must_haves:
  truths:
    - "version.json main version receives a MINOR bump (new user-facing Selective Rebuild feature) with buildDate updated to the current date, and the FFUUI.Core module version is bumped to match the .psd1 (version.json is the single source of truth)"
    - "FFUUI.Core.psd1 ModuleVersion is incremented with a ReleaseNotes entry describing the disposition ComboBox + 4-status rendering changes"
    - "CHANGELOG_FORK.md documents the Selective Rebuild Pipeline feature (REBUILD-01/02/03)"
    - "Full Pester suite is green and verify-app regression passes before the phase is marked complete (CLAUDE.md BLOCKING gate)"
  artifacts:
    - path: "FFUDevelopment/version.json"
      provides: "Bumped main version + FFUUI.Core module version + buildDate"
      contains: "FFUUI.Core"
    - path: "CHANGELOG_FORK.md"
      provides: "Phase 50 feature changelog entry"
      contains: "Selective Rebuild"
  key_links:
    - from: "FFUUI.Core.psd1 ModuleVersion"
      to: "version.json modules.FFUUI.Core.version"
      via: "matching version strings"
      pattern: "ModuleVersion"
---

<objective>
Finalize Phase 50: bump versions per the project versioning policy (a new user-facing feature is a MINOR main bump; the FFUUI.Core module version change must be mirrored in both the .psd1 and version.json with a buildDate update and release notes), record the feature in CHANGELOG_FORK.md, and run the BLOCKING verify-app regression gate required by CLAUDE.md before the phase can be marked complete. Covers REBUILD-01/02/03 closure.

Note on which manifests bump: the UI changes (plans 02/03) modify FFUUI.Core (FFUUI.Core.psd1) — that module's .psd1 version MUST bump. BuildFFUVM.ps1 (plans 04/05) reads version.json at runtime and is not a module manifest, so no .psd1 bump there; the version.json main bump covers it. WinPEDeployFFUFiles/ApplyFFU.ps1 $version is NOT touched (no WinPE-side code changed this phase).

Purpose: Versioning discipline + the mandatory regression gate are project-blocking requirements; skipping them blocks phase completion.
Output: Updated version.json, FFUUI.Core.psd1, CHANGELOG_FORK.md, and a green verify-app run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@docs/PM_CHECKLIST.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump FFUUI.Core.psd1 + version.json (MINOR main) and add release notes</name>
  <read_first>
    - FFUDevelopment/version.json (the file being modified: main version 1.10.2, buildDate, modules.FFUUI.Core.version 0.0.20)
    - FFUDevelopment/FFUUI.Core/FFUUI.Core.psd1 (the file being modified: ModuleVersion 0.0.20 + ReleaseNotes block)
    - CLAUDE.md (Versioning Policy + Version Increment Checklist — MINOR for new user-facing feature; module change requires version.json sync)
  </read_first>
  <action>
    Selective Rebuild Pipeline is a new user-facing feature -> bump version.json main `version` with a MINOR increment (1.10.2 -> 1.11.0) and set `buildDate` to the current date (2026-06-22). Bump `modules.FFUUI.Core.version` from 0.0.20 to the next patch (0.0.21) and update its `description` to reference Phase 50 (disposition ComboBox + 4-status scanner rendering). In FFUUI.Core/FFUUI.Core.psd1, set `ModuleVersion` to '0.0.21' to match, and add a ReleaseNotes entry: "Phase 50 Selective Rebuild: per-artifact disposition ComboBox (Reuse/Rebuild/Skip) replacing include checkbox; 4-status scanner rendering (Found/Degraded/Error/Missing); Disposition config round-trip."
    Keep the two FFUUI.Core version strings identical (psd1 ModuleVersion == version.json modules.FFUUI.Core.version) — this is the single-source-of-truth invariant. Do not alter other module versions.
  </action>
  <verify>
    <automated>cd "D:/claude/FFUBuilder/FFUDevelopment" && pwsh -NoProfile -Command "$v=Get-Content version.json -Raw | ConvertFrom-Json; $psd=Import-PowerShellDataFile FFUUI.Core/FFUUI.Core.psd1; if ($v.modules.'FFUUI.Core'.version -eq $psd.ModuleVersion -and $v.version -eq '1.11.0') { Write-Host 'VERSION-SYNC-OK' } else { throw 'version mismatch' }" ; echo "EXIT=$?"</automated>
  </verify>
  <acceptance_criteria>
    - version.json `version` is 1.11.0 and `buildDate` is 2026-06-22.
    - version.json modules.FFUUI.Core.version equals FFUUI.Core.psd1 ModuleVersion (both 0.0.21).
    - FFUUI.Core.psd1 ReleaseNotes contains a Phase 50 Selective Rebuild entry.
    - Verify prints VERSION-SYNC-OK (exit 0).
  </acceptance_criteria>
  <done>version.json MINOR-bumped + buildDate set; FFUUI.Core .psd1/version.json synced at 0.0.21 with release notes.</done>
</task>

<task type="auto">
  <name>Task 2: Add CHANGELOG_FORK.md entry for the Selective Rebuild Pipeline</name>
  <read_first>
    - CHANGELOG_FORK.md (the file being modified: existing entry format/headers at the top of the file)
  </read_first>
  <action>
    Add a new entry at the top of CHANGELOG_FORK.md (matching the existing entry style/heading convention) for v1.11.0 describing the Selective Rebuild Pipeline: per-artifact Reuse/Rebuild/Skip disposition control (REBUILD-01), selective per-phase execution that rebuilds only marked Drivers/AppsISO/DeployISO artifacts from a cold USB-Mode start (REBUILD-02), AppsISO USB copy path, 4-status artifact scanner UI (Found/Degraded/Error/Missing), and combining rebuilt + reused artifacts into the final USB (REBUILD-03). Reference the requirement IDs.
  </action>
  <verify>
    <automated>cd "D:/claude/FFUBuilder" && pwsh -NoProfile -Command "if ((Get-Content CHANGELOG_FORK.md -Raw) -match 'Selective Rebuild') { Write-Host 'CHANGELOG-OK' } else { throw 'missing changelog entry' }" ; echo "EXIT=$?"</automated>
  </verify>
  <acceptance_criteria>
    - CHANGELOG_FORK.md contains a new v1.11.0 entry mentioning "Selective Rebuild" and REBUILD-01/02/03.
    - Entry follows the existing changelog formatting.
    - Verify prints CHANGELOG-OK (exit 0).
  </acceptance_criteria>
  <done>CHANGELOG_FORK.md updated with the Phase 50 feature entry.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: BLOCKING verify-app regression + manual UI/end-to-end sign-off</name>
  <what-built>
    Phase 50 Selective Rebuild Pipeline: disposition ComboBoxes (7 cards), 4-status scanner rendering, Disposition config round-trip, AppsISO copy path, Disposition-driven copy gate, and selective per-phase rebuild execution (Drivers/AppsISO/DeployISO). Automated unit coverage is green across SelectiveRebuild.Tests.ps1, USBOnlyMode.Tests.ps1, and FFUUI.Core tests.
  </what-built>
  <action>
    BEFORE the checkpoint, run the BLOCKING automated gate required by CLAUDE.md: invoke the verify-app agent (Task tool, subagent_type="general-purpose", prompt: "Act as verify-app agent (read .claude/agents/verify-app.md). Run full regression verification.") and run the full Pester suite `.\Tests\Unit\Invoke-PesterTests.ps1` plus `Invoke-ScriptAnalyzer` on the changed files (BuildFFUVM.ps1, BuildFFUVM_UI.xaml-adjacent psm1s, FFUUI.Core.Handlers.psm1, FFUUI.Core.Config.psm1). All Pester tests MUST pass and no new PSScriptAnalyzer errors. If any fail, HALT and return to the relevant plan to fix before this checkpoint.
  </action>
  <how-to-verify>
    1. Confirm verify-app reported PASS (all Pester green, modules import, no new analyzer errors).
    2. Launch `.\BuildFFUVM_UI.ps1`, switch to USB Mode. Confirm each artifact card shows a disposition ComboBox with the correct item set per tier: FFU and WinPE Deploy ISO = single disabled "Reuse"; Drivers and Applications ISO = Reuse/Rebuild/Skip; Provisioning Package / Unattend.xml / Autopilot Profile = Reuse/Skip.
    3. Confirm the FFU card shows the helper text "To rebuild the OS image, switch to Full Build".
    4. Point an artifact at a degraded/zero-byte file (or simulate) and confirm "Found (degraded)" renders DarkOrange with a visible warning TextBlock.
    5. Save config, restart UI, reload config — confirm each disposition selection persisted.
    6. (Real env, if available) Mark Drivers=Rebuild + FFU=Reuse, run Create USB, and confirm via the Monitor log that only the driver rebuild phase executes and the USB assembles rebuilt + reused artifacts.
  </how-to-verify>
  <acceptance_criteria>
    - verify-app PASS: full Pester suite green; no new PSScriptAnalyzer errors; all modules import.
    - Manual UI checks (ComboBox tiers, FFU helper text, degraded rendering, config persistence) confirmed.
  </acceptance_criteria>
  <resume-signal>Type "approved" when verify-app passed and the manual UI/end-to-end checks look correct, or describe issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan changes versioning metadata + changelog and runs verification; no new runtime trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-14 | Tampering | version/changelog metadata | accept | Metadata-only edits; the verify-app + Pester gate is the integrity control confirming no regressions slipped in across the phase. N/A for execution surface. |
| T-50-SC | Tampering | npm/pip/cargo installs | accept | No package installs. |
</threat_model>

<verification>
- VERSION-SYNC-OK and CHANGELOG-OK automated checks pass.
- verify-app regression PASS (BLOCKING) before approval.
- Full Pester suite green; no new PSScriptAnalyzer errors.
</verification>

<success_criteria>
- version.json (MINOR + buildDate) and FFUUI.Core.psd1 synced with release notes; CHANGELOG_FORK.md updated.
- BLOCKING verify-app + full Pester suite green; manual UI/end-to-end sign-off obtained.
- Phase 50 ready to mark complete (REBUILD-01/02/03 closed).
</success_criteria>

<output>
Create `.planning/phases/50-selective-rebuild-pipeline/50-06-SUMMARY.md` when done.
</output>
