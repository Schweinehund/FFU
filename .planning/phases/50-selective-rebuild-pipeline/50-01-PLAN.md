---
phase: 50-selective-rebuild-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - FFUDevelopment/Tests/Unit/SelectiveRebuild.Tests.ps1
  - FFUDevelopment/Tests/Unit/USBOnlyMode.Tests.ps1
  - FFUDevelopment/Tests/Unit/FFUUI.Core.Handlers.Tests.ps1
  - FFUDevelopment/Tests/Unit/FFUUI.Core.ConfigValidation.Tests.ps1
autonomous: true
requirements: [REBUILD-01, REBUILD-02, REBUILD-03]
must_haves:
  truths:
    - "Wave-0 failing tests exist for every Phase 50 behavior (D-01 ComboBox config round-trip, D-10/D-11 4-status rendering, F1 AppsISO copy, F2 Disposition gate, F3 selective rebuild flags) so downstream plans are verifiable"
    - "Running the new + extended test files surfaces RED for not-yet-implemented behavior (no false greens)"
    # Note: this Wave-0 test plan is test-scaffolding only; the requirements listed in frontmatter (REBUILD-01/02/03) are covered substantively by plans 02-05. Plan 01's role is to provide the verification targets, not to implement the requirements.
  artifacts:
    - path: "FFUDevelopment/Tests/Unit/SelectiveRebuild.Tests.ps1"
      provides: "REBUILD-01 disposition config round-trip + D-10/D-11 4-status enum coverage"
      contains: "Describe"
    - path: "FFUDevelopment/Tests/Unit/USBOnlyMode.Tests.ps1"
      provides: "F1/F2/F3 structural text-search assertions"
      contains: "dispositionCheckTypes"
  key_links:
    - from: "SelectiveRebuild.Tests.ps1"
      to: "FFU.ArtifactScanner ArtifactStatus enum"
      via: "InModuleScope 'FFU.ArtifactScanner'"
      pattern: "InModuleScope"
---

<objective>
Create the Wave-0 test scaffolds that make every Phase 50 behavior verifiable BEFORE implementation. This plan writes (and intentionally fails) structural and unit tests covering: the ComboBox disposition config round-trip (REBUILD-01 / D-01), the 4-status scanner widening (D-10 / D-11 / F7), the AppsISO USB copy path (REBUILD-03 / F1), the Disposition gate rewrite (REBUILD-02 / F2), and the selective rebuild flags (REBUILD-02 / F3).

Purpose: The Nyquist rule requires every downstream task have an automated verify. These scaffolds are the verification targets. They are expected to be RED at the end of this plan and turn GREEN as plans 02-05 land.
Output: One new test file (SelectiveRebuild.Tests.ps1) and extensions to three existing test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-selective-rebuild-pipeline/50-RESEARCH.md
@.planning/phases/50-selective-rebuild-pipeline/50-PATTERNS.md
@.planning/phases/50-selective-rebuild-pipeline/50-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SelectiveRebuild.Tests.ps1 (REBUILD-01 round-trip + D-10/D-11 enum coverage)</name>
  <read_first>
    - FFUDevelopment/Tests/Unit/FFU.ArtifactScanner.Tests.ps1 (analog: module import pattern lines 27-48 + InModuleScope enum-assertion idiom)
    - FFUDevelopment/Tests/Unit/FFUUI.Core.ConfigValidation.Tests.ps1 (analog: New-MockState mock-State pattern lines 40-55)
    - FFUDevelopment/Modules/FFU.ArtifactScanner/Classes/ArtifactScanner.Classes.ps1 (source of truth: ArtifactStatus enum Found/Missing/Error/Degraded lines 22-27)
    - .planning/phases/50-selective-rebuild-pipeline/50-PATTERNS.md (the "SelectiveRebuild.Tests.ps1 — New file" section, lines 531-626)
  </read_first>
  <action>
    Create FFUDevelopment/Tests/Unit/SelectiveRebuild.Tests.ps1 as a new Pester 5.x file. Header: #Requires -Version 7.0 and #Requires -Modules Pester 5.0.0, with the synopsis block from 50-PATTERNS.md.
    Add an `InModuleScope 'FFU.ArtifactScanner'` Describe that asserts the ArtifactStatus enum exposes the values Degraded and Error (use [ArtifactStatus]::Degraded and [ArtifactStatus]::Error). Import the FFU.ArtifactScanner module via the PSModulePath-prepend pattern from the ArtifactScanner.Tests analog.
    Add a Describe 'Disposition Config Round-Trip' tagged 'Unit','SelectiveRebuild' that imports FFUUI.Core.Config.psm1, stubs `function global:WriteLog`, builds a mock $State with Controls.usbDriversDisposition.SelectedItem.Tag = 'Rebuild' (plus the other six dispCtrl controls and a full usbArtifactState hashtable with a `disposition` property per type), calls Build-UIConfiguration -State $state, and asserts $config.USBMode.Artifacts['Drivers'].Disposition -eq 'Rebuild'. Add a second It asserting the saved config contains NO 'Include' key on any artifact entry (D-01 removes Include).
    Mirror the mock-State shape from the ConfigValidation analog. Do NOT require a live WPF runtime — operate on hashtable/PSCustomObject logic only.
    Use 4-space indent, OTBS, full cmdlet names per CLAUDE.md PowerShell style. These tests are EXPECTED to fail at this point (Build-UIConfiguration still writes Include; disposition property may not exist yet) — that is the RED state.
  </action>
  <verify>
    <automated>cd "D:/claude/FFUBuilder/FFUDevelopment" && pwsh -NoProfile -Command "Invoke-Pester -Path 'Tests/Unit/SelectiveRebuild.Tests.ps1' -Output Minimal" ; echo "EXIT=$?"</automated>
  </verify>
  <acceptance_criteria>
    - File FFUDevelopment/Tests/Unit/SelectiveRebuild.Tests.ps1 exists and is discoverable by Pester (no Discovery-phase parse errors; the run reaches the It blocks).
    - File contains `InModuleScope 'FFU.ArtifactScanner'` and references both `[ArtifactStatus]::Degraded` and `[ArtifactStatus]::Error`.
    - File contains a `Build-UIConfiguration` round-trip assertion checking `Disposition -eq 'Rebuild'` and an assertion that no `Include` key is written.
    - Pester run completes Discovery without errors; behavioral Its may be RED (failing), which is the expected Wave-0 state.
  </acceptance_criteria>
  <done>SelectiveRebuild.Tests.ps1 created; Pester discovers it and runs the Its (failing assertions allowed).</done>
</task>

<task type="auto">
  <name>Task 2: Extend USBOnlyMode.Tests.ps1 with F1/F2/F3 structural assertions</name>
  <read_first>
    - FFUDevelopment/Tests/Unit/USBOnlyMode.Tests.ps1 (analog: existing structural text-search `$scriptContent | Should -Match` pattern lines 115-163)
    - .planning/phases/50-selective-rebuild-pipeline/50-PATTERNS.md (the "USBOnlyMode.Tests.ps1 — Extend" section lines 467-527, exact Context/It blocks to add)
    - .planning/phases/50-selective-rebuild-pipeline/50-RESEARCH.md (F1/F2/F3 sections — variable names: $CopyAppsISO, $AppsISOPath, dispositionCheckTypes, $rebuildDrivers, $rebuildAppsISO, $rebuildDeployISO, New-AppsISO, New-PEMedia)
  </read_first>
  <action>
    Append three new Context blocks to FFUDevelopment/Tests/Unit/USBOnlyMode.Tests.ps1, reusing the existing `$scriptContent` raw-read in BeforeAll (the $scriptContent variable that holds Get-Content of BuildFFUVM.ps1).
    Context 'F1 — AppsISO Copy Path': assert $scriptContent matches `\$CopyAppsISO\s*=`, `\$AppsISOPath\s*=`, `\$using:CopyAppsISO`, and `\$using:AppsISOPath`.
    Context 'F2 — Disposition Gate': assert $scriptContent does NOT match `PSObject\.Properties\.Match\(.Include.\)` (Include gate removed), matches `dispositionCheckTypes`, and matches `PSObject\.Properties\.Match\('Disposition'\)`.
    Context 'F3 — Selective Rebuild Flags': assert $scriptContent matches `\$rebuildDrivers\s*=`, `\$rebuildAppsISO\s*=`, `\$rebuildDeployISO\s*=`, `if\s*\(\$rebuildDrivers\)`, `New-AppsISO`, and `New-PEMedia`.
    Copy the exact It names and regex from 50-PATTERNS.md so the assertions match the variable names the pipeline plans (04/05) will introduce. These tests are EXPECTED to be RED now.
    Do not alter existing Context blocks. Use the existing file's indentation and style.
  </action>
  <verify>
    <automated>cd "D:/claude/FFUBuilder/FFUDevelopment" && pwsh -NoProfile -Command "Invoke-Pester -Path 'Tests/Unit/USBOnlyMode.Tests.ps1' -Output Minimal" ; echo "EXIT=$?"</automated>
  </verify>
  <acceptance_criteria>
    - USBOnlyMode.Tests.ps1 contains three new Context blocks named for F1, F2, F3 with the regex assertions listed above.
    - File parses and Pester reaches the new Its (Discovery succeeds).
    - The F1/F2/F3 Its are RED at this point (pipeline not yet modified) — expected Wave-0 state.
    - Pre-existing tests in the file still pass (no regressions introduced by the edit).
  </acceptance_criteria>
  <done>USBOnlyMode.Tests.ps1 extended with F1/F2/F3 Context blocks; Discovery clean; new Its present.</done>
</task>

<task type="auto">
  <name>Task 3: Extend Handlers + ConfigValidation tests for 4-status rendering and disposition controls</name>
  <read_first>
    - FFUDevelopment/Tests/Unit/FFUUI.Core.Handlers.Tests.ps1 (analog: existing mock-State pattern lines 28-63 — self-extend)
    - FFUDevelopment/Tests/Unit/FFUUI.Core.ConfigValidation.Tests.ps1 (analog: New-MockState and config assertions)
    - FFUDevelopment/FFUUI.Core/FFUUI.Core.Handlers.psm1 (source: current $artifactMap lines 26-34, binary Found/else branch lines 107-161)
    - .planning/phases/50-selective-rebuild-pipeline/50-UI-SPEC.md (Dynamic Foreground States table lines 105-115; $artifactMap extension lines 296-318)
  </read_first>
  <action>
    Extend FFUUI.Core.Handlers.Tests.ps1: add a Context 'Phase 50 — $artifactMap disposition keys' asserting the module source text (Get-Content of FFUUI.Core.Handlers.psm1 -Raw) contains `dispCtrl`, `tier`, and `warnCtrl` keys and does NOT contain `includeCtrl` (renamed). Add a Context 'Phase 50 — 4-status rendering' asserting the source contains a `switch` over status with case strings `'Degraded'` and `'Error'`, plus references to `DarkOrange` and a `usb{Type}Warning`-style control name (assert via regex `usb\w+Warning`).
    Extend FFUUI.Core.ConfigValidation.Tests.ps1 (or add cases there): assert Update-UIFromConfig restore logic reads `Disposition` (source text contains `usb${key}Disposition` or `Disposition`) and no longer restores `Include` for USB artifacts. Use the existing test file's mock/import idioms; if a structural source-text assertion is cleaner, use Get-Content -Raw on FFUUI.Core.Config.psm1.
    All new tests RED-expected (Handlers/Config not yet modified). Keep CLAUDE.md PowerShell style.
  </action>
  <verify>
    <automated>cd "D:/claude/FFUBuilder/FFUDevelopment" && pwsh -NoProfile -Command "Invoke-Pester -Path 'Tests/Unit/FFUUI.Core.Handlers.Tests.ps1','Tests/Unit/FFUUI.Core.ConfigValidation.Tests.ps1' -Output Minimal" ; echo "EXIT=$?"</automated>
  </verify>
  <acceptance_criteria>
    - FFUUI.Core.Handlers.Tests.ps1 contains assertions for dispCtrl/tier/warnCtrl, Degraded/Error switch cases, and DarkOrange.
    - ConfigValidation tests contain a Disposition-restore assertion and an Include-removal assertion for USB artifacts.
    - Both files parse; Pester Discovery clean; new Its present (RED allowed).
    - Pre-existing tests in both files still pass.
  </acceptance_criteria>
  <done>Handlers + ConfigValidation tests extended; Discovery clean; new disposition/4-status Its present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds test files only — no runtime trust boundary is crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-01 | Tampering | New test files driving CI gate | accept | Tests are read-only assertions over source text and enum values; no execution of artifacts, no network, no privileged ops. N/A justification: test-only plan. |
| T-50-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan. Pester is already an installed dependency (project standard). |
</threat_model>

<verification>
Run all four test files: `cd FFUDevelopment && pwsh -NoProfile -Command ".\Tests\Unit\Invoke-PesterTests.ps1"` — Discovery must be clean across the suite. New Phase 50 Its are allowed to be RED (this is Wave 0); they MUST turn green by plan 05/06.
</verification>

<success_criteria>
- SelectiveRebuild.Tests.ps1 exists and is discovered by Pester.
- USBOnlyMode / Handlers / ConfigValidation tests carry the new Phase 50 assertions.
- No Discovery-phase parse errors anywhere; no regressions to existing green tests.
</success_criteria>

<output>
Create `.planning/phases/50-selective-rebuild-pipeline/50-01-SUMMARY.md` when done.
</output>
