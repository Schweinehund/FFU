---
phase: 48-config-aware-revalidation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - FFUDevelopment/BuildFFUVM_UI.xaml
  - FFUDevelopment/FFUUI.Core/FFUUI.Core.Initialize.psm1
autonomous: true

must_haves:
  truths:
    - "Staleness banner XAML (borderStaleResults, txtStaleResults) exists between hypervisor info banner and progress panel"
    - "Export Diagnostics button (btnExportDiagnostics) exists near Refresh Checks button with IsEnabled=False"
    - "All new XAML controls are registered in Initialize-UIControls via FindName"
  artifacts:
    - path: "FFUDevelopment/BuildFFUVM_UI.xaml"
      provides: "Staleness banner and Export Diagnostics button XAML elements"
      contains: "borderStaleResults"
    - path: "FFUDevelopment/FFUUI.Core/FFUUI.Core.Initialize.psm1"
      provides: "Control registration for new dashboard elements"
      contains: "btnExportDiagnostics"
  key_links:
    - from: "BuildFFUVM_UI.xaml"
      to: "FFUUI.Core.Initialize.psm1"
      via: "x:Name references matched by FindName calls"
      pattern: "FindName.*borderStaleResults"
---

<objective>
Add XAML elements for the staleness banner and Export Diagnostics button to the Home tab, and register all new controls in Initialize-UIControls.

Purpose: Establish the visual structure for Phase 48 features. The staleness banner provides config-change awareness, and the Export button enables support diagnostics. Plan 03 wires the behavior.

Output: Updated BuildFFUVM_UI.xaml with new elements; updated FFUUI.Core.Initialize.psm1 with control registrations.
</objective>

<execution_context>
@C:\Users\joanderson\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\joanderson\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/48-config-aware-revalidation/48-CONTEXT.md
@.planning/phases/48-config-aware-revalidation/48-RESEARCH.md
@FFUDevelopment/BuildFFUVM_UI.xaml
@FFUDevelopment/FFUUI.Core/FFUUI.Core.Initialize.psm1
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add staleness banner and Export Diagnostics button to XAML</name>
  <files>FFUDevelopment/BuildFFUVM_UI.xaml</files>
  <action>
Add two new elements to the Home tab dashboard layout in BuildFFUVM_UI.xaml.

**1. Staleness Banner** — Insert AFTER the existing `borderHypervisorInfo` element (line ~92) and BEFORE the `pnlDashboardProgress` element (line ~94):

```xml
<!-- Staleness Banner (Phase 48: CFG-02) -->
<Border x:Name="borderStaleResults" Background="#FFF8E1" Padding="8" CornerRadius="3"
        Margin="0,4,0,4" Visibility="Collapsed">
    <StackPanel Orientation="Horizontal">
        <TextBlock Text="&#x26A0;" FontSize="14" Foreground="#F57F17" Margin="0,0,6,0" VerticalAlignment="Center"/>
        <TextBlock x:Name="txtStaleResults" Text="" Foreground="#F57F17" FontSize="12" VerticalAlignment="Center"/>
    </StackPanel>
</Border>
```

This uses amber/orange (`#FFF8E1` background, `#F57F17` text) per the user decision. The warning symbol `&#x26A0;` (⚠) is consistent with the existing dashboard warning indicators. Starts collapsed — shown by the SelectionChanged handler.

**2. Export Diagnostics Button** — Modify the existing Refresh button line to become a horizontal StackPanel containing both buttons. The current line at ~101 is:
```xml
<Button x:Name="btnRefreshChecks" Content="Refresh Checks" Width="140" HorizontalAlignment="Left" Margin="0,0,0,10" Padding="8,4"/>
```

Replace with:
```xml
<!-- Action Buttons (Phase 48: Export Diagnostics next to Refresh) -->
<StackPanel Orientation="Horizontal" Margin="0,0,0,10">
    <Button x:Name="btnRefreshChecks" Content="Refresh Checks" Width="140" Padding="8,4"/>
    <Button x:Name="btnExportDiagnostics" Content="Export Diagnostics" Width="160" Margin="8,0,0,0" Padding="8,4" IsEnabled="False"/>
</StackPanel>
```

The Export button starts with `IsEnabled="False"` per user decision (grayed out until first check completes). It sits to the right of Refresh, separated by 8px margin. Width 160 accommodates the longer text.

Do NOT modify any other tabs, the Window.Resources, or any existing controls beyond what's specified.
  </action>
  <verify>
Open BuildFFUVM_UI.xaml and confirm:
- `borderStaleResults` exists between `borderHypervisorInfo` and `pnlDashboardProgress`
- `txtStaleResults` is inside `borderStaleResults`
- `btnExportDiagnostics` exists next to `btnRefreshChecks` in a StackPanel
- `btnExportDiagnostics` has `IsEnabled="False"`
- XAML is well-formed (no unclosed tags)
- No other elements were changed
  </verify>
  <done>Staleness banner (borderStaleResults, txtStaleResults) and Export Diagnostics button (btnExportDiagnostics) added to Home tab XAML. Banner starts collapsed, export button starts disabled.</done>
</task>

<task type="auto">
  <name>Task 2: Register new controls in Initialize-UIControls</name>
  <files>FFUDevelopment/FFUUI.Core/FFUUI.Core.Initialize.psm1</files>
  <action>
In the `Initialize-UIControls` function in FFUUI.Core.Initialize.psm1, add FindName registrations for the 3 new controls. Add them in a clearly labeled section after the existing Phase 47 dashboard controls (after the `borderHypervisorInfo` and `txtHypervisorInfo` registrations).

Add a comment: `# ---------- Phase 48: Config-Aware Revalidation Controls ----------`

Register:
- `$State.Controls.borderStaleResults = $window.FindName('borderStaleResults')`
- `$State.Controls.txtStaleResults = $window.FindName('txtStaleResults')`
- `$State.Controls.btnExportDiagnostics = $window.FindName('btnExportDiagnostics')`

That's 3 new control registrations. Do NOT modify any existing control registrations.
  </action>
  <verify>
Grep for `borderStaleResults`, `txtStaleResults`, and `btnExportDiagnostics` in FFUUI.Core.Initialize.psm1 — all three should have FindName lines.
  </verify>
  <done>All 3 new controls registered in Initialize-UIControls with Phase 48 section labeling. FindName keys match XAML x:Name attributes exactly.</done>
</task>

</tasks>

<verification>
1. XAML is well-formed: No unclosed tags, proper nesting
2. All new x:Name values in XAML have matching FindName calls in Initialize-UIControls
3. Existing controls unchanged (btnRefreshChecks still exists and is functional)
4. Staleness banner positioned between hypervisor info and progress panel
5. Export button is adjacent to Refresh button
</verification>

<success_criteria>
- Staleness banner visible in Home tab layout (starts collapsed)
- Export Diagnostics button visible next to Refresh (starts disabled)
- All 3 new controls accessible via $State.Controls in PowerShell code
- No existing UI functionality affected
</success_criteria>

<output>
After completion, create `.planning/phases/48-config-aware-revalidation/48-02-SUMMARY.md`
</output>
