---
phase: 48-xaml-mode-toggle-usb-tab
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - FFUDevelopment/BuildFFUVM_UI.xaml
autonomous: true
requirements:
  - UIMODE-01
  - UIMODE-02

must_haves:
  truths:
    - "A RadioButton group with 'Full Build' and 'USB from Existing' is visible above the TabControl"
    - "All Full Build tabs have x:Name attributes for programmatic visibility control"
    - "Full Build tabs default to Visible, USB Mode tab defaults to Collapsed"
    - "The XAML parses without exceptions via XamlReader"
  artifacts:
    - path: "FFUDevelopment/BuildFFUVM_UI.xaml"
      provides: "Mode toggle RadioButtons and x:Name on all tabs"
      contains: "rbFullBuild"
  key_links:
    - from: "FFUDevelopment/BuildFFUVM_UI.xaml"
      to: "FFUDevelopment/BuildFFUVM_UI.ps1"
      via: "FindName discovers new x:Name controls"
      pattern: "x:Name=\"rb(FullBuild|USBMode)\""
---

<objective>
Add a mode toggle RadioButton group to the main UI XAML and assign x:Name attributes to all Full Build tabs so Phase 49 can programmatically toggle their visibility.

Purpose: UIMODE-01 (mode switch control) and UIMODE-02 (build controls hide when USB Mode active) require named controls in the XAML. This plan adds the toggle row and names all tabs.

Output: Modified BuildFFUVM_UI.xaml with mode toggle row and named TabItems.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-xaml-mode-toggle-usb-tab/48-CONTEXT.md

<interfaces>
<!-- Existing XAML structure the executor needs to know -->

From FFUDevelopment/BuildFFUVM_UI.xaml:
- Line 56: `<Grid Margin="10">` — root Grid
- Lines 57-72: Grid.RowDefinitions (Row 0 = TabControl with Height="*", Rows 1-13 = Auto for progress/status/buttons)
- Line 75: `<TabControl x:Name="MainTabControl" TabStripPlacement="Left" ... Grid.Row="0">`
- Line 77: `<TabItem Header="Home" Padding="20">` — NO x:Name (shared tab)
- Line 86: `<TabItem Header="VM Settings" Padding="20">` — NO x:Name (Full Build tab)
- Line 223: `<TabItem Header="Windows Settings" Padding="20">` — NO x:Name
- Line 301: `<TabItem Header="Updates" Padding="20">` — NO x:Name
- Line 334: `<TabItem Header="Applications" Padding="20">` — NO x:Name
- Line 541: `<TabItem Header="M365 Apps/Office" Padding="20">` — NO x:Name
- Line 587: `<TabItem Header="Drivers" Padding="20">` — NO x:Name
- Line 735: `<TabItem Header="Build" Padding="20">` — NO x:Name
- Line 924: `<TabItem Header="Monitor" x:Name="MonitorTab" Padding="20">` — HAS x:Name (shared)
- Line 931: `<TabItem Header="About" x:Name="AboutTab" Padding="20">` — HAS x:Name (shared)
- Line 990: `</TabControl>` — end of tab control
- Lines 992-1002: Progress bar, status, buttons below TabControl
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add mode toggle RadioButton row and name all tabs</name>
  <files>FFUDevelopment/BuildFFUVM_UI.xaml</files>
  <read_first>
    - FFUDevelopment/BuildFFUVM_UI.xaml (full file — understand current Grid row layout, TabControl position, all TabItem declarations)
    - .planning/phases/48-xaml-mode-toggle-usb-tab/48-CONTEXT.md (decisions D-01 through D-12, D-20 through D-22)
  </read_first>
  <action>
Modify FFUDevelopment/BuildFFUVM_UI.xaml with the following changes:

**1. Insert a new Grid row for the mode toggle (per D-01):**

In the root Grid.RowDefinitions (lines 57-72), insert a new `<RowDefinition Height="Auto"/>` as the FIRST row (before the current Row 0 which has Height="*" for the TabControl). This shifts the TabControl to Grid.Row="1" and all subsequent rows (progress bar, status, buttons) increment by 1.

After the change, the row layout will be:
- Row 0: Auto — Mode toggle (NEW)
- Row 1: * — TabControl (was Row 0)
- Row 2-14: Auto — Progress, status, buttons (each incremented by 1)

Update ALL Grid.Row references below the RowDefinitions:
- `<TabControl ... Grid.Row="0">` becomes `Grid.Row="1"`
- `<ProgressBar ... Grid.Row="1"` becomes `Grid.Row="2"`
- `<TextBlock x:Name="txtStatus" Grid.Row="2"` becomes `Grid.Row="3"`
- `<StackPanel Grid.Row="3"` (buttons) becomes `Grid.Row="4"`

**2. Add the RadioButton group (per D-01, D-02, D-03, D-04, D-05, D-06):**

Insert before the TabControl, in Grid.Row="0":

```xml
<!-- Mode Toggle: Full Build vs USB from Existing (per D-01, D-06) -->
<StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10,5,10,5" VerticalAlignment="Center">
    <TextBlock Text="Mode:" FontWeight="Bold" FontSize="14" VerticalAlignment="Center" Margin="0,0,10,0"/>
    <RadioButton x:Name="rbFullBuild" Content="Full Build" GroupName="ActiveMode" IsChecked="True" FontSize="14" VerticalAlignment="Center" Margin="0,0,15,0"/>
    <RadioButton x:Name="rbUSBMode" Content="USB from Existing" GroupName="ActiveMode" FontSize="14" VerticalAlignment="Center"/>
</StackPanel>
```

Key details per decisions:
- x:Name="rbFullBuild" and x:Name="rbUSBMode" (D-06)
- Content labels "Full Build" and "USB from Existing" (D-03)
- GroupName="ActiveMode" (D-04)
- IsChecked="True" on rbFullBuild (D-05 — default is FullBuild)
- No icons, no descriptions (D-02)
- Inline horizontal layout (D-02)

**3. Add x:Name to all Full Build tabs that lack them (per D-07):**

These 7 tabs need x:Name added for Phase 49 visibility toggling:
- `<TabItem Header="VM Settings" Padding="20">` → add `x:Name="tabVMSettings"`
- `<TabItem Header="Windows Settings" Padding="20">` → add `x:Name="tabWindowsSettings"`
- `<TabItem Header="Updates" Padding="20">` → add `x:Name="tabUpdates"`
- `<TabItem Header="Applications" Padding="20">` → add `x:Name="tabApplications"`
- `<TabItem Header="M365 Apps/Office" Padding="20">` → add `x:Name="tabM365AppsOffice"`
- `<TabItem Header="Drivers" Padding="20">` → add `x:Name="tabDrivers"`
- `<TabItem Header="Build" Padding="20">` → add `x:Name="tabBuild"`

Format: `<TabItem Header="VM Settings" x:Name="tabVMSettings" Padding="20">`

Home, Monitor, and About tabs are SHARED (D-08) — Home does NOT need x:Name (it has no visibility toggling), Monitor and About already have x:Name.

**4. Do NOT add the USB Mode TabItem yet** — that is Plan 02.

**5. Do NOT add any event handlers or code-behind** — Phase 49 handles all wiring (D-12).
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "try { [xml]$x = Get-Content 'FFUDevelopment/BuildFFUVM_UI.xaml' -Raw; $ns = @{x='http://schemas.microsoft.com/winfx/2006/xaml'; d='http://schemas.microsoft.com/winfx/2006/xaml/presentation'}; $rb = $x.SelectNodes('//*[@Name=\"rbFullBuild\" or @Name=\"rbUSBMode\"]', (New-Object Xml.XmlNamespaceManager($x.NameTable))); Write-Host \"RadioButtons: $($rb.Count)\"; if($rb.Count -lt 2){throw 'Missing RadioButtons'}; Write-Host 'PASS' } catch { Write-Host \"FAIL: $_\"; exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    - FFUDevelopment/BuildFFUVM_UI.xaml contains `x:Name="rbFullBuild"` with `Content="Full Build"` and `GroupName="ActiveMode"` and `IsChecked="True"`
    - FFUDevelopment/BuildFFUVM_UI.xaml contains `x:Name="rbUSBMode"` with `Content="USB from Existing"` and `GroupName="ActiveMode"`
    - FFUDevelopment/BuildFFUVM_UI.xaml contains `x:Name="tabVMSettings"` on the VM Settings TabItem
    - FFUDevelopment/BuildFFUVM_UI.xaml contains `x:Name="tabWindowsSettings"` on the Windows Settings TabItem
    - FFUDevelopment/BuildFFUVM_UI.xaml contains `x:Name="tabUpdates"` on the Updates TabItem
    - FFUDevelopment/BuildFFUVM_UI.xaml contains `x:Name="tabApplications"` on the Applications TabItem
    - FFUDevelopment/BuildFFUVM_UI.xaml contains `x:Name="tabM365AppsOffice"` on the M365 Apps/Office TabItem
    - FFUDevelopment/BuildFFUVM_UI.xaml contains `x:Name="tabDrivers"` on the Drivers TabItem
    - FFUDevelopment/BuildFFUVM_UI.xaml contains `x:Name="tabBuild"` on the Build TabItem
    - TabControl has `Grid.Row="1"` (shifted from 0)
    - ProgressBar has `Grid.Row="2"` (shifted from 1)
    - txtStatus has `Grid.Row="3"` (shifted from 2)
    - Buttons StackPanel has `Grid.Row="4"` (shifted from 3)
    - XAML parses as valid XML: `[xml](Get-Content BuildFFUVM_UI.xaml -Raw)` does not throw
  </acceptance_criteria>
  <done>Mode toggle RadioButton group is visible in Grid.Row="0" above the TabControl. All 7 Full Build tabs have x:Name for programmatic visibility control. Grid.Row references updated for shifted layout. XAML parses without errors.</done>
</task>

</tasks>

<verification>
1. XAML file parses as valid XML
2. RadioButton controls rbFullBuild and rbUSBMode exist with correct attributes
3. All 7 Full Build tabs have x:Name attributes
4. Grid.Row values correctly shifted (+1) for TabControl, progress bar, status, and buttons
</verification>

<success_criteria>
- Mode toggle RadioButton pair visible in XAML with correct names, labels, GroupName, and default
- All Full Build tabs named for Phase 49 visibility toggling
- No XAML parse errors
- No changes to code-behind files (XAML structure only)
</success_criteria>

<output>
After completion, create `.planning/phases/48-xaml-mode-toggle-usb-tab/48-01-SUMMARY.md`
</output>
