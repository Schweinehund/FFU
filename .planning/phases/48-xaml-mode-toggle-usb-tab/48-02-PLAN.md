---
phase: 48-xaml-mode-toggle-usb-tab
plan: 02
type: execute
wave: 2
depends_on:
  - 48-01
files_modified:
  - FFUDevelopment/BuildFFUVM_UI.xaml
autonomous: true
requirements:
  - UIMODE-03

must_haves:
  truths:
    - "USB Mode tab exists in the XAML with artifact card layout"
    - "Required Artifacts section contains FFU and DeployISO cards with status/path/size/age TextBlocks"
    - "Optional Artifacts section contains Drivers, PPKG, Unattend, Autopilot, AppsISO cards"
    - "Each artifact card has a disabled CheckBox, disabled Browse button, and placeholder TextBlocks"
    - "USB Mode tab defaults to Visibility=Collapsed"
    - "XAML parses without exceptions"
  artifacts:
    - path: "FFUDevelopment/BuildFFUVM_UI.xaml"
      provides: "USB Mode TabItem with all artifact controls"
      contains: "usbModeTab"
  key_links:
    - from: "FFUDevelopment/BuildFFUVM_UI.xaml"
      to: "FFUDevelopment/Modules/FFU.ArtifactScanner/Classes/ArtifactScanner.Classes.ps1"
      via: "Control names map to ArtifactType enum values"
      pattern: "usb(FFU|DeployISO|Drivers|PPKG|Unattend|Autopilot|AppsISO)"
---

<objective>
Add the USB Mode TabItem to BuildFFUVM_UI.xaml with grouped artifact cards, per-artifact controls (CheckBox, Browse, status/path/size/age TextBlocks), and FFU-specific metadata fields.

Purpose: UIMODE-03 requires artifact-focused controls visible when USB Mode is active. This plan creates all the XAML structure that Phase 49 will wire to scanner data and event handlers.

Output: Modified BuildFFUVM_UI.xaml with complete USB Mode tab content.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-xaml-mode-toggle-usb-tab/48-CONTEXT.md
@.planning/phases/48-xaml-mode-toggle-usb-tab/48-01-SUMMARY.md

<interfaces>
<!-- ArtifactScanner data model — controls must map to these types -->

From FFUDevelopment/Modules/FFU.ArtifactScanner/Classes/ArtifactScanner.Classes.ps1:
```powershell
enum ArtifactType {
    FFU         # Required
    DeployISO   # Required
    Drivers     # Optional
    PPKG        # Optional
    Unattend    # Optional
    Autopilot   # Optional
    AppsISO     # Optional
}

class ArtifactResult {
    [ArtifactType]   $ArtifactType
    [ArtifactStatus] $Status       # Found/Missing/Error/Degraded
    [string]         $FilePath
    [long]           $FileSizeBytes
    [int]            $AgeDays
    [FFUMetadata]    $Metadata     # FFU-only: WindowsVersion, WindowsSKU, Architecture
}
```

<!-- Existing XAML patterns to follow -->
Tab definition pattern: `<TabItem Header="Name" x:Name="tabName" Padding="20">`
GroupBox pattern: `<GroupBox Header="Section Name" Margin="0,0,0,15">`
Browse button pattern: Grid with TextBox width="*" + Button width="80"
CheckBox pattern: `<CheckBox x:Name="chkName" Content="Label" IsEnabled="False"/>`

<!-- Control naming convention from D-20, D-21, D-22 -->
All USB controls use `usb` prefix: usb{ArtifactType}{Property}
Tab name: usbModeTab
Examples: usbFFUStatus, usbFFUPath, usbFFUBrowse, usbFFUInclude, usbFFUSize, usbFFUAge
FFU metadata: usbFFUVersion, usbFFUSKU, usbFFUArch
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add USB Mode TabItem with Required Artifacts section</name>
  <files>FFUDevelopment/BuildFFUVM_UI.xaml</files>
  <read_first>
    - FFUDevelopment/BuildFFUVM_UI.xaml (full file — find insertion point after Build tab and before Monitor tab, study GroupBox/Grid patterns from Drivers tab lines 587-735)
    - FFUDevelopment/Modules/FFU.ArtifactScanner/Classes/ArtifactScanner.Classes.ps1 (ArtifactType enum, ArtifactResult properties, FFUMetadata properties)
    - .planning/phases/48-xaml-mode-toggle-usb-tab/48-CONTEXT.md (decisions D-13 through D-22)
  </read_first>
  <action>
Insert a new TabItem in BuildFFUVM_UI.xaml between the Build tab (closes around line 921) and the Monitor tab (starts around line 924, after Plan 01 shifts). The USB Mode tab must:

**Tab declaration (per D-18, D-19, D-22):**
```xml
<TabItem Header="USB Mode" x:Name="usbModeTab" Padding="20" Visibility="Collapsed">
```

**Overall structure (per D-13, D-14):**
```xml
<ScrollViewer VerticalScrollBarVisibility="Auto">
    <StackPanel Margin="10">
        <!-- Header -->
        <TextBlock Text="USB from Existing Artifacts" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>
        <TextBlock Text="Create deployment USB media from pre-built artifacts without running the full build pipeline." TextWrapping="Wrap" Foreground="#666666" Margin="0,0,0,15"/>

        <!-- Required Artifacts GroupBox (D-14) -->
        <GroupBox Header="Required Artifacts" Margin="0,0,0,15" Padding="10">
            <StackPanel>
                <!-- FFU Artifact Card -->
                {FFU card content - see below}

                <Separator Margin="0,10"/>

                <!-- Deploy ISO Artifact Card -->
                {DeployISO card content - see below}
            </StackPanel>
        </GroupBox>

        <!-- Optional Artifacts GroupBox (D-14) -->
        <GroupBox Header="Optional Artifacts" Margin="0,0,0,15" Padding="10">
            <StackPanel>
                {Drivers card}
                <Separator Margin="0,10"/>
                {PPKG card}
                <Separator Margin="0,10"/>
                {Unattend card}
                <Separator Margin="0,10"/>
                {Autopilot card}
                <Separator Margin="0,10"/>
                {AppsISO card}
            </StackPanel>
        </GroupBox>
    </StackPanel>
</ScrollViewer>
```

**FFU Artifact Card structure (per D-15, D-16, D-17, D-20, D-21):**

Each artifact card follows this Grid layout pattern:
```xml
<!-- FFU Artifact -->
<Grid Margin="5">
    <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>  <!-- Row 0: Type label + include checkbox -->
        <RowDefinition Height="Auto"/>  <!-- Row 1: Status -->
        <RowDefinition Height="Auto"/>  <!-- Row 2: Path + Browse -->
        <RowDefinition Height="Auto"/>  <!-- Row 3: Size + Age -->
        <RowDefinition Height="Auto"/>  <!-- Row 4: FFU metadata (FFU only) -->
    </Grid.RowDefinitions>

    <!-- Row 0: Label + Include checkbox -->
    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,5">
        <CheckBox x:Name="usbFFUInclude" Content="FFU Image" FontWeight="Bold" FontSize="13" IsEnabled="False" IsChecked="True" VerticalAlignment="Center"/>
    </StackPanel>

    <!-- Row 1: Status -->
    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="20,0,0,3">
        <TextBlock Text="Status: " Foreground="#888888" FontSize="11"/>
        <TextBlock x:Name="usbFFUStatus" Text="(scanning...)" FontSize="11" Foreground="#888888" FontStyle="Italic"/>
    </StackPanel>

    <!-- Row 2: Path + Browse button -->
    <Grid Grid.Row="2" Margin="20,0,0,3">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Column="0" Text="Path: " Foreground="#888888" FontSize="11" VerticalAlignment="Center"/>
        <TextBlock x:Name="usbFFUPath" Grid.Column="1" Text="(not scanned)" FontSize="11" Foreground="#888888" FontStyle="Italic" TextTrimming="CharacterEllipsis" VerticalAlignment="Center" Margin="0,0,5,0"/>
        <Button x:Name="usbFFUBrowse" Grid.Column="2" Content="Browse..." Width="75" FontSize="11" IsEnabled="False" VerticalAlignment="Center"/>
    </Grid>

    <!-- Row 3: Size + Age -->
    <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="20,0,0,3">
        <TextBlock Text="Size: " Foreground="#888888" FontSize="11"/>
        <TextBlock x:Name="usbFFUSize" Text="--" FontSize="11" Foreground="#888888" Margin="0,0,15,0"/>
        <TextBlock Text="Age: " Foreground="#888888" FontSize="11"/>
        <TextBlock x:Name="usbFFUAge" Text="--" FontSize="11" Foreground="#888888"/>
    </StackPanel>

    <!-- Row 4: FFU-specific metadata (per D-15) -->
    <StackPanel Grid.Row="4" Orientation="Horizontal" Margin="20,3,0,0">
        <TextBlock Text="Version: " Foreground="#888888" FontSize="11"/>
        <TextBlock x:Name="usbFFUVersion" Text="--" FontSize="11" Foreground="#888888" Margin="0,0,10,0"/>
        <TextBlock Text="SKU: " Foreground="#888888" FontSize="11"/>
        <TextBlock x:Name="usbFFUSKU" Text="--" FontSize="11" Foreground="#888888" Margin="0,0,10,0"/>
        <TextBlock Text="Arch: " Foreground="#888888" FontSize="11"/>
        <TextBlock x:Name="usbFFUArch" Text="--" FontSize="11" Foreground="#888888"/>
    </StackPanel>
</Grid>
```

**DeployISO Card:** Same pattern as FFU but WITHOUT Row 4 (no metadata row). Control names:
- `usbDeployISOInclude` (CheckBox, IsEnabled="False", IsChecked="True")
- `usbDeployISOStatus` (TextBlock, "(scanning...)")
- `usbDeployISOPath` (TextBlock, "(not scanned)")
- `usbDeployISOBrowse` (Button, IsEnabled="False")
- `usbDeployISOSize` (TextBlock, "--")
- `usbDeployISOAge` (TextBlock, "--")
- CheckBox Content="WinPE Deploy ISO"

**Standard Optional Artifact Card pattern (for Drivers, PPKG, Unattend, Autopilot, AppsISO):**
Same as DeployISO (no metadata row). 4 rows: label+checkbox, status, path+browse, size+age.

Control names for each (using `usb{Type}{Property}` per D-21):

Drivers:
- `usbDriversInclude` (CheckBox, Content="Drivers", IsEnabled="False", IsChecked="False")
- `usbDriversStatus`, `usbDriversPath`, `usbDriversBrowse`, `usbDriversSize`, `usbDriversAge`

PPKG:
- `usbPPKGInclude` (CheckBox, Content="Provisioning Package", IsEnabled="False", IsChecked="False")
- `usbPPKGStatus`, `usbPPKGPath`, `usbPPKGBrowse`, `usbPPKGSize`, `usbPPKGAge`

Unattend:
- `usbUnattendInclude` (CheckBox, Content="Unattend.xml", IsEnabled="False", IsChecked="False")
- `usbUnattendStatus`, `usbUnattendPath`, `usbUnattendBrowse`, `usbUnattendSize`, `usbUnattendAge`

Autopilot:
- `usbAutopilotInclude` (CheckBox, Content="Autopilot Profile", IsEnabled="False", IsChecked="False")
- `usbAutopilotStatus`, `usbAutopilotPath`, `usbAutopilotBrowse`, `usbAutopilotSize`, `usbAutopilotAge`

AppsISO:
- `usbAppsISOInclude` (CheckBox, Content="Applications ISO", IsEnabled="False", IsChecked="False")
- `usbAppsISOStatus`, `usbAppsISOPath`, `usbAppsISOBrowse`, `usbAppsISOSize`, `usbAppsISOAge`

**Key details:**
- Required artifacts (FFU, DeployISO) have `IsChecked="True"` on their CheckBox
- Optional artifacts have `IsChecked="False"` on their CheckBox
- ALL CheckBoxes and Browse buttons have `IsEnabled="False"` (D-16, D-17)
- Tab has `Visibility="Collapsed"` (D-18)
- Tab is positioned after Build tab, before Monitor tab (D-19)
- Total named controls: 1 tab + 7 artifacts * (Include + Status + Path + Browse + Size + Age) = 43 controls + 3 FFU metadata = 46 new named controls
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "try { [xml]$x = Get-Content 'FFUDevelopment/BuildFFUVM_UI.xaml' -Raw; $content = Get-Content 'FFUDevelopment/BuildFFUVM_UI.xaml' -Raw; $checks = @('usbModeTab','usbFFUInclude','usbFFUStatus','usbFFUPath','usbFFUBrowse','usbFFUSize','usbFFUAge','usbFFUVersion','usbFFUSKU','usbFFUArch','usbDeployISOInclude','usbDeployISOBrowse','usbDriversInclude','usbPPKGInclude','usbUnattendInclude','usbAutopilotInclude','usbAppsISOInclude'); $missing = @(); foreach($c in $checks) { if($content -notmatch $c) { $missing += $c } }; if($missing.Count -gt 0){throw \"Missing controls: $($missing -join ', ')\"}; if($content -notmatch 'Visibility=.Collapsed.'){throw 'Missing Collapsed visibility'}; Write-Host 'PASS: All USB Mode controls present' } catch { Write-Host \"FAIL: $_\"; exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    - FFUDevelopment/BuildFFUVM_UI.xaml contains `x:Name="usbModeTab"` with `Visibility="Collapsed"` and `Header="USB Mode"`
    - FFUDevelopment/BuildFFUVM_UI.xaml contains `GroupBox Header="Required Artifacts"` with FFU and DeployISO cards inside
    - FFUDevelopment/BuildFFUVM_UI.xaml contains `GroupBox Header="Optional Artifacts"` with Drivers, PPKG, Unattend, Autopilot, AppsISO cards inside
    - FFU card has all 10 named controls: usbFFUInclude, usbFFUStatus, usbFFUPath, usbFFUBrowse, usbFFUSize, usbFFUAge, usbFFUVersion, usbFFUSKU, usbFFUArch (plus the CheckBox)
    - DeployISO card has 6 named controls: usbDeployISOInclude, usbDeployISOStatus, usbDeployISOPath, usbDeployISOBrowse, usbDeployISOSize, usbDeployISOAge
    - Each optional artifact (Drivers, PPKG, Unattend, Autopilot, AppsISO) has 6 named controls following usb{Type}{Property} pattern
    - All CheckBox controls have `IsEnabled="False"`
    - All Button controls have `IsEnabled="False"`
    - Required artifact CheckBoxes (FFU, DeployISO) have `IsChecked="True"`
    - Optional artifact CheckBoxes have `IsChecked="False"`
    - USB Mode tab is positioned between Build tab and Monitor tab in the XAML source
    - XAML parses as valid XML: `[xml](Get-Content BuildFFUVM_UI.xaml -Raw)` does not throw
  </acceptance_criteria>
  <done>USB Mode TabItem exists with 7 artifact cards in Required/Optional GroupBoxes. All 46 named controls are present with correct disabled states and placeholder text. Tab defaults to Collapsed. XAML parses without errors.</done>
</task>

</tasks>

<verification>
1. XAML file parses as valid XML
2. USB Mode tab positioned between Build and Monitor tabs
3. All 46+ named USB Mode controls present with usb prefix
4. Required/Optional grouping correct (FFU+DeployISO required, 5 others optional)
5. FFU card includes metadata fields (Version, SKU, Arch)
6. All interactive controls (CheckBox, Button) are disabled
7. Tab visibility is Collapsed by default
</verification>

<success_criteria>
- USB Mode tab has complete artifact card layout matching ArtifactType enum
- All controls named per D-20/D-21 convention for Phase 49 wiring
- No XAML parse errors
- No code-behind changes (XAML structure only)
</success_criteria>

<output>
After completion, create `.planning/phases/48-xaml-mode-toggle-usb-tab/48-02-SUMMARY.md`
</output>
